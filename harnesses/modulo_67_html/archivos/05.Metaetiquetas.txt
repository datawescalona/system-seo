Entramos en el quinto y último punto de este curso donde hablaremos de las metáetquetas. Uno de los elementos que se encuentran dentro de la etiqueta HEAD, de la estructura de una página HTML, son los metadatos. Los principales podríamos decir que son los siguientes. Bueno, especificar la codificación de la página, la descripción de la misma, keywords principales, autor. Podemos hacer una metáetiqueta para forzar que el contenido de la página se actualice cada x minutos, en este ejemplo cada 30 minutos. Podemos especificar la metáetiqueta robots con atributos como index, follow, que veremos posteriormente. Y la metáetiqueta viewport, con atributos específicos para ver el visionado de la página web desde dispositivos móviles. Bueno, en estos pantallazos vemos ejemplos de la página web, con esta etiqueta del viewport puesta y sin poner. Dentro de los atributos para ser más importante, está el atributo robots. Robots hace referencia a la forma que tendrán para actuar todos los rastreadores que entren en nuestra página web. Robots es la más conocida, pero tenemos que tener en cuenta que podemos crear un metadato por cada robot, como por ejemplo Googlebot. Podríamos decir que es una página web que es Googlebot. Podríamos decidir que Googlebot ponerlo con etiqueta no index y por ejemplo robots todo el resto o ponerlo como index. Sería una configuración que podríamos hacer y así con los diferentes robots o rastreadores que existan. Los principales valores para el atributo content de estos robots o rastreadores son los siguientes. Tenemos all, que significa que hay restricciones de indexación y publicación. Esta directiva es el valor predeterminado y no tiene ningún efecto si se utiliza de forma explícito. Tenemos los atributos no index que lo que hace es que no se muestre la página de los resultados de búsqueda y el no follow, que nos siguen los enlaces de esta página. Y por otro lado tenemos el atributo no, que es equivalente a incluir no index y no follow en este contenido. Dentro de la etiqueta GAD de HTML también encontramos diferentes enlaces como los siguientes. A nivel deseo son importantes los idiomas, como hemos visto anteriormente. Tenerlos bien especificados pues tenemos que tener el atributo rel con el valor alternate que si recordamos lo que estamos diciendo es que es una versión alternativa a la página web que estamos consultando actualmente, sería la misma página web con los mismos contenidos pero en otro idioma. Y el ACRFLAN, que es un atributo importante también cuando estamos enlazando a páginas con otro idioma. Entonces en este caso pues estamos especificando que es el español. La etiqueta Canonical también es muy importante de Carl Seo y lo que hace es especificar a los rastreadores que estamos hablando de una página con contenido original. Las etiquetas con enlaces a... bueno lo que hemos comentado anteriormente, cuando tenemos una página con paginación, es importante tener en el GAD de la página en enlace a la página anterior y a la página siguiente. Y también en el GAD incluimos la etiqueta link con el atributo rel y el valor icon que básicamente es para el favicon de la página web, que es el iconito que se ve en la pestaña del navegador a la izquierda del título de la página. Muchas veces este icono, este enlace existe en varios tamaños y resoluciones, para adaptarse a si la página se ve desde móvil, desde navegador o desde varios dispositivos. También en el GAD de la página como comentábamos anteriormente, incluimos códigos de seguimiento, píxels o contenedores. Es muy común cuando queremos analizar y guardar información de las visitas que se producen en nuestro sitio web e tener que configurar o añadir información de rastreo en nuestra página. Cada herramienta nos dará unas instrucciones concretas de qué tenemos que hacer para conseguir el seguimiento. Por ejemplo, Google Analytics nos dará sus instrucciones, otras herramientas nos pedirán que incluamos píxeles de conversión o en todas las páginas o solo en las páginas de conversión, una vez por ejemplo el usuario ha rellenado el formulario de solicitud de información, etcétera. Toda esta información se suele incluir en el GAD. Para no tener que recurrir a los desarrolladores y cambiar el código de la página web cada vez que necesitamos una modificación de este tipo, normalmente se recurre a Google Tag Manager, que es una herramienta que nos permite añadir muchos códigos de seguimiento y otros tipos de código dentro de nuestra página web sin necesidad de cambiar el código. El funcionamiento es, seguimos las instrucciones de instalación de Google Tag Manager, aquí os pongo un ejemplo de lo que sería el script de seguimiento que añadiríamos en el GAD de la página. A partir de aquí, esta herramienta nos permite añadir dentro de este contenedor otro tipo de código. Además se puede configurar de forma que se añada solo en una página, se añada solo en una URL en concreto, se añada en todo el sitio. Podemos configurar, es una herramienta muy potente que podemos configurar de muchas formas y nos puede ayudar mucho a todas estas tareas de incluir códigos dentro del GAD de la página. Y bien, con esto finalizamos el curso de HTML. Espero que os haya gustado y bueno, nos vemos en otros cursos. Muchas gracias.