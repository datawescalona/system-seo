Hola de nuevo, bienvenidos al tercer punto de la asignatura de servidores del Master Deseo de Vixeo, donde hablaremos de los servidores de NS. Los servidores de NS hoy en día son fundamentales para el correcto funcionamiento de internet y nos facilitan en navegar a varias páginas webs porque nos permiten aprendernos un nombre de dominio, como Google.es, y no tener que aprenderse la dirección IP del servidor que aloja esta página. Los servidores de NS convierten un nombre de dominio a una dirección IP o viceversa. Son un componente primordial para el correcto funcionamiento de internet. El alta de páginas públicas se realiza comprando un dominio a empresas acreditadas. Se propagan a nivel mundial, existen diferentes tipos de registro y trabajan con el puerto 53. Cuando nosotros queremos publicar nuestra propia página web y nos interesa que esta sea accesible desde cualquier parte del mundo, es necesario que compremos un nombre de dominio. Por ejemplo, mi página punto es, es un nombre de dominio. Existen empresas en internet acreditadas para realizar este tipo de trámite y que son las que nos ofertan estos nombres de dominio y a las que podemos recurrir para comprarlo. Una vez compramos un nombre de dominio, la compra del nombre de dominio es temporal, puede ser un año, dos años, cinco años, diez años, no sé todas las franjas de tiempo en las cuales podemos reservar un dominio, pero sí que hay que tener claro que no es algo permanente y que cada año, cada dos años, cada cinco años tienes que entrar y renovar esta compra. Una vez compras este nombre de dominio, tú únicamente tienes el nombre y lo que tienes que hacer es apuntar una vez compras el nombre de dominio en la empresa o página que hayas decidido, lo que tienes que hacer es entrar en la configuración del mismo y configurar las DNS, de forma que si yo tengo mi dominio.com, mi dominio.es tengo que especificar a qué dirección IP quiero apuntar. La dirección IP de una máquina identifica un equipo dentro de toda la red mundial de internet y es el número que utilizan los routers para encontrar el servidor, en este caso, que estamos buscando. Como a nivel de internet los routers necesitan este número, esta dirección IP para encontrar el destino que estamos buscando, tendríamos que aprendernos de memoria todas las direcciones IP de todas las páginas web que quisiéramos consultar. Como esto es completamente imposible, se crea el servidor DNS que lo que hace es asociar este número a un nombre de dominio. Por lo tanto, una vez compramos un nombre de dominio, tenemos que entrar en esta plataforma y configurar a dónde queremos que apunte. En un nombre de dominio, quiero que apunte a esta máquina, a esta IP. Y entonces, esto se propagará a nivel mundial por todos los servidores DNS y a partir de ese momento, los servidores DNS transformarán el nombre de dominio en la dirección IP para que los routers sepan llegar al destino que estamos buscando. Un ejemplo de alta de estos registros en la página web denominalea.com es el siguiente de aquí. Aquí vemos dos dominios, linkfp.es y matrícula.linkfp.es que tienen un registro del tipo A a una dirección IP en concreto. Es muy común que varios dominios puedan apuntar a una dirección IP, a la misma dirección IP, porque dentro de los servidores webs hay un concepto que se llama virtualhost, que si los configuramos correctamente, podemos alojar en el mismo servidor, por lo tanto, en la misma dirección IP, varias páginas webs con varios dominios. Entonces, una vez contratamos un dominio, lo que tenemos que hacer es darlo de alta. Cada página web, por ejemplo, en nominalia se visualiza así, pero el formato de visualización puede variar, según la página en la cual hayamos contratado el nombre de dominio. Nosotros, a nivel de nuestro equipo cliente, con el comando ipconfig.ol, podemos ver el servidor DNS que estamos utilizando, que puede ser el de nuestro proveedor de internet. Normalmente, estos servidores en redes domésticas se reparten de forma automática y se utilizan los que provee el mismo proveedor de internet. Pero bueno, nosotros somos libres también aquí de usar un servidor DNS propio, usar uno de otro proveedor, podemos utilizar el servidor DNS que queramos. Pues con este comando, podemos ver que servidor DNS estamos utilizando. Que en este caso, son el 2.16.146.35.35 y hay un servidor secundario, que en el caso de que el primero nos responda, pasaríamos hacia la consulta al segundo. Todo esto es automático, cuando la llegamos por internet, a nivel de protocolos de red, ya funcionan así. Y el segundo sería el 2.16.149.36.36. Bien, pues una vez damos de alta el DNS en nuestro proveedor de dominio, donde hayamos comprado el nombre de dominio, y verificamos que servidor DNS está utilizando en nuestra máquina para resolver, podemos ejecutar el siguiente comando, dentro de la consola de comandos de Windows, que es el comando NSLockup. Y poniendo NSLockup y el nombre de dominio, nos vale decir con nuestro servidor web predeterminado, que IP tiene, si lo encuentra, si no lo encuentra, y en el caso de que lo encuentre, que IP es la que resuelve. Con este comando, si no ponemos nada, estaremos usando los servidores DNS que hemos visto anteriormente, que son los que están configurados en nuestro equipo cliente, para navegar por internet. Pero si después del nombre de dominio, añadimos un espacio y una dirección IP, podemos especificar concretamente, que servidor DNS queremos que nos resuelva esta petición. Vale, entonces en este ejemplo, hemos puesto el servidor DNS 8888, que es el DNS de Google, y bueno, nos está diciendo, que en el primer caso, no reconoce de quién es este servidor DNS, pero nos da una respuesta, que linkfp.es corresponde a esta IP, que vemos a continuación, que si os fijáis es la misma que estaba de alta, anteriormente el DNS de la página de nombre de dominios, y en esta segunda consulta, el número que respondes lo mismo, vale, es para el servidor de Google linkfp.es, tiene la misma IP que para el servidor que estaba usando, mi máquina cliente. Y bueno, de esta forma, en el caso de que tengamos servidores DNS internos instalados por nuestra parte, pues con este comando podemos ir probando, que resuelve uno y que resuelve el otro. En principio, todos los servidores DNS, deberían de responder a un nombre de dominio en la misma IP, no tendría sentido que para un DNS fuese una, y para otros otra, vale, es prácticamente imposible, aunque nosotros podemos forzar esto de forma local. Todos los sistemas operativos, disponen de un fichero que comúnmente se le conoce, como cache del DNS, en el cual podemos forzar esta resolución, ¿eh? Aquí os pongo el ejemplo de la ruta, de dónde se encuentra este archivo en un equipo Windows, y este archivo es tan simple como introducir una dirección IP, tabular y un nombre de dominio, y a partir de aquí, independientemente de lo que conteste los servidores DNS, este equipo resolverá que linkyafp.es está en la IP 1.1.1.1.1, porque este archivo, a nivel de sistema operativo, está configurado para, cuando tiene que hacer una consulta de DNS, verifica el primero este archivo, y si en este archivo no está la resolución, preguntarla al servidor DNS que esté establecido. Pero en el caso de que en este archivo exista, ya no se pregunta al servidor DNS, y este es el prioritario y el que resuelve. Entonces, en algunos casos, podríamos usar esto. Este archivo puede ser útil también, porque en el caso de que los servidores DNS caigan o fallen o hay algún problema, que son problemas a nivel global, pero los hay, ¿vale? Que algún, yo qué sé. Por ejemplo, telefónica, pues el servidor DNS se ha caído. Pues si nosotros conocemos la IP de la página a la que nos queremos conectar, que sí es la de nuestra empresa, la tenemos que conocer, podemos utilizar este archivo para podernos conectar a páginas webs con servidores DNS caídos. Por otro lado, comentar también que existen páginas webs que son públicas y que comprueban el estado de un registro DNS a nivel mundial. Está de aquí, que se llama DNS Check. Si os fijáis, introduzco linkfp.es a izquierda, específico que es un registro de tipo A, le de a buscar, y me aparece que en muchos servidores del mundo, ¿vale? No son todos, obviamente, pero muchos de ellos y los que aparecen aquí son servidores bastante importantes, ¿vale? Pues me está dando información de qué IP tiene el registro linkfp.es en cada uno de estos servidores. ¿Cuándo se suelen consultar estas páginas webs? Bien, pues normalmente, cuando hay algún problema con DNS, o también cuando hacemos algún cambio. Puede ser que nosotros tengamos una página web alojada en una máquina y por lo que sea, de un día para otro, necesitemos cambiar esa máquina y tengamos que cambiar la IP que resuelve en nuestro nombre de dominio. En estos casos, comentar que como el servicio DNS se tiene que propagar a nivel mundial, ¿vale? Es un servicio que tiene unos servidores jerárquicos y los servidores grandes informan a los pequeños, etcétera, etcétera, etcétera, tiene un tiempo de propagación. Se dice que son 48 horas, ¿vale? Que un cambio de DNS de un nombre de dominio que lo cambio de servidor y que a partir de ahora quiero que en vez de consultar mi máquina local, consulte una máquina de Amazon porque es una migración, se dice que hay que tener en cuenta que puede tardar hasta 48 horas. Yo, en mi experiencia personal, nunca ha tardado tanto. Puede ser que como es algo de propagación normal en llegar al último servidor DNS al que esté más alejado, pues a lo mejor sí que hay algún sitio que ha tardado 48 horas. Pero yo he visto cambios casi inmediatos a nivel de ciudad, a nivel nacional, igual tarda 15 o 20 minutos en mi experiencia personal. Y sí que a nivel mundial se puede retrasar un poquito más este cambio, pero yo en ningún caso he vivido cambios de 48 horas. Entonces, estas herramientas, estas páginas, como DNS Checker, pues es importante conocerlas, saber que existen. Y bueno, yo, por ejemplo, yo trabajo en una empresa nacional que precisamente está aquí, linkyafp.es. Y cuando hemos hecho estos cambios, pues al final lo que yo he mirado es una página web equivalente a esta de aquí, de checkdns, pero más enfocada en servidores nacionales, para saber a nivel de España cuánto ha tardado en hacer esta propagación o para ir revisando si se está haciendo correctamente o no. Y en algunos casos que ha habido algún problema o me ha interesado saber algún servidor de DNS en concreto que aún no se había actualizado o casos ya más particulares, pues con el comando que hemos visto anteriormente en ese locup, si alguna de estas páginas me decía que aún no estaba funcionando bien, pues via comando, yo ejecutaba esta misma operación para ver si podía obtener más información de que estaba pasando y, y bueno, a fin de acabo, revisar que esta propagación se realice correctamente. Una vez a nivel nacional se ha hecho este traspaso y ya todos los servidores nacionales responden con la nueva dirección IP, pues sí que nos pasaríamos a herramientas como DNS Checker para revisar a nivel mundial cómo está yendo esta propagación. Como hemos comentado hasta ahora, todo el acceso público a páginas webs requiere de registrar nuestro servidor de DNS a nivel mundial, ¿vale? que se propagen, pero también nosotros podemos tener en nuestras empresas servidores DNS internos. Por ejemplo, si queremos usar Active Directory, que es la herramienta de Microsoft para la gestión de usuarios a nivel empresarial, esta herramienta para funcionar necesita sí o sí usar un servidor DNS. Aparte que tener un servidor DNS instalado dentro de tu propia red local, mejora rendimiento de las búsquedas en internet porque cada vez que tus usuarios buscan algo, no se tienen que ir a un servidor de DNS externo por internet, el servidor de DNS local responde a estas peticiones y esto hace que la respuesta sea mucho más rápida, ¿vale? al ser local que al tenerse que ir a internet. Entonces aquí os pongo un ejemplo de la captura de pantalla de lo que sería el servidor DNS de Microsoft instalado, ¿vale? Aquí se ha dado de alta, si os fijáis un registro tipo A con tres W y una dirección IP y un registro tipo A también de mail a otra IP. Normalmente, cuando nosotros tenemos un nombre de dominio, hay algunas aplicaciones que requieren de el lar de alta algunos registros DNS. Es muy común, aplicaciones como Google Analytics, Stack Manager, etcétera, etcétera, que cuando tú quieres registrar un dominio, ellos quieran validar que ese dominio es tuyo. Y muchas de estas aplicaciones, una de las formas que tienes para validar y comprobar que este dominio es tuyo, es mediante el servidor DNS. Normalmente te piden que generes un registro del tipo TXT y la misma aplicación, pues el mismo Google Analytics, dice, oye, para creerme que este dominio es tuyo y que tú tienes acceso y que tú lo puedes administrar, dame de alta este registro. Te dan las instrucciones de esta herramienta, tú tienes que entrar en la interfaz correspondiente y dar de alta este registro y después pedir a Google, en este caso, que te verifique que este registro lo has dado de alta. Entonces Google lanza la consulta, ve que has sido capaz de crear un registro en ese dominio, por lo tanto, considera que es de tu propiedad o que por lo menos eres tú que lo administra y ya te deja avanzar y te valida que ese dominio es tuyo. Ese registro TXT es temporal, Google lo quiere únicamente para la verificación, luego se puede eliminar sin ningún problema. Otras aplicaciones como, por ejemplo, los gestores de correos electrónicos, si yo tengo el nombre de dominio en nominalia, pero quiero trabajar con servidores de correo, de Google, de Microsoft o otro tipo de servidores, tendré que configurar estos DNS. Será una tarea que no será complicada al final es dar de alta registros y todas estas herramientas te especifican muy bien y muy concretamente los registros que has de dar. Cuando tú estás configurando el correo de Microsoft en este caso, pues te da una tablita, tienes que crear estos registros. Y una vez los tengas dados de alta, volver a la herramienta del servidor de correo, en este caso, que estemos configurando y darle a validar. Si que hay que tener en cuenta, a veces puede haber un delay de un tiempo de espera entre que tú aplicas los cambios y se ejecutan, pero como ya he comentado anteriormente, bajo mi experiencia personal, este tiempo de espera nunca ha sido muy elevado. Y bien, con esto acabamos ya el tercer punto del curso de servidores, del Master Deseo de Vixeo, y pasaremos al siguiente punto en el que hablaremos de otros servidores.