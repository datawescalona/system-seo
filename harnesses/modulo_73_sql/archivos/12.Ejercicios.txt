Ahora hemos visto esta parte teórica, vamos a ver unos casos concretos, simples, donde podéis usar el SQL. Primero, vamos a usar en este ejercicio una base de datos de prueba de una netix, podéis acceder a la misma base de datos gratis, si abrís una cuenta de well be query, tendréis un proyecto que se llama, como está indicado ahí, be query public data, y dentro hay una tabla que se llama, guanetics sample, es un extracto del año 2017 de una cuenta que seguramente se han inventado, para que podamos jugar un poco con los datos de guanetics, como si fuera una real. Si queremos las sesiones totales de esta base de datos, pues muy fácil, lo que hemos visto al final es que selecciono suma de visitas, as visits, le cambio el nombre, para que otra vez, lo reivito, lo reivito, pero es que vais a ver que una vez os va a pasar, no aparezca con un nombre medio raro, indico de dónde provienen los datos, y aquí proviene de, como estamos en be query, siempre es proyecto.conjuntodedata.datas, Hay importancia del móvil, eso puede ser interesante para vuestros clientes, queréis saber si para tal o tal el móvil es más usado que el desktop, muchas veces va a ser la respuesta, pero ojo que hay unos sectores como v2v y tal, donde se usa todavía más el desktop, hasta en 2021, así que ojo con esta hipotecis que no siempre es verdad, así que aquí como lo hacemos, seleccionamos la suma de las visitas, nos interesa saber si hay más visitas con dispositivos móviles o con desktop tablet, después seleccionamos la columna donde tenemos esta información, le ponemos el nombre, porque el nombre es un poco largo, y otra vez indicamos desde qué base de datos, y ahí así que agrupamos por mobile, porque aquí nos interesa tener una visión resumen, una tabla dinámica de la suma del tráfico 1, si está con mobile y 2, si no está con mobile, y veis aquí en este caso pues tenemos más visitas sin mobile, o sea, desde otros dispositivos que con mobile, que tiene sentido, igual porque esta tabla es del año 2017. Gran ventaja de este código, y ahí es donde vais a empezar a ver la ventaja del SQL, es que este código lo podéis aplicar para cualquier cliente, en caso que quisiéráis saber si un proyecto tiene más o menos visitas desde mobiles, pues tendréis que exportar los datos, o bueno, y ahora informe de IOTX si queréis, para tal o tal cliente, este caso copia ese código, lo pegáis, cambiais únicamente la fuente, el from, ahí con la base de datos de otro cliente, y está pues esta información mucho más rápido, que tiene que hacer todos los pasos para que den vuestros proyectos o clientes. Cada vez más importante, es también una información que nos interesa mucho cuando entra un cliente o un proyecto, saber si el tráfico orgánico es el canal más importante o está dentro de los más importantes, o hasta ahora no se ha trabajado nada y casi no hay tráfico orgánico, y ahí pues una estructura que ya hemos visto, suma de visitas, queremos también el channel grouping, que es el canal en este caso, la fuente agrupamos por canal y esta vez sí que ordenamos por visitas de mayor a menor, si nos aseguramos que arriba de nuestra tabla tengamos el canal más importante, que en estos datos que tenemos, canalizamos el canal más importante, es el tráfico orgánico. Y otra vez, quiero hacer la misma escipa a un cliente, copio, pego el código, modifico la parte de from y ya tengo el resultado, mucho más eficiente para repitir el análisis. Subtítulos por la comunidad de Amara.org