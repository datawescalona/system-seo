Vale, el front, que es el de donde queremos los datos, si trabajáis en local, en general es el nombre de base de datos y ya está. Si trabajáis en BigQuery, os pongo el ejemplo de BigQuery porque es lo que usamos en Nidigoo, pero realmente no sé cómo funcionar en los otros servicios, pues es una estructura un poco similar, tenéis que indicar el nombre del proyecto, el nombre del conjunto de datos, el nombre de la tabla. Vale, al final corresponde a la organización de los datos en una cuenta de BigQuery y tenéis que indicar los tres. Si indicáis únicamente el nombre de la tabla, os va a decir que no existe, que tenéis que indicar la estructura completa. Os pongo aquí, pero si trabajáis también con BigQuery, que por cierto, en general, si nunca habéis usado el servicio, os regarán créditos, así que lo podéis hacer sin ningún problema.