Bueno, vamos ya al contenido fuerte de la sinatura que es a lo que hayamos venido aquí que es hablar de frameworks. Con respecto a frameworks vamos a destacar primero los frameworks que sería por la parte del lado del servidor. Una vez ya está explicado todo lo que es el lenguaje del lado del cliente, lo que es el lenguaje del lado del servidor. En este caso nos encontramos con los frameworks, bueno vamos a hablar en este caso del lenguaje de PHP, pero que sepáis que existen otros lenguajes de servidor como pueden ser Java, como pueden ser Fyton y bueno en este caso el framework de PHP más conocido y el único es Laravel. Que la Laravel es un framework que está basado en Symphony. Symphony lo conoceréis como he explicado en otras ocasiones porque bueno al final es en lo que está basado por ejemplo PrestaSob y por ejemplo empresas puertes como pueden ser precios componentes tienen su framework basado en Symphony. Laravel pues bueno básicamente es una muy buena opción para que quiere tener un back totalmente personalizado con web complejas quiere decir cuando tú por ejemplo quieres tener una web que te permita un... vamos a poner un ejemplo un calendario gestionable que un profesor del back pueda seleccionar las horas en las que está disponible y que en el front el usuario puede elegir las horas en las que está disponible, lo ideal sería utilizar un framework tipo Laravel. Cuando quieres hacer por ejemplo un marketplace gigantesco antes que utilizar WordPress que te puede hacer muchísima llamada PHP te puede estar creando recursos totalmente necesarios afectando el VPO y haciendo el proyecto muy difícil de escalar lo mejor es utilizar frameworks de este tipo en este caso Laravel utiliza un motor de plantillas llamado blade.php que al final hace que sea PHP por cierto no lo he comentado Laravel es un framework PHP estas plantillas pues te permite agilizar básicamente el código y te permite bueno tener una facilidad a la hora de hacer el front end. Sin embargo esto puede ser sustituido por frameworks de front end que lo veremos ahora más adelante como pueden ser view o como puede ser angular. Cuando se quieren tener las cienes personalizadas tan complejas como podrías ser una web de velolacar que necesitas ciertas estadísticas necesites hacer ese almacenaje de información ese intercambio entre usuarios y necesitas controlar tantas cosas del back un entorno como el de WordPress tan cerrado te puede ser complicado con un entorno de framework de la Laravel pues puedes gestionar y puedes cambiar cosas distintas. No hay una única forma de estructurar un proyecto de Laravel pero bueno como hemos explicado antes es un framework y sí que te da un entorno de trabajo pero aún así hay muchísimas variedades de opciones en las que puedes escalar el proyecto de una forma de otra te permite una gestión grande de usuarios y bueno usted para lo que te acapetas ahora qué pasa estos entornos que son maravillosos para hacer web complejas que por cierto es muy difícil averiguar cuál es el framework que utiliza una web por dentro debido decir es muy difícil averiguar el framework de back que utiliza una web debido a que no es algo que se suela mostrar en el front y bien en este caso cuando se hacen webs de este tipo la colaboración entre el SEO y el webmaster es totalmente necesaria porque aquí no tenemos plugins tipo yo a SEO no tenemos plugins que estén adaptados realmente para SEO para un fácil entendimiento entonces es muy importante conocer la base de cada una de las implementaciones que se hacen en SEO para poder hacer ese tipo de implementaciones en un proyecto limpio entonces por ejemplo ahora veremos cómo se pueden poner las metatiquetas dentro del árabe y así vamos a entender un poquito más cómo funciona cuando se hace SEO desde el código si bien es cierto que con webmaster no hace falta tocar nunca la base de datos y con la web pues sí que necesitas tocar pues o más código directamente o si que necesitas hacer cambios de la base de datos lo mejor sería que el propio programador del proyecto se encargue de habilitar pues dichos campos para poder por ejemplo poner metatiquetas por ejemplo poner títulos poder meter jayson ahora qué pasa un programador vas a ver por ejemplo generar un sima automático vas a ver generar un jayson generar teléfono automática te vas a ver generar etiquetas qué pasa que él no sabe cómo las leo ve no tiene porque saber cuál es la forma correcta de implementar una etiqueta de una meta etiqueta no tiene porque saber cómo se hace un flan de una forma correcta no tiene que saber por qué saber cómo funciona un canonical de hecho es algo que se deja bastante de su campo entonces nuestro trabajo como SEO en estos proyectos es la supervisión de que todas estas implementaciones son de una forma correcta hablar con el propio programador para explicarle qué es lo que tendría que hacer y cómo se tendría que mostrar de cara al usuario y de cara a los motores de búsqueda así que voy a mostrar unos cuantos ejemplos bien pues realmente la meta era la versión exactamente igual que cualquier otro tipo de meta etiqueta pero hay que tener muy claras cuáles son para nosotros nuestras metatiquetas cómo funcionan las metatiquetas y cómo van de base, es decir hay que entender el código de lo que es una etiqueta title y lo que va dentro del title hay que entender lo que es una meta robots y por ejemplo cuando se le rellena el content con all, con none, con index, con follow, con no follow y hay que entender todo esto porque el programador va a saber hacer programación va a enfocarse esa programación en la funcionalidad de la web no va a saber enfocar el código de la programación en cuanto al SEO entonces un programador no sabe exactamente qué es un canonical no sabe qué tiene que llamar una canonical no tiene porque saber que las urles absolutas o las relativas son las que tienen que estar en la canonical en este caso lo digo para grabarlo la canonical tiene que estar la urle absoluta pero no tiene porque saber qué es un Twitter image un Twitter title entonces bien a lo que me refiere es que hay que saber colaborar con un programador y paso a paso para comprobar cómo funciona una página web aquí junto con David el programador de vixeo hemos hecho un larabel muy básico en este caso estamos juntando el vas con el front simplemente para que veáis cómo cómo altera y cómo cambian las cosas aquí por ejemplo vemos que este es el título que esta es la descripción y por ejemplo podemos hacernos un auto canonical que esto lo podríamos programar e implementar nosotros mismos aquí por ejemplo le ponemos un meta robots no que no tiene mucho sentido hacer un canonical hacia la propia página y un no pero simplemente lo veo para lo pongo para que veáis cómo funciona gracias a sumi y entonces bueno cuando veis el código fuente ya deberías que funciona que no funciona que el canonical funciona que el descripción funciona ahora que falla podemos ver aquí falla de programación estamos viendo que cuando estamos poniendo este campo cuando vamos aquí y le llenamos este campo de aquí o no lo rellenamos igualmente nos pone esto aquí esto es un fallo de programación porque porque en este caso el programador tendría que prever un condicional de que si no se rellena este campo esta meta etiqueta no existiría si no se rellena este campo esta meta etiqueta no existiría y así con todas es decir se tendría que hacer un condicional primero de comprobar si hay valor y si hay valor se pone esa meta etiqueta correspondiente entonces no está tarea en este tipo de entornos ya que no existen plugins para este tipo de entornos realmente lo que tenemos que saber es cómo funcionan todo el conjunto de meta etiquetas cómo funciona todo el conjunto json cómo funciona un signmap porque si bien un programador va a saber generar un sign map automático va a saber generar un json va a saber generar un json y decir que por ejemplo coja exactamente el mismo contenido que sea el h1 para poner por ejemplo un headline en un json de noticia va a saber hacerlo pero vamos a ser nosotros los que le vamos a tener que indicar qué información va a tener que poner en ese json qué información va a tener que poner en esa meta etiquetas y que meta etiquetas son las que no corresponden entonces junto con el programador es cuando tienes que sentarte y decir miramos a hacer una página que se puedan poner los títulos que se puedan poner la descripción es que se puedan poner el canonical y esto es el un entorno se parte desde cero tiene sus ventajas y sus desventajas su desventaja principal está clara es que conllevan mucho más trabajo al principio su ventaja principal es que va a ser un entorno totalmente limpio y totalmente personalizado y además totalmente adaptable a cuando haya actualizaciones va a ser muchísimo menos que el arredor de cabeza cuando por ejemplo ocurran paginaciones acerca de cómo se inventaron como generar esas paginaciones no vas a tener problema de que algo te genera un código y no se pasa exactamente de dónde viene porque se va a estar partiendo de una forma bastante limpia y bueno eso sería básicamente cómo funciona el entorno de las meta etiquetas y el entorno de los gisso en la arave porque recuerdo que el arave es un entorno de bacán es decir sí que se puede generar por medio del entorno del arave pero realmente donde está la implementación del arave es en el pack en el front en lo que hay que ver con el propio programador que es lo que se va a mostrar entonces cuando estamos utilizando un framework de bacán lo importante es ver con el programador qué cosas necesitamos nosotros poder implementar en qué necesitamos libertad y por ejemplo de cara al front en qué es lo que se debe mostrar yo por ejemplo suelo recomendar y suelo pedir que me dejen en cualquier caso primero que no sé que cuando ocurra esto cuando ocurra que no se rellena el contenido que la meta etiqueta no aparezca pero sólo pedir tanto en el footer como en el gea que me dejen un espacio para poder insertar código yo por si por ejemplo quiero meter una meta que aquí no se contempla como por ejemplo en el anabalibro raster o por ejemplo si quiero poner un jason particular que nos ha contemplado y que no está automatizado por ejemplo con productos o compre el crams lo ideal de esto es que se pueda automatizar todo y adaptar totalmente a la personalización del proyecto de cada uno sin necesidad de cargar jamás que bien necesarios y necesidad de cargar ciertos elementos innecesarios digamos que esto sería un entorno pues totalmente limpio y aquí estamos viendo en el código que simplemente están las metas etiquetas y en el body las etiquetas que hemos querido generar ya está entonces esto te permite una versatilidad en el sentido de que vas a poder personalizar y adaptar la página a SEO a tu conveniencia además va a poder permitir tener un mayor manejo del proyecto por ejemplo en entornos como hemos comentado antes de un marketplace donde son distintos negocios que van teniendo sus propios packs y un va general para organizarlo todo lo ideal no es juntar work pressure sobre work pressure sobre work pressure metiendo el 100.000 plugins porque eso va a tener una carga brutal y bestial hay man para hacer un montón de ricos a la base de datos lo cual para alentizar la página y ya no estoy hablando sólo de vpo ya estoy hablando de que puedes tener un sentido de que la página esté tumbada más tiempo del día que activa por lo cual en este caso puedes con la versión entorno muchísimo más limpio si muchísimo más robustos para este tipo de proyectos proyectos por ejemplo de una compañía aérea tal se acabará teniendo que trabajar pues con framework de este calibre entonces trabaja con framework de este calibre todo el sistema de metas etiquetas lo he tenido que implementar cada uno más o menos conforme se ha ido designando y esto sería en el caso de los frameworks del servidor