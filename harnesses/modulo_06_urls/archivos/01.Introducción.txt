Hola, muy buenas. Mi nombre es Christopher Crude y actualmente trabajo como SEO manager. Hoy os vengo a dar una clase sobre qué son las URL y cómo debemos de optimizarlas. Lo primero de todo lo vamos a localizar o lo vamos a dividir en 6 puntos, el primero de todo es que es una URL, el segundo serán los usos HTML que le podemos dar a esas URL, el tercer punto será cómo podemos hacer que las URL sean amigables, el cuarto serán buenas prácticas, SEO dentro de las URL, el quinto cómo optimizamos estas URL para las diferentes partes de la web y lo último de todo cómo auditamos estas URL, es decir cómo sabemos si están bien construidas, cómo sabemos si están bien definidas de cara al usuario, de cara a los buscadores. El primer punto de todo es que es una URL, la URL viene de las siglas Uniform Resources Locator, que no viene a ser otra cosa que una dirección única y específica que le asignamos a cada uno de los recursos que necesitamos de nuestra página web para que cargue esa parte dentro de internet. Normalmente son páginas web, pueden ser fotos, pueden ser vídeos, pueden ser documentos PDF, pueden ser incluso los propios documentos con los que trabajamos en Google Sheets, el correo, cualquier tipo de gestión o cualquier tipo de recurso que tenga una carga asícrónica o tenga una carga que venga definida por una descarga de recursos de internet. Lo más importante es que las URL nos ayudan a localizar mediante un navegador o buscadores las páginas que son atractivas para nosotros, las páginas que son atractivas para el usuario, las páginas que están mejor optimizadas de cara a los buscadores. Un ejemplo de URL muy sencillito es como ponemos aquí, ¿vale? Como puede ser la propia página de bixeo, como podéis ver se compone de https, 2.0, doble slash, bixeo.com, ¿vale? Lo primero de todo que tenemos que definir es ¿para qué nos sirve una URL? sobre todo y lo más importante es que nos sirve para localizar la información que buscamos en internet, es decir cualquier recurso, cualquier URL, cualquier página que tenga que esté indexada dentro de los buscadores mediante esa URL podemos encontrarlo, ¿vale? Esto lo que nos sirve es para identificar y organizar el contenido que queremos encontrar. Otro punto muy importante es que para poder acceder directamente a cualquier página o sitio podemos hacerlo siempre y cuando recordemos la URL, es decir en el ejemplo que hemos puesto anteriormente en bixeo, ¿vale? sabiendo que es bixeo.com podemos acceder directamente a la página de bixeo. Y luego otro punto súper importante o los dos puntos más importantes, ¿vale? es que nos ayuda a mejorar el posicionamiento siempre y cuando estén optimizadas a ojo de los buscadores, ¿vale? estas técnicas de optimización, cómo debemos de trabajarlas las veremos en los siguientes vídeos paulatinos que iremos estudiando ahora mismo, ¿vale? Y luego otro cosa muy importante a tener en cuenta es que nos sirven para ayudar a los buscadores a que entienda nuestro proyecto gracias a la arquitectura web, es decir lo que le estamos diciendo a Google o a los buscadores es toda la información, todas esas URLs, todas esas páginas que contiene nuestra propia página web o nuestro propio proyecto tienen que ser indexadas, se pueden mostrar, tenemos que también saber cuáles debemos demostrar cuáles no, pero bueno esto lo iremos viendo en vídeos sucesivos, ¿vale? Uno de los puntos que tenemos que saber son las diferentes partes de la anatomía de la URL, ¿vale? Uno de ellos es el protocolo. El protocolo es la primera parte que se puede ver en el navegador, ¿vale? la HTTPS dos puntos dobles las. Esto lo que nos está diciendo es el protocolo que no deja de ser el método que nosotros necesitamos, el método establecido para intercambiar y transferir datos alrededor de una red informática. En este caso tenemos dos tipos de protocolo, ¿vale? el HTTP y el HTTPS. ¿Cuál es la principal diferencia entre el HTTP y el HTTPS? Básicamente la seguridad. El protocolo HTTPS lo que nos está impidiendo es que otros usuarios puedan interceptar, puedan acceder a la información confidencial que se transfiere entre el cliente y el servidor web a través de internet, ¿vale? Aquí os pongo un ejemplo de cómo es una página con HTTPS, ¿vale? que tiene el SSL optimizado, como podéis ver tiene la S justo detrás y lo que nos está indicando en la barra del buscador es que la conexión es segura. Otra de las partes de la anatomía de la URL es el dominio. El dominio no es otra cosa como que nos indica el servidor web que se solicita. En este caso si estamos dentro de VIXEO pues estamos solicitando el dominio asociado a VIXEO.com, ¿vale? cualquier página, cualquier recurso que venga asociado a este dominio lo estamos pidiendo para que se muestren los navegadores. Normalmente suelen ser la nombre de la marca acompañado de .com, .es, .en, .fr. También el dominio, algo muy importante, puede contener W's dobles o no W's dobles, ¿vale? Aquí por ejemplo os pongo dos ejemplos de optimización o de URL's que pueden estar optimizadas, ¿vale? con el dominio que sería en este caso. Siempre y cuando no tienen las W's como puede ser HTTPS, doble punto, doble slash, VIXEO.com y luego un ejemplo de una página que tiene asociada las W's como puede ser HTTPS, doble punto, doble snack, triple W, Nike.com. Otra de las partes es la ruta. La ruta es una de las partes más importantes en las que debemos trabajar y las que debemos de optimizar. ¿Por qué? Porque nos sirve para diferenciar la parte específica del servidor web que queremos cargar. Es decir, ¿qué página en concreto queremos encontrar? ¿Qué página en concreto queremos que los navegadores nos encuentren? Nos muestren. En este caso una de las principales ventajas, una de las principales virtudes que tiene la ruta es que hace entender a los navegadores y a los buscadores el camino que ha de seguir para llegar a una URL concreta. En el caso que os pongo, de VIXEO.com, lo que estamos pidiendo es que cargue la página de servicio SEO. Algo muy importante es que normalmente esta ruta tiene que ir optimizada con la palabra clave principal. Un punto muy, muy, muy importante si queremos ayudar a nuestro proyecto a posicionar orgánicamente. Otra parte que puede que también podamos ver es el idioma. Normalmente después del nombre del dominio.com, en este caso VIXEO.com, podemos definir el idioma. Este idioma se puede indicar de varias maneras como veremos un poquito más adelante, que puede ser después del dominio, antes del dominio. Entonces esperemos las diferentes formas de cómo lo debemos indicar y cuál es la diferencia entre cada punto de idiomático. Aquí os pongo un ejemplo, seguindo con el ejemplo de antes de Nike, tenemos Nike.com y barra slash el idioma, que en este caso es español. Y otra de las partes que también es importante son los parámetros adicionales. Estos parámetros adicionales son listas de valores que normalmente vienen separadas por símbolo como puede ser la interrogación, como puede ser la higriega y en este caso se suele dar mucho en el caso de los e-commerce, con el caso de los filtros. También lo que intentamos llevar un poquito más allá es a su RLs, mediante estos filtros es que detallamos características propias de las RLs. Y normalmente estos parámetros lo que nos interesa es que nos indexen, que no se muestren dentro de los resultados de búsqueda de los navegadores. Aquí os pongo un ejemplo, como puede ser en el caso de Adidas, hacemos una búsqueda de zapatillas del número 37, en el caso de calzado de hombre y tenemos el ejemplo de parámetro, como podéis ver, tenemos el símbolo de interrogación. Bueno, en el próximo vídeo lo que veremos son los usos del HTML de las diferentes RLs.