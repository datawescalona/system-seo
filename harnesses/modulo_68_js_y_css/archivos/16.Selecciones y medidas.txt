Bien, entiendo que hay visto las diferencias y como pueden afectar o no afectar al deseo. Además, recordemos que si hacemos una llamada de aquí le podemos añadir pues el pilo de el prefech y todos esos complementos que se hablaron dentro de la clase de WPO. Bien, ya hemos visto cómo funciona los tipos de formas de llamar al CSS. Sin embargo, tenemos que ver algo también importante que son los electores y las medidas. Primero vamos a ver acerca de los electores. Hay distintas formas de llamar a distintos elementos de CSS como estábamos viendo aquí. Básicamente los estilos los estábamos llamando simplemente por el elemento y vamos a ver un ejemplo de por qué no siempre conviene llamar por el elemento. Imaginemos que queremos un dir en particular, uno de los discos que veremos aquí, pues digamos que le queremos poner mucho más margen entre los lados. Y le queremos poner por ejemplo un width de un 80% y un margin auto. Aquí lo estábamos poniendo en el elemento en particular y no ningún problema. Ahora, ¿qué ocurre si yo esto de aquí? No lo quiero poner como hemos comentado antes y lo quiero poner directamente en el elemento y lo quiero poner en la hoja de estilos. Tendría que llamar al dir y haría eso, pero entonces me cambiaría absolutamente todos los dips. ¿Cómo puedo llamar este elemento div en concreto? Bien, hay distintas formas de selectores, os voy a explicar las más básicas. Una es asignando un ID. A un ID se le puede llamar, esto sería el nombre del ID, ¿cómo se prefiera? Ya una clase que se hace de esta forma se le puede llamar también como se quiera. Ahora veremos qué diferenciar. Bien, si yo por ejemplo a un ID, además esto te lo hará de forma automática, también te digo que cuando estás en una en esta consola y hay una clase y un ID te prioriza el ID, pero bueno, decir esto simplemente a nivel de trabajar que si se le llama a la clase se le puede crear un estilo totalmente a la clase. Si yo por ejemplo a el ID de identificación, pues por ejemplo quiero hacer que todo este ID tenga más margen de arriba y abajo, se puede poner así por ejemplo, y tendría más margen, pero sólo tendría más margen de este elemento. Para diferenciarlo bien en los elementos le voy a poner un background simplemente para que vosotros los podáis distinguir, un poquito leve y para que no nos queden a un apretado le voy a poner un pad de 16 píxeles. Si yo ahora había estilos SS, y lo pongo aquí, esto me funcionaría sin ningún tipo de problema y sólo estaría recalcado para identificación. Vale, como se hace es con un hasta y poner el nombre del ID. No hace falta ni siquiera poner el nombre del elemento, si lo ponen hasta se identifica con el ID. Esto también para que lo supáis. Y qué diferencia hay con una clase? Vale yo a la clase, clase, clase, pues le puedo poner por también un background, se lo pongo de este color, le voy a poner un poquito más transparente para que no recalque tanto, y a poner un pad de... para que también se vea la diferencia. Vale, ¿qué diferencia una identificación de una clase? Porque dice, diréis, bueno, y los utilizo para lo mismo. No, no exactamente. ¿Por qué? Porque la identificación debería ser una identificación única, para único elemento en toda la página. Sin embargo, la clase sí que se le pueden llamar a más elementos con el mismo nombre de la clase. Si le llamáis con el mismo ID a otras clases, con el sírrico de que en cierto navegador no funcione y además es que es una mala práctica. Sin embargo, si le llamamos así a la clase, vemos que tanto esta clase como esta clase se llaman igual y funcionan exactamente igual. Vale, eso por un lado. Ahora, vamos a ver otro tipo de selectores que son los dos más importantes. Que lo que os recomiendo será que os metáis de hecho en uv3skool, que es una página bastante útil y buena para aprender, que aquí podréis ver pues cosas más avanzadas como por ejemplo el before, el after, focus, el hover, que si lo queréis, no tengo ningún problema. Esto no corresponde tanto a ello, pero lo puedo responder en un directo que hace exactamente a cómo funciona. ¿Cómo nos puede funcionar esto? Vale, imaginemos y vamos a poner alguna clase a todos los spam. No me va a complicar la vida con el nombre. Vale, entonces, pues tenemos varias opciones. Primero, esto también para que lo sepáis. Vale, vamos a llamar a todos los spam que tengan la clase spam. Menos por ejemplo, este de aquí no la va a tener y vamos a crear algún spam más para que lo diferenciáis. Vale, vamos a hacer que todos los spam estén en negrita simplemente para hacer una diferenciación clara, ¿vale? Vale, así distinguís todos los que son spam. Y de hecho, vamos a hacerle una tipografía más grande para que sea más fácil distinguir. Vale, y ahora todos los que están dentro de la etiqueta spam, básicamente son de color negrito y con este tamaño. Vale, vamos a diferenciar básicamente las clases. Vamos a poner que todos los que tengan la clase spam, que sería poniéndole el puntito delante, van a ser de color, vamos a poner un color llamativo que hace por ejemplo, un naranja fuerte. Bueno, no están llamativos, perdón, no soy muy bueno con los colores. Venga, un azul fuerte. Vale, lo guardamos y comprobamos que no todos los spam, porque este por ejemplo no tiene la clase spam, tienen ese color. Ahí podemos ver un tipo de diferenciación. Vale, ahora imaginemos que es que aquí el azul con el azul no nos termina de pegar tanto. Si hubiese ido no centrando tanto en el color, sino simplemente quiero que veáis esa diferencia. Entonces, ¿qué pasa? Si yo quiero que todos los spam independientemente de la clase que tengan sean oblicuos, todos los spam que hay dentro de esta clase van a ser oblicuos, pero los que hay en otros sitios no. Si yo quiero, todos los spam absolutamente todos y todos los enlaces que estos hace separándolo con una coma tengan un underline, todos los spam y todos los a van a tener ese underline. Y si le pongo por ejemplo los h2 también, esto ¿qué hace? Esto hace que evitemos hacer que el spam tenga esto, es decir, hace que evitemos esta práctica. Funcional es exactamente igual, porque funciona exactamente igual. Sin embargo, lo que estamos haciendo es añadir muchísimo más texto haciendo que pese más el css. De esta forma, con una simple línea y haciendo estas comas, pues nos pesará muchísimo menos el css y es una forma mucho más eficiente de hacerla. Es decir, recordemos que cuanto más texto, cuanto más caracteres tenga el archivo, más pesará y más lentas será la descarga Luego hay otra característica, que es el asterisco, que esto es para seleccionar absolutamente todos los elementos. Si todos los elementos son, por ejemplo, vamos a ponerle un ejemplo que no lo vamos a mantener, hacemos que todos los elementos como máximo tengan un pixel afectaría a todos los elementos en teoría. Por ejemplo, hacemos que todos los elementos tengan un display no, nos desaparecen absolutamente todos los elementos, aunque los elementos que están ahí visualmente no se muestran ninguno. Esto simplemente es un selector de absolutamente todo. Todos los elementos que intenten tener un div desaparecen. Pasaría eso también exactamente. Y bueno, todos los elementos y aparte de un div no tiene sentido porque realmente están funcionando todos. Y bueno, luego hay múltiples opciones. Ya los comentarios he comentado las más básicas, pero aquí tenéis bastante para elegir. A mí, por ejemplo, uno que solo elegí bastante es el de los child elements, que es el de first child o el last child, que es para elegir el último o el primer elemento de los que hay. O también se puede elegir, por ejemplo, un child en concreto, que es para seleccionar un elemento en particular. Se pueden seleccionar los pares, los impares, el elemento número 4 que esté dentro de tal elemento. Es decir, hay una multitud y una gran variedad de opciones. Bien, hasta ahí los selectores.