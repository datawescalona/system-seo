Hola, llegamos ya al último punto del curso de JavaScript del Master de SEO de Vixeo, en el que hablaremos de los principales eventos de lenguaje de programación JavaScript y veremos algunos ejemplos. Los eventos de JavaScript se utilizan para detectar acciones que hacen navegador o usuario. Y los eventos principales son estos que indico aquí, que iremos viendo uno a uno en diferentes ejemplos. El primer ejemplo de ellos es el evento onChange, que aquí vemos que está como atributo dentro de un elemento select de un HTML. Y lo que va a hacer es que cada vez que cambie el valor de este elemento va a ejecutar esta función. La función está aquí y lo que vemos es que nos va a añadir dentro del elemento demo, que es el parrafo este de aquí. Lo que hemos seleccionado es desplegable con el valor escogido en X, que X capturará el valor que se ha seleccionado. Si probamos el ejemplo lo ejecutamos de nuevo, vemos que debajo de esta frase no hay nada, y que en el momento que nosotros cambiamos la selección, pues aparece este texto con el valor de la selección que hemos escogido. Este sería el funcionamiento del elemento del evento onChange. Ahora vamos a probar el evento onClick, que es bastante simple y fácil de entender, que básicamente acciona la función o el código que hayamos puesto dentro de este valor, cuando precisamente hacemos clic en este elemento. Aquí el onClick está en un botón, pero podría estar en cualquier elemento, podría estar en el h1, podría estar en cualquier parrafo, no necesariamente tiene por qué ser únicamente un botón al que se le haga clic. Entonces en este caso al pulsar lo que está haciendo este ejemplo es ejecutar esta función, que añade este código, o añade estas palabras dentro del linea HTML de este elemento. Por otro lado tenemos estos dos eventos onMouseOver y onMouseOut, que básicamente vemos que este está ejecutando esta función y esta otra función, que están aquí las dos definidas. Y básicamente lo que hacen estas funciones es cambiar la altura y la anchura del disk, que es el elemento que contiene este código, por lo tanto en este caso es esta imagen de aquí. Entonces vemos que cuando pasamos el ratón por encima el tamaño cambia y cuando el ratón sale hacia afuera vuelve a su estado original. Y estas son las dos funciones, la función que se ejecuta cuando el ratón está por encima y cuando el ratón se sale. Por otro lado tenemos el evento onKtown, que en este campo de texto por ejemplo está aplicada y cada vez que pulsemos una tecla del teclado pues se ejecuta esta función, que al final es un alert, y por último el evento onLoad. ¿Qué lo que hace es ejecutar esta función inmediatamente después de que se haya realizado la carga del elemento? En este caso cuando Bodhi esté completamente cargado se ejecutará esta función, ¿vale? Y bien, con esto ya hemos visto los eventos principales del código Hascript y damos por finalizado el curso de Hascript de este master. Espero que os haya gustado.