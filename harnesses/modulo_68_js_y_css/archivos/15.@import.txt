Bien, como recordamos hemos visto el externo, el interno, el inline y la roba import. Vamos a comprobar como se hace una roba import. Vale, tenemos aquí todo esto. Entonces vamos a añadir otra hoja de estilos dentro de la propia carpeta de aquí. Me voy a poner import css, se le puede poner absolutamente el nombre que quiera. Vamos a hacer un ejemplo, vamos a hacer que el nav, esto también lo he comentado antes. Para mí esto no es importante que lo sepáis para el SEO, pero quiero que veáis lo que es un flex. Y que realmente, bueno, pues aquí le vamos a decir que me expando los enlaces de esta forma. Así, digamos que esta es la mejor forma de maquetar, normalmente con el flex y con el grid. Ahora veremos un poquito más a fondo después realmente cómo funciona el display, simplemente para que lo sepáis a modo de anécdota. Pero bueno, al final se consigue este resultado que funciona un poco casi a modo responsive ya. Porque siempre va a mantener esa proporción. Lo cual es un dato bastante importante. Vale, ponemos esto aquí, pero bueno, le he metido una hoja de estilos que no estoy llamando de ningún lado. Por lo cual no funciona. Cual sería otra forma de llamarlo? Primero, lo puedo llamar con una URL relativa como hemos hecho antes. Debería funcionar ya, porque con que yo esté en esta carpeta se debería llamar desde el index. Bueno, realmente es precisamente esto forma bastantes errores. Por lo cual, la mejor forma sería llamarlo desde una URL. Así vamos a seguir teniendo problemas. Entonces vamos a coger la URL absoluta. Vamos a comprobar que la URL esté correcta. Funciona de una forma correcta. Vale, la regla import antes sí que se podía llamar desde el footer. Sin embargo, se tiene que llamar desde la parte de arriba de una hoja de estilos. Está llamándola así. Bueno, ya hemos visto la forma de llamarlo. Realmente se puede llamar así y en teoría debería poder llamarse así sin la URL absoluta también. Se podría llamar también con la URL relativa. Porque bueno, estar aquí dentro de la carpeta se puede llamar así o la forma. Es una metodología que se utiliza bastante con las fuentes. Sin embargo, cabe decir una cosa. Ahora ya que estamos explicando las tres, tenemos el externo, el interno, el link y el import. Vamos a explicar cada una de ellas y cuál es la necesidad más para el SEO. La de import no la recomiendo nunca. Debido a que hacer una llamada a una hoja de estilos de CSS es más lento que hacer la de HTML. Y además de los horrores que puede ocasionar, realmente a nivel de performance es muchísimo peor y es muchísimo menos sostenible que llamarlo desde HTML. Por lo cual en la medida de lo posible, incluso con las tipografías con todo, evitarnos a roba import. Entonces nos queda la hoja de estilos, la hoja de estilos interna y inline. El inline no lo recomiendo primero porque al final acaba siendo una clase que funciona para un elemento solo en toda la web. Por lo cual si tú estás repitiendo esto y lo pones absolutamente en todos los sitios, por ejemplo, vamos a ponerlo también en todos los h2, al final lo que estás haciendo es aumentando considerablemente el peso del propio HTML. Por lo cual no conviene. Eso por un lado. Por otro lado, en este caso, tú cuando haces la carga de la página, ya no estoy cargando ningún complemento. Esto es útil y además esto para mí es importante que los complementos ahora oitar web no es una forma óptima, ya que bueno cuando se están haciendo este tipo de pruebas, por ejemplo, nos estaban saliendo archivos de JavaScript, que realmente yo no estaba cargando ni estaba utilizando, porque yo no he llamado ningún archivo de JavaScript. Cuando cargamos la web vemos que realmente está la página y las tres hojas de estilos que hemos llamado. ¿Qué pasa? Que aquí estas hojas de estilos, estas hojas de estilos y estas hojas de estilos son archivos, los cuales los puedo guardar en cache. Tanto con la roba import como con la llamada independientemente de que sea con una URL relativa o con una URL absoluta. Sin embargo, esto de aquí que es el CSS interno o esto de aquí nunca lo puedo dar en la página en cache. Por lo cual cada vez que yo cargue esta página voy a tener que descargarme todo este contenido y todo este contenido. Al mismo que parece poco. Si la mismo comproba esta página, es una página hecha con Pestasoc, lo que podemos comprobar es que tenemos un estilo interno aquí. Estos son scripts que están internos pero funciona básicamente el JavaScript con el CSS en este sentido funcionan similar. Si me estoy poniendo de forma interna, no lo estoy poniendo cargar en cache. Y aquí por ejemplo estoy teniendo otra larga cantidad de estilos que voy a tener que cargar cada vez que yo navegue por la web. Por lo cual siempre se carga me va a tener que cargar todos los estilos. Fijaos en toda la línea de código que estaba pasando con el JavaScript ocurre lo mismo. En este caso es JavaScript no es CSS, pero con CSS también lo tenía puesto así. Aquí vemos que nuestro JavaScript carga un montón de JavaScript, lo cual tampoco es realmente óptimo. Pero al final lo que vamos observando es que yo cada vez que quiero hacer una carga de la página me va a tener que leer todos esos archivos que conllevan un peso. Y ese peso lo voy a tener que descargar en mi ordenador cada vez que yo quiera navegar dentro de esa página web. La otra forma, si yo voy al enlace 2, al enlace 1, al enlace 3 y me quedo en la home y ya he descargado una vez este archivo o este archivo ya no lo tengo que descargar más. Ya tengo ese estilo implementado por eso no es lo mismo en una página. Además os voy a poner el mismo ejemplo ¿vale? Os acordáis de los errores que he comentado antes. No es lo mismo cuando yo cargo así una página y me hace esto que hemos comentado. Esto es por el orden del CSS que está mal implementado que cuando yo ya estoy navegando por aquí. Aun así me hace ese efecto porque leer lo demás antes que luego el documento. Pero ya no tiene que descargar el documento entero. De esta forma esto vaya muchísimo más traumático y muchísimo más lento. Si nosotros además viésemos el ejemplo cargándolo con muchísima menos conexión de internet, no es lo mismo cuando hacemos control f5, es decir, recargando el cache entero. Por lo cual todos los documentos que tengo descargados no funcionan. Vamos a ver con lentitud cómo va funcionando todo el proceso de carga. Veis que funciona bastante lento. También nos recomiendo hacer esto normalmente para comprobar cómo funciona vuestra página o va a estar cargando. Esto no es óptimo. Pero si lo des cuenta lo que ocurre es que hay muchos estilos que se den a posteriori. Veis que hay muchísimos tachados. Esto es porque primero se llama la indicación, luego otra, luego otra, luego otra, luego otra, luego otra, luego otra, luego otra. Y esto es lo que acaba generando un mal funcionamiento de toda esa carga. Ahora que ocurre que ya tengo descargado el archivo. Por lo cual, aunque va a tener que volver a leerlo todo, los archivos como este que ya están descargados ya no hay absolutamente ningún problema. Porque esto ya lo tengo descargado y va a tener que leer todo este contenido antes. Pero esto ya cuando lo llames a ver lo que es. Lo que pasa es que estos dinos dan las implementaciones como acepta la carga de JavaScript. Pero si ahora mismo le diría a F5 sin dar la control y no regalo el caché, aún con la misma velocidad veis que ya carga muchísimo más rápido. Si ahora mismo borrase el caché y le diría contra el F5, me iría muchísimo más lento.