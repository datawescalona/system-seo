Hola, iniciamos la sexta parte del curso de JavaScript del Master de Ser de Bixeo, en el que vamos a trabajar o vamos a hablar de los operadores del lenguaje de programación JavaScript, que nos permitirán realizar ciertas operaciones con variables o valores. Los operadores aritméticos son comunes a la mayoría de lenguajes de programación y nos permiten operar con valores o variables. Aquí vemos los operadores típicos de suma resta, multiplicación, exponente, división, el símbolo del porcentaje que a nivel de operador lo que hace es devolver el resto de una división y los operadores de incremento y decremento, que al final lo que hacen es sumar o restar uno. Eso es mismo poner a más más que poner a es igual a más uno. La utilización de estos operadores es bastante simple y al final es una utilización como una fórmula matemática, podemos añadir el más, podemos poner paréntesis, multiplicaciones, todas las operaciones que queramos en una misma línea, con variables o números. En este caso, en este ejemplo, declamamos una variable a que contiene el valor 3 y después guardamos en la variable x la suma de 100 más 50 multiplicado por a, que en este caso sería multiplicado por 3. Y el resultado de todas estas operaciones es almacenaría en la variable x. Por otro lado, tenemos operadores de asignación que permiten asignar valores a las variables. También a nivel de operadores son muy comunes y todos los lenguajes de programación los necesitan y los tienen. Entonces estos operadores que hacen pues, por el primer caso, es el igual, que estamos diciendo que x es igual a y. En el caso de que y contenga el valor 5, pues estamos diciendo que x, a partir del momento en el que se declara esta línea, también contiene este mismo valor. Los operadores de asignación permiten asignar valores a las variables. También son utilizados en la mayoría de lenguajes de programación, al final todo el tema de operadores es bastante común, que todos los lenguajes de programación los tengan prácticamente iguales, hay alguno que cambia, pero la gran mayoría de suma, resta, multiplicación y todas estas operaciones son similares. Entonces, a nivel de operadores de asignación tenemos el igual, que en el primer ejemplo, en el caso de que y tuviera un valor de 5, lo que hacemos es igualar x ahí, por lo tanto hacemos que x también sea 5. En los operadores más igual, menos igual y bueno, cualquier símbolo más el igual, lo que está haciendo es aplica este símbolo directamente con estas dos variables y lo almacena a la misma vez, con la misma orden, dentro de la variable x. En este ejemplo, que tenemos a la derecha de la pantalla donde x es 10 y es 5, tenemos el operador multiplicación igual, que lo que nos haría es almacenarnos en el número 50 dentro de la variable x. Por otro lado, tenemos los operadores de comparación que permiten detectar las diferencias entre variables o valores. Aquí están dos explicados, es un ejemplo sacado de la página web de Ovidores School, que es documentación oficial de este lenguaje. Entonces al final, ese operador de igual nos dice si en este caso nos compara si x es igual a 8 y nos dice que es falso. Bueno, son operadores que devuelven siempre un booleano, devuelven verdadero o falso, en base a si la igualdad se cumple o no. Son todos bastante lógicos, el igual, el diferente, mayor, menor que, etcétera, los que normalmente causan un poquito más de dudas, pero que son muy simples, es el triple igual, o el doble igual en el caso del ejemplo de no es igual. Este tercer igual lo que hace es, además, de comparar el valor, está comparando el tipo de dato. Si vemos aquí este ejemplo, lo que estamos programando es que tenemos una variable x de valor 5 y estamos introduciéndole dos comparaciones dentro de dos elementos de la CTML. Estamos introduciéndole si x con los tres iguales es igual a 5 entre comillas. ¿Qué pasa? Que como el valor de x no está entre comillas, estamos guardando este 5 como valor numérico. La primera comparativa nos da que es falso, ¿vale? Porque también comparar el tipo, entonces si este tipo numérico y este 5 entre comillas es tipo cadena de texto, el resultado es falso, ¿no es igual? En cambio, si solo lo comparamos con el doble igual, con este otro operador de comparación, el resultado que nos da es verdadero, porque considera que 5, aunque esté aquí entre comillas y aquí sea referencia numérica, lo que hace el doble igual es no tener en cuenta el tipo de variable, únicamente tiene en cuenta el valor. Esta es la diferencia entre el triple igual y el doble igual. Y por último, existen los operadores lógicos que permiten detectar comparaciones lógicas entre variables y valores. Estos operadores también son muy comunes, son sobre todo el i, el o y el operador de negación. Que lo que te hace es negarte el tu o false, que te haya dado otra comparativa, pues lo que hace es darse el contrario. Si una comparativa te ha dado tu y la niegas, el resultado final de esta validación es false. Existen más tipos de operadores que se pueden consultar en esta documentación oficial, pero bueno, en este curso hemos visto los principales, los más básicos y los que más se utilizan, y son los importantes de controlar en un nivel de JavaScript básico. Y con esto ya hemos visto todos los operadores y vamos a por el séptimo punto del curso de JavaScript, donde veremos y analizaremos las estructuras de control de este lenguaje.