Ahora vamos a ver la arquitectura técnica de Magento. Vamos a empezar por el tech stack. ¿Qué es lo que necesitas tener para poder desarrollar o para poder hacer tu proyecto en Magento? Pues tienes que tener un servidor web, ya sea con Apache o con Engings, con cualquiera de las dos funciona bien y yo he trabajado más con Apache quizás, pero mis desarrolladores en general, muchos de ellos prefieren Engings, bueno, es cuestión de gustos y de ver qué particulares tiene cada uno, pero funciona bien con los dos. Tenemos que tener PHP instalado en la versión, en las versiones 7. Es importante trabajar con Composer. Composer es un gestor de paquetes de PHP para instalación de paquetes de PHP y es lo que se utiliza por los equipos técnicos, lo que es más utilizado para la instalación de Magento como para la instalación de componentes dentro de Magento. Y luego teníamos PHP Unit para las pruebas unidades de código. Como base de datos, pues trabaja pues con Maya SQL, con MariaDB o con Percona, son todas bases Maya SQL y en principio con esto no hay ningún tipo de problemas. ¿Se puede conectar a otros tipos de datos? Sí, pero no es algo trivial, no es algo que de salida sea sencillo hacer. Es interesante instalar sistemas de cache, como pueden ser Redis o Varnish, la gente tiene su propia gestión de cache, pero bueno, es un presencia importante. Magento es especial a nivel de performance y entonces todo el tema de cache es un valor añadido que le puedes aportar a tu proyecto de Magento para mejor. Y ya más, ya veremos un poco más adelante que es importante para la estrategia de deseo. Y luego, desde la versión 2.4, el motor de búsqueda de Magento y eso es una cosa que ha complicado un poquito más esa instalación o ese uso de la plataforma para desarrollar, incluso para instalárselo tú mismo. El motor de búsqueda es elastic search, que es un motor de búsqueda por indexación de datos muy potente, que te permite hacer muchas cosas, pero que es ciertamente más complicado utilizar que el propio motor de búsqueda que tenía esta versión que era basado en Maya SQL. Y luego a nivel de gestión de colas, pues podemos tener un rabid message queue o bueno, o algo similar. Esto para la gestión de colas de procesos y de mensajería. Bueno, vamos con la arquitectura general de técnica de Magento. Magento está montado en arquitectura en capas. Como os he comentado antes, está basado en PHP, he hecho además sobre un framework, que está en framework ahora láminas. Y es una arquitectura en capas bastante clásica, donde tenemos la capa de presentación, que es la que maneja toda la parte de layouts, bloques, bloques de, digamos, de funcionales y templates, plantillas. Tenemos la capa de servicio, donde tenemos todas las capas de comunicaciones, tenemos las apps tanto res como SOAP, tenemos la capa de dominio, donde tendríamos toda la parte lógica de negocio. Es un poco el corazón de nostalgia de negocio, todo aquello que queramos hacer particular, lo tendríamos ahí o aquello que hace en cuanto a lógicas de negocio, el propio Magento también estaría aquí. Y lo tendríamos la capa de persistencia, que es, digamos, lo que se comunica directamente con la base de datos, que ejecuta todos los cruts de base de datos. Hay que decir que cruts son las siglas de create, read, update y delete. Las cuatro acciones que puedes hacer a nivel de datos, con la base de datos, es crear, es leer, es actualizar y es el borrado de datos. Y toda esa parte está en la capa de persistencia. En cuanto al frontend, esta es la estructura que tiene a nivel de archivos un proyecto en Magento. En frontend se trabaja con una cosa que se llama temas, ¿vale? Entonces los temas no es más que la plantilla, sería como la plantilla, a aplicar dentro de nuestro, la base de proyecto en Magento. De base, tú cuando instalas Magento puedes instalar un tema base o se instala un tema base, una plantilla base y luego además puedes instalar unos datos base, con lo cual tienes un Magento Funcional, y ese tema se llama Luma. Y entonces ese tema Luma o incluso un tema en blanco que se llama Blank Temp, que tiene Magento, se puede, lo puede transformar a partir de una cosa que se llama herencia, que permite se heredar. O sea, tú puedes coger un tema y heredarlo para crear otro. Luego el layout. El layout tiene una estructura definida, ¿vale? Esto sería una estructura de layout y luego tienes los bloques, ¿no? Los bloques son, son como porciones de código que te permiten mostrar algo, es como una plantilla de algo de un caso de uso muy concreto, ¿no? Como podría ser una lista de categorías, una lista de subcategorías, productos destacados, ese tipo de cosas las puedes meter en bloques de, en bloques e incrustarla dentro de la propia estructura de layout de la plantilla. Y las plantillas definen cómo los bloques de layout serán presentados. Exactamente, o sea, le puedes aplicar CSS por encima, puedes agruparlos, puedes definir el orden en el que sale cada uno de los, de los bloques y demás. Esto es la estructura de layout y bloques. Y aquí es lo que decía, ¿no? Todo lo que son bloques de código, pues tenemos cosas que tienen ya una funcionalidad concreta, ¿no? Esto por detrás tiene un código que accede a, a los servicios de Magento y se trae aquello que necesitamos para mostrar, en este caso, por ejemplo, la lista de subcategorías. Aquí habría, por ejemplo, el, digamos, el log in a newsletter, links de varios para el footer, bueno, esto es una estructura básica con un header, un sidebar left y una parte principal y un footer. Es una estructura bastante clásica y, bueno, pero hay, hay, hay bares que se pueden utilizar. Hay un concepto Magento que es súper importante, que es el de extensibilidad. ¿Por qué? Porque Magento se extiende. Magento nunca se toca el código base. Magento al código base le llamaremos siempre core, el core de Magento. Y el core de Magento nunca se toca. Porque quien se cuida de actualizar ese core es la propia doble, la propia Magento. Si tocamos ese core, nos exponemos a tener problemas de rendimiento, problemas de seguridad graves, y sobre todo problemas de seguridad graves por no poder actualizar Magento en la nueva versión que pueda salir que además solucione alguno de esos temas. Con lo cual lo que hacemos siempre que queremos o bien ampliar una funcionalidad o bien crear una funcionalidad nueva que no exista en nuestro proyecto es extender Magento. Como dice la frase, permite expandir de forma fácil el conjunto de funciones del propio producto. Se ha diseñado así para poder personalizarlo y mejorarlo ampliando fácilmente sus funciones, sus instalaciones, para que de las funcionalidades actuales ampliar funcionalidades e integrarse con software de terceros. Y dentro de esas extensiones que se llaman podemos extender temas, podemos extender módulos y hacer módulos de terceros. Los módulos de terceros son extensiones de Magento a nivel de funcionalidades nuevas o cambios de funcionalidades que ya existen. Podemos crear una lógica empresarial específica, podemos extender funcionalidades que tiene el propio core sin llegar a tocar el core, al tributo de entidad y idiomas muy importantes. Podemos extender Magento a nivel de creación de paquetes y idiomas, sobre todo para el tema de internacionalización. Los módulos de Magento es el corazón del desarrollo para la extensión. Lo que hacemos es extender Magento y creamos módulos o extensiones. Y esos módulos son aquellos que instalaremos dentro de nuestra plataforma, dentro de nuestro e-commerce, para poder tener una funcionalidad específica que no tenemos en el paquete Out of the Box. El diseño modular de los componentes de software es el principio arquitectónico central del producto. Los módulos autónomos de código discreto están organizados por función, lo cual reduce las dependencias externas de cada módulo. Permite modificarlo o reemplazarlo sin afectar al código. Es decir, son autónomos, son totalmente aislados unos de otros, o se intentan aislar unos de otros. Este acoplamiento flexible de componentes de software reduce el efecto dominó a lo largo de su base de código cambiante. Es decir, intentamos no tener herencia técnica porque nos permiten, en teoría, si se hace bien nos permitirá poder plugar y desplugar, poder tener y quitar módulos sin afectar al, en gran medida, el comportamiento de nuestro comercio o de nuestra tienda online. ¿Qué tipologías de módulos hay? Pues tenemos los módulos que interactúan con otras partes de la aplicación para conseguir una determinada funcionalidad. Puede contener tanto el interfaz de usuario de la parte pública como del Bacoffice. El Bacoffice es la interfaz de usuario de la parte privada, digamos, es la parte de gestión por detrás del e-commerce, es la área de administración. Y además puede interactuar con otros módulos o con otras partes de Magento. Los temas que permiten cambiar el look and feel de la aplicación, tanto del storefront, que es, digamos, la parte frontal, la parte más visual, como de la zona de administración. También se puede cambiarla en look and feel. Y luego tenemos los lenguajes, ¿no? Que son los paquetes de lenguaje para asistir a la internacionalización y localización del proyecto. Y nos permite traducir tanto los textos del storefront como de la zona de administración. Bueno, esta es la estructura de un módulo de Magento. Es como condición que el mínimo tiene que tener estos tres ficheros, el Reset Selection PHP, el módulo XML y el Composer JSON. Y a partir de ahí, pues, todo lo que necesitamos a nivel de código. Podemos aplicar extensiones al planeta de administración o Bacoffice, que es el Admin, ¿vale? El storefront, que es el frontal de la aplicación. Podemos aplicar módulos a la base, digamos, que es el código ausente, muy importante en sí, pero ausente del Admin y el storefront, pero que son temas que tienen internos Magento. Ahora creación de procesos programados, para ello podemos crear módulos. Y toda la parte de comunicaciones, digamos, con la parte de la pyrrex, apisobio, graf, quivel. Entre los módulos pueden existir relaciones y dependencias entre ellos. Los tipos de relaciones son un módulo Usa Otro Módulo, si este módulo es el módulo A invoca el comportamiento del módulo B. Podría ser también que un módulo reacciona a otro módulo. Es decir, el módulo B lanza un evento que es escuchado por el módulo A y hace algún tipo de acción. Un módulo customiza, se dice que customiza cuando el módulo A modifica el comportamiento del módulo B. O sea, puedo cambiar el comportamiento de un módulo. Un módulo que implementa al menos parte o todo el funcionamiento del módulo B. Es decir, yo tengo un módulo que hace una serie de acciones y puedo instalar otro que hace las mismas, pero a lo mejor las hace de forma más eficiente. Y reemplaza si el módulo A provee su propia versión de la implementación del B. Basicamente es quitar alula del otro módulo. En cuanto a dependencias, podemos tener dos tipos de dependencia. Una dependencia dura o una suave. La dura es que usa lógica y definiciones y estructura de la base datos dentro del otro módulo. Esto sucede sobre todo cuando son varios módulos ligados unos con otros porque todos interactúan sobre un concepto, sobre un área de negocio concreta. Por ejemplo, hay módulos que te extienden todo imagento a una versión de Marketplace que dependen de otros módulos más pequeños instalados dentro de Magento. Por ejemplo, un módulo de una compañía de terceros que, por ejemplo, que son Marketplace, puedes tener un módulo de Marketplace y luego un módulo de pago, pero que sólo funcione para pagos. Los pagos que en Marketplace tienen sus particularidades pues que sólo funcionen si tengo instalado mi módulo de Marketplace y no puedo instalar este módulo de pagos. Y la dependencia suave sería solamente dependencias a nivel de que hay un módulo que confirma la disponibilidad de otro. Hoy estás solamente si estás o no estás, pero no hay conflicto, no hay enlace directo entre uno y otro o simplemente lo extiende. Y esto sería un poco una foto de la base de datos. En realidad, yo creo que tiene más tablas, es una base de datos enorme y a nivel de base de datos Magento trabaja con un concepto que se llama indexación que es que indexa toda la información de esa base de datos para hacerla mucho más liviana, las consultas a los buscadores y demás y genera un montón de tablas sanidadas, un montón de tablas relacionadas. Intenta generar tablas muy grandes que te den la información necesaria para cada una de las partes funcionales de la aplicación. Y luego una breve nota de instalación, como os he comentado antes, Magento tiene tres formas de instalarlo. Tienes a través de una conexión con los repositorios, con Git, con el Git de Adobe. Aunque esto se usa más para aportaciones acódicas y con el Git de Adobe se utiliza más para aportaciones de código para poder descargar Magento, pero podrías hacerlo. Tenemos un download desde un FTP, te descargas el código, lo haces en la instalación, lo colocas en sus idiomas en la instalación. O tenemos lo que es más comúnmente utilizado, porque no solo se utiliza para esto, sino se utiliza para otras muchas cosas, que es el uso de Composer, lo que os explicaba que es la herramienta de gestión de paquetes de PHP. Esto sería un ejemplo de muy básico, hay más cosas que podéis ver en esa URL que hay ahí. Es una versión muy básica de cómo sería una instalación de Magento. Hay que decir que para poder descargar Magento tienes que tener un usuario en Adobe, en Magento, en el portal de Magento. Y a partir de ahí, de crearte ese usuario te crea dos claves, una pública y otra privada, que aquí en estas opciones se usan como login y password. Básicamente hay un momento en donde te pedirán login y password y te pones como login la clave pública como password la clave privada. Cualquier persona puede darse de alta en Magento y descargar el código o instalar el código en la versión open source, en la versión comers, solo las empresas validadas como partners de Magento pueden usar el código de la versión comers. Y básicamente es esto, es primero crear el proyecto en tu máquina, ya sea local o sea en el servidor, donde aquí además le dices el tipo de producto que quieres, una comers, una community edition y la versión que quieres. Luego tienes la instalación, en la instalación es donde le das, donde haces todo el setup de datos básicos de instalación, como el nombre del localhost o el usuario de la base de datos, tienes también el usuario de la zona de administración, tienes varias cosas que te permiten hacerlo. Y luego tienes cómo instalar los datos, los datos de sample data, los datos de ejemplo. Pues con estas dos sentencias de aquí, al nivel de consola, el sample data deploy y el setup upgrade. Cada vez que se hace algo, si tiene que limpiar la cache, es algo que con el tiempo te vas acostumbrando. Muy bien, pues hasta aquí la parte técnica.