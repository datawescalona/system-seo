Vamos a seguir con los tipos de SimeMap que pueden haber. Parte del SimeMap tradicional que sería un enlace con todas las huelhas listadas, tenemos todo este tipo de SimeMaps. Para empezar, tenemos el SimeMap index, que sería un índice que incluiría diferentes SimeMaps. Tendramos un SimeMap en la que enlazaremos únicamente imágenes, un SimeMap sobre vídeos que lo mismo enlazaría únicamente enlaces de los vídeos que queremos que wallrastré. También para proyectos que sean periódicos, noticias, si están dando datos de alta en Google News podemos tener un SimeMap también enfocado en Google News, podemos tener un SimeMap únicamente para la versión móvil en las webs que no tengamos una versión responsiva de nuestro proyecto. También estaría el HTML de SimeMap y por último tendramos los SimeMaps dinámicos que serían aquellos que se actualizarían solos cada un periodo definido de tiempo cuando creemos nuevas URLs o eliminamos URLs que tengamos en nuestra web. Vamos a ver unos cuantos ejemplos sobre diferentes tipos de SimeMap. Para empezar tenemos este caso aquí que sería el SimeMap index, el SimeMap index va enfocada a todas las webs que tengan más de 50.000 URLs porque es el límite de páginas que podemos incluir dentro del archivo SimeMap XML. También muchos casos nos sirven también para categorizar las diferentes URLs que tengamos en nuestra web si se trata de una webcown, una estructura muy grande. En el ejemplo de esta diapositiva podemos ver el SimeMap que tiene pezcomponentes. Dentro de su SimeMap index podemos observar varios enlaces a SimeMaps más concretos que es donde se encontrarán todos los enlaces. En esta que tenemos aquí sería el ejemplo que tiene el SimeMap de PhoneHouse que como podemos observar no está tan enfocada a un SimeMap definido según tipo de página sino que tienen un SimeMap en concreto por cada familia de producto. Como podemos ver más abajo tienen SimeMaps de productos de el aire, tienen SimeMaps de productos de afeitadoras, SimeMap para productos acuáticos y así para casi todas las categorías que tiene dentro de su web. Seguimos con el SimeMap para Google News como hemos mencionado anteriormente sería únicamente enfocado a los proyectos que estén dados de alta en Google News que trabajan noticias y que se actualiza casi directamente y aquí en el ejemplo podemos ver el SimeMap que tiene Sport. En esta diapositiva podemos ver el ejemplo del SimeMap enfocado a Google News que tiene la web Sport.es. Seguimos con otro tipo de SimeMap que sería el SimeMap HTML se trata de una práctica que no se utiliza tanto pero digamos es una versión más enfocada a los usuarios en el que también se trabajan en enlazo interno directamente con Dunk or Text y a través de palabras clave. Aquí tenemos un ejemplo también sobre cómo se vería un SimeMap HTML al final se trata de una URL más dentro de nuestra web con una estructura más user friendly con todas las URLs según la categorización que tengamos en nuestra página.