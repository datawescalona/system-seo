Seguimos con los atributos que podemos añadir a un SIMA que se tratan de 4 en total como podéis ver. La primera sería LOG que sería la localización en donde podremos añadir el HR de flank. También tenemos las MOD que sería la última modificación, ChangeFread que sería la frecuencia de cambio y por último el tag de la prioridad. Nos centraremos básicamente en las dos primeras porque son las que tienen mayor impacto y aquí podremos ver un par de ejemplos. Aquí en el primer ejemplo se trataría de un SIMA donde tenemos los cuatro atributos, tenemos el atributo de localización que sería la URL, tendramos el atributo de última modificación con la fecha, tenemos la frecuencia del cambio que en la primera URL le podemos ver que es anual y tenemos la prioridad que va sobre 0.0 a 1.0. Aquí en el segundo ejemplo podemos ver un caso de que añaten el tag de HR de flank dentro del SIMA. Aquí tenemos un segundo ejemplo que podemos ver el atributo HR de flank añadido dentro del SIMA que os preguntaréis qué es el atributo HR de flank, lo veréis más adelante en el módulo de SEO Internacional pero haciendo un rápido resumen se trataría de una etiqueta o marcado que le estamos dando a entender a Google las diferentes versiones de idioma que tiene nuestra página.