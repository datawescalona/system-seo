Vamos a entrar ya en el último punto de la clase que serían qué prácticas utilizamos años de SEO respecto a los signmaps. Tenemos como primer punto el incluir el enlace al signmap o a nuestro signmap pindex dentro del archivo robots.txt ya que al ser el primer archivo que rastrea los diferentes rastreadores o las arañas queremos que el enlace al signmap sea lo primero que lea. Como segundo punto también vamos a incluir el enlace de nuestro signmap dentro de la herramienta wall search console para que la herramienta tenga y nos pueda dar el estado que tiene nuestro archivo signmap. Como podéis ver aquí en el ejemplo dentro del search console tenemos la opción de signmap y nos deja la opción también de incluir la URL y nos muestra el status en el que se encuentra. Como segundo punto vamos a querer incluir siempre el enlace de nuestro signmap a la herramienta de wall search console. Como podéis ver en el pantallazo estaríamos dentro de una propiedad de wall search console de un proyecto. Abajo de cobertura nos encontraremos con el apartado de signmap en donde nos permite incluir el enlace a nuestro archivo signmap. Una vez incluido nos muestra el estado en el que está y en esta imagen podemos ver el estado de todas las URL que tengamos dentro del signmap. Una vez añadido el signmap dentro del search console podemos ver y auditar el estado de todas las URLs que tiene nuestro signmap. Como podéis ver en la pantalla nos muestra cuántas URLs dentro del signmap cuentan con error, cuántas son válidas pero hay avisos de potenciales errores. Vamos a ver cuántas URLs son válidas y son correctas y también el número de URLs que están excluidas. ¿Qué tenemos que hacer con este informe? Pues entrar cada una o dos semanas a los proyectos, revisar que todo esté en el que todas las URLs importantes de nuestro proyecto estén incluidas y que no estén dando ningún error y si tenemos URLs que creemos relevantes y están dentro del signmap y están siendo excluidas como este caso aquí que hay 49, ver el motivo y realizar acciones para corregir este punto. La siguiente práctica es la que mencionamos en la parte de atributos que es relevante dentro nuestro archivo signmap siempre añadir el atributo lock y el atributo de última modificación que son las dos que cuentan con mayor impacto. Lo siguiente de los atributos es lo que mencionábamos en el punto anterior que sería toda la parte de las especificaciones intentar respetar todos los puntos que se han mencionado y que no estemos incluyendo URLs no relevantes, URLs que no se puedan rastear o URLs que tengan código de respuesta que no sea 200 y ya la última recomendación es que trabajemos bien la estructura de nuestro signmap index en el caso que necesitemos cuando tengamos un proyecto de más de 50.000 URLs o que cuentan con muchas verticales y queremos categorizar mejor en nuestra página web. Y esto ha sido todo para la clase de signmaps. Si os ha quedado alguna duda o creéis que necesitáis información más detallada sobre algún punto en específico acudir alguno de nuestros tutores que os responderemos todas vuestras dudas. Nos vemos en la siguiente elección.