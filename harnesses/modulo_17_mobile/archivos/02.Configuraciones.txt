Bienvenidos al apartado de configuraciones dentro del módulo de Mobile. En este apartado veremos muy en particular y muy en concreto cómo son los distintos tipos de configuraciones que realmente se pueden utilizar en una web para adaptarla a la visión y lidar a los dispositivos móviles. Como hemos comentado previamente esto tiene gran importancia y muchísimo impacto en el SEO. Entonces vamos a ver la forma más correcta de configurar todo este tipo de opciones y entonces veremos qué formas son correctas, cuáles no, qué formas hay a hacerlo y la variedad de opciones que tenemos. Así que bueno, vamos allá. En primer lugar os quiero hablar de la Meta Viewport. La Meta Viewport es una meta etiqueta como hemos visto en una clase anterior ya sabemos lo que son las meta etiquetas, iremos viendo muchísimo más a lo largo de todo el máster y además está en concreto la veremos muy en profundidad en el apartado de programación para SEO. En este caso la vamos a ver de forma básica simplemente para que veamos el efecto que tiene y cómo hacer una configuración básica al respecto. Esta meta con el actitud de NameViewport aquí he puesto un content que además es el content que indicaba un reportefecto que realmente hace una especie de escala 1 a 1. Ya lo veremos muy bien particular, además aquí os dejo una URL para que entendáis mejor la utilización que tiene y realmente unos aspectos básicos. En este caso estamos viendo un ejemplo que es una imagen que cogíto previamente y en este ejemplo bueno simplemente veríamos cómo podría ser una web que no tiene configurado el MetaViewport. El MetaViewport recoge lo que es el ancho de la pantalla dependiendo del tamaño del dispositivo y en este caso si no se incluye esta etiqueta para los dispositivos móviles lo que puede entender es que por ejemplo tenga una anchura de unos 980 píxeles y así que se vea como se ve el dispositivo, se ve como si estuviéramos en esa anchura y que realmente estamos en un zoom para afuera por lo cual el usuario incluyendo por el tamaño de letra por un motivo o otro tenga que pellizcar hacer zoom, hacer scroll hacia la derecha o hacer hacia la izquierda para poder leer todo el contenido en general y entonces esto sería un error fatal porque a nivel de usuario el usuario enseguida se sale este tipo de páginas porque son realmente difíciles de leer desde el dispositivo móvil. En este caso el ejemplo de la derecha vemos que la Meta Viewport está especificada y encaja con la anchura del dispositivo. Si son imágenes adaptables ya veremos los distintos tipos de imágenes y tal que se pueden poner o que se pueden cambiar en cuanto al dispositivo móvil y vemos que además aquí una configuración distinta esto no es sólo por lo que sería la etiqueta MetaViewport que ahora veremos más adelante. En general simplemente obtiene una regla bastante básica si digamos que vuestra web en mobile funciona para cualquier tipo de navegador digamos que reciente actual nuevo como google chrome o el apel mobile safari debería funcionar para los algoritmos de google. En este caso metiéndome en el enlace que os he recomendado aquí sí que es cierto que bien esto es simplemente para avanzar contenido no hace falta que sepáis inglés y no queréis verlo es simplemente un añadido aquí podéis ver cómo funcionaría la etiqueta MetaViewport si es cierto que aquí tiene también cierto contenido de css que hace este tipo de cambio pero realmente el viewport sí que hace un cambio bastante notable como podéis ver por ejemplo apel también tiene su propia etiqueta MetaViewport dentro de esto y veis que tiene además una configuración distinta estas configuraciones ya la veremos más adelante y hablaremos bastante en ellos como os he explicado antes de todas formas la configuración que hay aquí por ejemplo es bastante básica es la estándar y realmente como he dicho si realmente se ve bien en los tipos positivos móvil en los navegadores nuevos no debería haber ningún tipo de problema en cuanto a los algoritmos de google sin embargo me parece importante regalcar la importancia de la etiqueta metaViewport en cuanto a la optimización mobile