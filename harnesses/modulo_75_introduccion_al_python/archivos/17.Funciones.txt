Bien, ahora veamos qué son las funciones de Python. Vamos a decir rápidamente que una función es una secuencia de sentencias que tiene un nombre y que ejecuta una operación desciada. Lo que vamos a ver a continuación serán sus elementos y sintaxis, que son los argumentos y finalmente, cómo podemos importar módulos. Muy bien, llegó el momento de adentrarnos al tema que desde mi punto de vista es el más importante del curso. En esa sección vamos a hacer uso de todo lo que hasta el momento hemos estudiado. Pues vamos allá. Una función es una secuencia de sentencias que tiene un nombre en específico y que ejecuta una operación desciada. Esta sentencia se especifica en una definición de función. Este concepto lo veremos más adelante. Una función es una herramienta útil para reutilizar código en nuestro programa. Bien, vamos a decir que en nuestro camino de programación en Python nos vamos a encontrar situaciones diversas donde tengamos que escribir un mismo código una y otra vez. Una solución es copiar y pegar las sentencias pero se pierde la eficiencia en la construcción de nuestro código. Hago referencia al tiempo de programación y sobre todo a lo largo que puede llegar a ser nuestras líneas. Esto puede llevar a cometer errores al momento de tener la necesidad de modificar el mismo. Hagamos un ejemplo donde utilizaremos conceptos que a lo mejor aún no sabes pero medida que avancemos con este tema tendrás el detalle del mismo. Bien, en primer lugar supongamos que queremos buscar un número para luego multiplicarlo por un factor en concreto. Bien, entonces para este ejemplo vamos a buscar un número en una lista y luego a multiplicar multiplicarlo por un factor. Vale, lo escribo para que quede claro. Bien, en esta misma lista voy a crear una lista que contenga los números enteros del 1 al 10. Para ello voy a crear la variable lista de números. Bien, entonces vamos por la lista 1, 2, 3, 4, 5, 6, 7, 8, 9 y 10. Bien, en esta misma celda voy a crear la variable número buscado y le voy a asignar por ejemplo el número entero 6. Bien, también voy a crear la variable factor y le voy a asignar el número 30. También voy a crear la variable r y le voy a asignar la multiplicación de dos variables. Bien, del número buscado por el factor. Bien, mi variable r va a ser quien guarde el valor resultante de la multiplicación del número buscado. Bien, por el factor. Vale, ejecutamos para guardar los valores dentro de las variables y ahora vamos a tratar de simular este problema haciendo uso de un if else. Veamos cómo hacerlo. Hago un if y decíamos que queremos buscar un número en una lista. Bien, esto lo podemos hacer de la siguiente manera. Si el número buscado está dentro de una lista y para ello vamos a usar el operador in, lista de números. Bien, hago dos puntos y le voy a decir a Python que ejecute lo siguiente. En caso esta condición sea verdadera, imprime el valor resultante de la variable r. Caso contrario, es decir, hago un else dos puntos, imprime el string el número buscado no se encuentra en la lista. Bien, entonces lo que estamos haciendo en la primera línea es una condición de búsqueda, es decir el número buscado que contiene el número 6 lo va a buscar, va a recorrer cada índice hasta encontrar el número que está asignado a la variable número buscado. Si es positivo, si es positivo va a imprimir el valor resultante de la variable r. Caso contrario, nos va a decir que el número buscado no se encuentra en la lista. Esto básicamente. Bien, entonces vamos a ejecutar para ver qué obtenemos. Ejecutamos y obtenemos en este caso el valor de 180. ¿Por qué? Porque el número buscado 6 sí está en la lista y como está en la lista, entonces multiplica el valor de 6 por 30. Bien, y 6 por 30 efectivamente es 180, justamente lo que hemos obtenido como resultado en esta celda. Supongamos que queremos guardar el código junto a las listas pero lo que queremos modificar son ciertas variables. Entonces lo que tenemos o lo que podemos hacer es copiar y pegar el mismo código. Bien, entonces vamos a hacer lo siguiente, vamos a copiar las variables y también vamos a copiar esta línea. Para este segundo ejemplo lo que vamos a modificar va a ser un número buscado. Por ejemplo, ya no quiero que sea 6 ahora quiero que sea 11. Bien, es la única variable que voy a modificar para este ejemplo. Podría ejecutar para guardar los valores de eso, de las variables y vamos a ejecutar estas líneas para ver qué obtenemos. Tratar que es que contienen las mismas indicaciones, el mismo if, el mismo que está cambiando, que es una variable. Vamos a ejecutar y obtenemos en este caso que el número buscado no se encuentra en la lista. ¿Por qué? Porque el número buscado o sea efectivamente está fuera del limite de la lista. Ahora supongamos que quiero otra vez modificar otras variables y también quiero llevarlo digamos a otro programa o a otro bóculo. A ver, lo que tenemos que hacer es de igual forma copiar y pegar las variables, copiar y pegar el trozo de código. Bien, y en este caso lo que quiero hacer es modificar el número buscado nuevamente, le quiero asignar el número 5 y el factor también lo quiero modificar. Por ejemplo, también el número 5. Bien, entonces vamos a ejecutar bien para guardar los valores nuevos en las variables creadas y también vamos a ejecutar ese trozo de código para ver ahora qué obtenemos. Es que estamos obteniendo el valor de 25. ¿Por qué? Porque el número buscado se encuentra en la lista de números. Además, como no encontró bien, imprime el valor de la variable r y el valor de la variable r y la multiplicación del número buscado por el factor 5x5 efectivamente es 25. Como te das cuenta puedo continuar compiendo y pegando las variables junto al trozo de código una y otra vez hasta lo que yo quiera y simplemente puedo modificar los valores que contiene variables. Pero ahora vamos a tratar de definir este trozo de código de una opción. Bien, para ver cuál sería la diferencia. Tomar en cuenta que estamos haciendo un ejemplo muy corto, muy pequeño, bien, con solamente 4 optimizaciones, vamos a ver, vamos a hacer la opción y ahora vamos a ver cómo responde. Vamos a definir una opción, estamos de muy diferente a 2x5, vamos a abrir paredes y le voy a pasar dos parámetros. En este caso el número y el factor. Si es que no desuena esto de DF o esto de parámetros, lo que estoy hablando es que ahora a compilación conforme pase el tema, una, dos o tres detalles de que no digo que no es un método que puede funcionar, pero no es como nosotros funcionamos, pero en esta función voy a crear una lista, vamos a copiar con esta variable dentro de la lista de números. Me tengo una lista de números dentro de la función. Voy a hacer un siguiente, le voy a decir a Payton, también voy a usar un elctetron, una función, hago un 10, número buscado, vale, con número, simplemente está en lista de números, hago 2 puntos y quiero que se pute esto en el carabero, lo siguiente, voy a crear una variable r, voy a igualar la multiplicación del número por el factor, voy a hacer que me retorne el pringles del resultado, es decir, el valor que va a contener la multiplicación de número por el factor, caso contrario, por eso hago un lc, 2 puntos, quiero que imprima el número buscado no se encuentra en la lista, entonces, a ver, vamos a ejecutar para crear la función, ejecutamos y ya tenemos creada nuestra función multiplicado, a ver, vamos a llamar a la función de esta forma por su nombre, decíamos que tenían un nombre en específico, multiplicador, hablo paréntesis y le voy a pasar los argumentos que sean necesarias y le voy a pasar los argumentos que son necesarios, entonces, le voy a pasar un número y un factor, a ver, vamos a pasarle el número y factor del primer ejemplo, el número era 6 y el factor 30, entonces colocamos 6 como 30, en esta misma celda, voy a llamar nuevamente a la función y le voy a cambiarlos a argumentos y le voy a poner el número buscado y el factor del segundo, el número es 11 y 30, vale, entonces, vamos a poner 11 y 30, en esta misma celda voy a hacer lo mismo, pero voy a poner en esta ocasión el número y factor del tercer ejemplo, que era 5 y 5, vamos a ejecutar para ver qué conseguimos, para la primera línea que contiene el factor 30 y el número 6, obtenemos el valor de 180 y si se encuentra, ese mismo valor lo obteremos porque hemos obtenido en el primer ejemplo, en el mismo, efectivamente, para la segunda línea hemos obtenido también el mismo valor del número buscado que nos encuentra en la lista, como en el segundo ejemplo y en la tercera línea también hemos obtenido el valor de 25, tal cual hemos obtenido en el tercer ejemplo, esto hace que las funciones nos airem hacer más eficiente en la construcción de un programa, bien, podemos repetir, podemos reutilizar código a lo largo y a lo largo de nuestro programa, muy bien, vamos a continuar, voy a borrar estas celdas y vamos a continuar con los elementos y las sintaxis de una función, a ver, vamos a añadir más celdas, como hemos visto, una función es un fragmento de código que recibe parámetros, ejecuta acciones y retorna un resultado, una función repeta la siguiente sintaxis, siempre vamos a empezar para definir una función, la palabra DEF, seguidamente vamos a colocar el nombre de la función y luego dentro de un paréntesis los parámetros que le vamos a pasar a la función al momento de llamarla, seguidamente dos puntos y luego el cuerpo que básicamente con las instrucciones o acciones que quiero que ejecute, finalmente voy a tener que usar una palabra reservada que es RETUR, que contiene o que representa mejor dicho el valor de la función, bien, entonces vamos a hacer un resumen, en una definición de función la palabra reservada en el encabezado es DEF, seguido de ello tenemos el nombre de la función y una lista de parámetros entre paréntesis, la lista de parámetros puede estar vacías o contenerfuelo o más parámetros, ahora veamos a detalle los elementos y sintaxis de una función, bien, en principio habíamos visto, habíamos creado mejor dicho una función que se llama multiplicador y habíamos trabajado en él usando definiciones, usando algunos conceptos que de repente como dije en ese momento que no sabías, ahora lo vamos a ver a detalle a la función, bien, como hemos visto una función es el fragmento de código que recibe parámetros ejecuta acciones que retornan un resultado, una función debe de adentrar la siguiente intacta, vamos a empezar siempre con la palabra DEF, leído de nombre de la función, nombre de función, luego le vamos a pasar parámetros, vamos a hacer dos puntos, viene el cuerpo de la función y finalmente el valor de rato, como vemos una definición de función, la palabra reservada en el encabezado, de seguido de ello tenemos el nombre de la función y una lista de parámetros que están entre paréntesis, la lista de parámetros puede estar vacía o contenerfuelo o más parámetros, en cualquiera de los casos el uso del paréntesis es obligatorio, siempre, luego, pero dos puntos, viene el cuerpo, que hace referencia a uno o más sentencia o instrucción al paréntesis, cada una de ellas con la misma sangría y finalmente tenemos la palabra reservada retor que representa el valor del retorno de la función, ahora veamos un poco más cada uno de esos elementos para tenerlos claros, supongamos que quiero crear una función que transforme una palabra de minúsculas a mayúsculas, vamos a empezar con la palabra reservada DEF, le voy a poner un nombre de cualquiera, por ejemplo mayúsculas, voy a abrir el paréntesis y voy a crear el parámetro para, voy a hacer dos puntos y dentro del cuerpo voy a crear una variable r, donde el parámetro palabra le efectúe una función upper para transformar la palabra de minúsculas a mayúsculas, luego en la siguiente línea, lo que voy a hacer es crear una frase, un string, de la siguiente manera la palabra voy a hacer una más para concatenar strings, voy a crear un opacio, hago un más, coloco el r que es la variable que guarda el valor resultante de la palabra en minúsculas en mayúsculas, hago más y luego coloco esta en mayúsculas, vale perfecto y finalmente lo que voy a hacer es llamar a la palabra reservada a RETUR y colocarle r, bien entonces lo que voy a obtener como valor de salida de esta función es el string final que va a contener mi variable r, bien vamos a ejecutar para crear esta función y una vez tenemos esta función creada, como la ejecutamos, a esto se le conoce como llamada de una función, a ver vamos a cortar estas celdas, bien estamos en este tema y vamos a abrir celdas, perfecto, entonces para llamar a una función se debe usar el nombre de la función seguido del paréntesis, entonces por ejemplo, por ejemplo si quiero llamar a la función mayúsculas tengo que abrir el paréntesis y tengo que colocar seguidamente dentro, dentro del paréntesis un parámetro, vale por ejemplo, voy a pasarle en este caso el parámetro hola, el parámetro hola es un string, a ver vamos a ejecutar y obtenemos efectivamente la frase la palabra hola está en mayúsculas, bien el parámetro que le pasamos es un string en este caso es hola, luego la función lo que hizo es pasar esta palabra que es hola, bien le ejecutó una función upper que lo transforma en mayúscula y finalmente guardó este string o frase dentro dentro de la variable r, por lo que el valor resultante debería ser una frase como esta, por eso es lo que hemos obtenido como resultado la palabra hola está en mayúsculas, notar que la función se ejecuta solo cuando se le llama, vale, de otra forma no se puede ejecutar, ahora pasemos al tema de los argumentos, bien vamos a borrar otra vez estas celdas que no hacen falta y vamos a agregar celdas por aquí, vale, muy bien, los argumentos se especifican entre paréntesis después del nombre de la función, los argumentos van separados por comas, ¿qué pasa si a esta función que definimos anteriormente no le pasamos ningún argumento, vale, a ver vamos a intentar llamar a mayúsculas, vale, vamos a abrir paréntesis pero no le vamos a pasar ningún argumento, vale, vamos a ver si al ejecutar Python nos dice o nos arroja algún error ejecutamos y efectivamente nos visualiza un error que dice que hace falta un argumento y este argumento es el argumento palabra, este error de acuerdo a nuestra función dice que básicamente nuestra función necesita un argumento y tenemos que señernos a lo que ha definido en la función creada, una función debe ser llamada con el número correcto de argumentos ni más ni menos, esto borrar, esto borrar, voy a hacer nuevamente esto de mayúsculas, borrar, los argumentos se especifican entre paréntesis después del nombre de la función, los argumentos van separados por comas y ¿qué pasa si a esta función que definimos no le pasamos ningún argumento, vale, vamos a llamar a la función mayúsculas, vamos a abrir paréntesis y no le voy a pasar ningún argumento, veamos Python que nos da como resultado al momento de ejecutar esta celda, ejecutamos y tenemos un error porque de acuerdo a nuestra función este necesita un argumento, bien nosotros tenemos que señernos a lo que se ha definido en la función creada, una función debe ser llamada con el número correcto de argumentos ni más ni menos, vamos a continuar voy a borrar estas celdas, vale, como habrás escuchado estuve diciendo parámetros argumentos, haciendo referencia a los elementos que están dentro del paréntesis después del nombre de la función en una definición de función, bien, recordar que una definición de función es esto, básicamente es empezar desde la palabra reservada def hasta la palabra reservada retur, entonces desde la perspectiva de una función un parámetro es la variable que va entre paréntesis en la definición de la función y un argumento es el valor que le pasamos a la función cuando se le llama, básicamente estos dos conceptos los podemos usar para lo mismo, básicamente es la información que se le pasa a una función finalmente, aunque si queremos ponernos estrictos tendríamos que respetarlo dicho, veamos el siguiente punto, valor de parámetro predeterminado, vale, podemos añadir a una función un parámetro predeterminado, lo que significa que si llamamos a una función sin argumento, utilizará el valor predifinido, veamos un ejemplo utilizando la función anterior, vale, por ejemplo, a ver, vamos a copiar y pegar esta función en esta celda y lo que vamos a hacer es lo siguiente, en el apartado de los parámetros voy a utilizar este parámetro palabra y le voy a igualar, le voy a igualar el string hola, vale, a ver, vamos a ver, ejecutamos para crearlo nuevamente, notar que la nueva función mayúsculas contiene, contiene un nuevo parámetro predeterminado, vale, y se está manteniendo las líneas de código, bien, hago referencia al cuerpo y hago referencia al valor de retor, simplemente lo que he hecho es modificar o colocar como parámetro predeterminado la palabra hola, vale, entonces vamos a llamar a la función mayúsculas, voy a abrir paréntesis y no le voy a pasar ningún argumento, ningún argumento, vale, vamos a ver si esta vez obtenemos el mismo error que obtuvimos anteriormente, ejecutamos y en esta ocasión no obtenemos ningún error porque ya teníamos un parámetro predefinido, bien, lo que quiere decir es que si no le pasamos como dije hace un momento, si no le pasamos ningún argumento va a utilizar como valor del argumento el parámetro predeterminado que definimos en la función, vale, por ejemplo si yo coloco mayúsculas, abro paréntesis y dentro del coloco por ejemplo tabla, ¿qué pasaría si ejecutamos? ejecutamos y obtenemos la palabra tabla está en mayúscula, quiere decir que si yo le paso un nuevo argumento, vale, un string en específico, bien, va a tomar a éste como parámetro, vale, y va a obviar el parámetro predeterminado, vale, a ver seguimos con otro ejemplo mayúsculas, mayúsculas y voy a colocar nuevo por ejemplo, veamos que obtenemos, ejecutamos y obtenemos la palabra nuevo está en mayúscula, nuevamente obvio el parámetro predeterminado, bien, y utilizó como argumento el string nuevo, a ver, a ver nuevamente quiero utilizar a mayúsculas mayúsculas y no le voy a pasar absolutamente nada, otra vez como en esta celda, vamos a ver si nuevamente obtenemos lo mismo, a ver, aquí, aquí el error es el nombre de la función, a ver, vamos a escribirlo de una forma correcta porque tiene un nombre en específico y tenemos que escribirlo bien, ejecutamos y efectivamente obtenemos lo mismo que hemos obtenido en esta celda, la función voy a repetirlo, vale, para que quede claro, esa función si no le pasamos ningún argumento utilizará como argumento el parámetro que hemos predefinido en nuestra función, vale, notar que si no le pasamos ningún parámetro, tomará como argumento predeterminado la palabra hola, vale, continuamos, vamos a agregar más celdas aquí, muy bien, ahora en este punto vamos a preguntarnos si podemos pasar una lista o un diccionario como argumento, en realidad se le puede pasar cualquier tipo de dato como argumento a una función, nosotros podemos definir una función con parámetros de cualquier tipo, siempre cuando respete lo que definimos en el cuerpo, quiero decir el argumento que nosotros le pasamos, la función lo va a tratar como tal con el mismo tipo de dato en que le pasamos como argumento, hagamos un ejemplo, supongamos que queremos imprimir todos los elementos de una lista, para ello voy a hacer lo siguiente, creo la función def, voy a llamar a la función imprime palabras, voy a abrir el paréntesis, voy a pasarle el parámetro jugadores que va a referencia a una lista, voy a hacer dos puntos y como dije quiero imprimir cada elemento de una lista y lo voy a hacer de la siguiente forma, voy a hacer un for y in jugadores, hago dos puntos y le voy a decir que imprima cada elemento y ejecutamos para guardar o para crear esta función imprime palabras y en esta celda lo que voy a hacer es crear una lista, le voy a llamar por ejemplo real Madrid, vamos a crear la lista, el primer string quiero que sea venicios, el segundo string quiero que sea benzema y el tercer string un crack rodrigo, ejecutamos para guardar la lista dentro de la variable real Madrid y ahora lo que vamos a hacer es llamar a la función imprime palabras y como lo hacemos simplemente colocamos el nombre de la función imprime palabras, voy a abrir el paréntesis y voy a colocar el nombre del nombre de la lista que quiero pasarle como argumento, entonces en este caso como ya cree real Madrid voy a pasarle justamente esta lista, veamos qué pasa, voy a ejecutar y efectivamente obtenemos cada elemento de la lista impresa en esta celda, tenemos a venicios, tenemos a benzema y tenemos a rodrigo, hagamos otro ejemplo pero en esta ocasión quiero crear un diccionario, voy a crear un diccionario Barcelona por ejemplo, el índice 30, el nombre del jugador Gaby por ejemplo índice 3, valor piquet, índice 8 y valor al vez, notar que esto es un diccionario, no es una lista así que vamos a ejecutar para guardar el diccionario dentro de la variable Barcelona y vamos a tratar de usar la misma función pero esta vez vamos a pasarle nuestro diccionario Barcelona, vamos a llamar o nuevamente simplemente colocando el nombre de la función creada, imprime palabras, vamos a abrir paréntesis y en él vamos a colocar el argumento que le queremos pasar, esta vez habíamos dicho que queremos Barcelona ejecutamos y efectivamente obtenemos el 30, el 3 y el 8, bien pero realmente nosotros queríamos obtener estos valores, notar que imprime las claves pero ¿qué pasa si queremos imprimir los valores? que en este caso son los nombres, tendríamos que adecuar el cuerpo de la función para que lo ejecute por ello es necesario definir el cuerpo en base al tipo de datos del argumento, para ver lo mejor adecuamos nuestra función en caso sea de tipo diccionario, para que impriman los valores y no las claves, a ver vamos a definir nuevamente una función, vamos a hacer def imprime palabras, 2, vale, voy a abrir el paréntesis, le voy a pasar nuevamente el parámetro jugadores, vamos a hacer dos puntos y vamos a hacer lo siguiente, bien yo quiero que imprima en caso mi argumento sea un diccionario los valores, entonces lo que quiero hacer es lo siguiente, dentro del cuerpo voy a validar si el argumento que le estoy pasando es un tipo de dato en específico y si es un tipo de dato en específico le voy a hacer que ejecute ciertas acciones de acuerdo, de acuerdo al tipo de datos, por ejemplo si el tipo de dato de jugadores es igual a una lista, voy a hacer dos puntos y le voy a decir que ejecute lo siguiente, básicamente lo mismo que hemos creado en estas líneas, for i in jugadores, dos puntos, print i, caso contrario para yo voy a usar un elif, si el tipo de dato de jugadores que es el parámetro bien es un diccionario, quiero que hagas lo siguiente, voy a guardar en esta variable valores, lo siguiente jugadores que es un diccionario en este caso, bien le voy a sacarlos valores haciendo uso de la función valores, hacemos valores, abrimos paréntesis, bien y seguidamente lo que voy a hacer es usar un for, for i in valores, voy a hacer dos puntos y voy a imprimir también cada iteración, cada i, vamos a ejecutar para guardar, para crear la función imprimir palabras, barra baja dos, ejecutamos y ahora voy a crear nuevamente, a ver simplemente podemos copiar la lista y el diccionario en esta celda, lo voy a traer para tenerlo a la vista, tenemos a real Madrid que es un tipo de dato lista y tenemos el Barcelona que es un tipo de dato diccionario, vamos a ejecutar para nuevamente guardar los valores dentro de las de la variable que hemos creado y vamos a llamar a la función imprimir palabra dos, simplemente colocando el nombre, vamos a abrir el paréntesis y le vamos a pasar el argumento real Madrid, veamos que obtenemos, ejecutamos el nombre de la variable está incorrecta, jugadores no está definido, por aquí hemos escrito mal jugadores, acá está el error, jugadores, muy bien ahora sí ejecutamos nuevamente la función imprimir palabras dos que por allí siempre hay que tener cuidado con los valores o los o cómo escribimos bien este cada palabra cada parámetro, vale, bien entonces ahora sí otra vez ejecutamos esta celda y ejecutamos, bien entonces en esta ocasión real Madrid contenía una lista y como era una lista el cuerpo de la función continuó, continuó ejecutando las instrucciones que están después, después del if, vale, porque jugadores efectivamente era una lista, por eso que imprime los valores dentro de ella, bien, hicimos el mal Rodrigo, vale, y ahora voy a llamar nuevamente a la función imprime palabras, ojo que hay que escribir siempre bien, vale, para que funcione, abro paréntesis, coloco Barcelona, ejecutamos y en esta ocasión sí obtenemos los valores más no los índices, por qué, porque como el tipo de datos de Barcelona es un diccionario, Python continuó ejecutando las acciones que están después del if, ahora vamos a ver, a detalle, la declaración retur, vale, a ver, vamos a abrir un poco, vamos a añadir un poco de celdas, perfecto, bien, una función puede o no devolver un valor, lo que significa que la definición de una función puede llevar o no la declaración retur, que una función lleve la declaración retur es importante para tener la posibilidad de guardar el valor que devuelve una función en una variable o reutilizarlo en otra función, en nuestra función anterior no definimos la declaración retur como te has dado cuenta, solo imprimimos ciertos valores por lo que no podremos guardar nuestra salida en alguna variable, voy a hacer, voy a copiar, voy a hacer uso de esta función, copiar y pegar en esta celda, voy a hacer lo siguiente, voy a también usar la lista que creamos de la red madrid y Barcelona, lista y diccionario, y vamos a ejecutar, vamos a ejecutar estas dos celdas, simplemente hemos traído la función que habíamos creado anteriormente y también hemos traído la lista y diccionario que creamos anteriormente, vale, muy bien, entonces lo que vamos a hacer es lo siguiente, vamos a crear en una variable a, y en él voy a guardar lo que lo que obtenga del imprime palabras 2 utilizando la lista red madrid, muy bien, voy a ejecutar y obtenemos el print de cada elemento de la lista Vinicius, Benzema y Rodrigo, a ver ahora quiero averiguar si justamente los valores que están dentro de la variable a son precisamente los elementos de cada, los elementos de nuestra lista red madrid, veamos, a ver si imprimo a, ejecutamos y obtenemos un valor de non, notar que esta variable no guarda ningún dato, no tiene ningún dato, pero qué pasa si añadimos la declaración retur en lugar de print, qué pasa, a ver vamos a copiar y pegar nuevamente nuestra función de ejemplo, vamos a copiar y pegar nuevamente también la lista y el diccionario, pero en esta ocasión vamos a colocar la palabra retur en lugar del print, vale, entonces voy a hacer un retur y, aquí falta la t, retur y, vale, aquí también le cambiaré el print y le voy a poner retur y, muy bien, entonces, vamos a ejecutar a ver, voy a llamarle par a parline a tres, ejecutamos y ya hemos creado la función imprime palabra tres, pero en esta ocasión con, el con la palabra reservada a retur vale, veamos ejecutamos nuevamente la lista y el diccionario y veamos qué pasa si queremos hacer el mismo ejercicio es decir crear una variable a y en el guardar el resultado que obtenemos de la función imprime palabras a ver vamos a copiar y pegar. En este caso nuestra función se llama imprimepalagoras barra baja 3 y veamos qué sucede. Ejecutamos, ejecutamos, vale, y ahora vamos a imprimir el valor de A. Ejecutamos y en esta ocasión obtenemos, obtenemos el valor de definicios. Notar que solamente hemos obtenido un solo valor, un solo valor. Ahora ¿qué pasa? Si otra vez quiero crear una variable B pero en lugar de usar el real Madrid voy a usar Barcelona. Vamos a ver si sucede lo mismo que lo que ha sucedido usando el real Madrid. Usaré el Barcelona ahora. Vamos a ver qué pasa. Ejecutamos, ahora quiero imprimir la variable B para ver qué guarda. Bien, y esta vez también. Notar que solamente hemos obtenido el primer valor de cada elemento, pero esto pasa porque el cuerpo no está construido para obtener todos los valores de mi diccionario o de mi lista. Bien, sino que solo obtiene el primero. ¿Y qué pasa si ahora queremos una función que convierta los valores de un diccionario a una lista? Bien, para este ejemplo vamos a modificar la función anterior. Vamos a copiar y pegar bien la función anterior. Y ahora le vamos a llamar barra baja 4 al final. Bueno, mejor lo voy a cambiar el nombre y le voy a llamar convierte lista o convierte lista, vale. También va a tener un parámetro jugadores y en esta ocasión, a ver, vamos a borrar esto, y en esta ocasión lo que vamos a hacer es vamos a crear una lista vacía dentro de la variable lista de esta forma. ¿Basía? Luego vamos a crear una variable valores y en él voy a extraer los valores de la lista jugadores. Luego lo que voy a hacer es lo siguiente. Voy a hacer un for, for i, in valores, voy a hacer dos puntos, voy a llamar a la lista vacía y haciendo uso de la función append, voy a abrir paréntesis, voy a colocar el i. Lo que voy a hacer es añadir cada valor que recorra mi variable i en la lista valores y lo voy a añadir en la lista vacía lista. Entonces, finalmente lo que quiero retornar como valor de esta función para ello hago uso de retur. Quiero que retorne la lista que en principio va a estar vacía, vale. Bien, entonces vamos a hacer lo siguiente, vamos a ejecutarlo y ahora quiero crear por ejemplo el diccionario Barcelona, bueno lo voy a traer simplemente, copio y pego, lo ejecuto y vamos a ver qué pasa si creo la variable b y en él voy a guardar lo que retorne de mi función convierte lista, por ejemplo lo voy a llamar convierte list, voy a abrir el paréntesis y voy a colocar el argumento que me obliga a colocar. Entonces en este caso quiero colocar Barcelona, bien y voy a imprimir en esta misma celda lo que obtenga de Barcelona, bien ejecutamos y efectivamente obtenemos en valor de una lista los valores que contienen mi diccionario. Con esto quiero dejar claro que cada función tiene un objetivo específico, que cada una resuelve un problema en concreto y podemos crear el cuerpo con las sentencias que creamos convenientes, pues bajo esta premisa resulta recomendable en la medida que sea posible documentar nuestras funciones, punto importante que veremos ahora, entonces vamos a borrar estas celdas y vamos a ver cómo documentamos funciones, a ver vamos a abrir, documentar una función resulta de muchos casos una tarea tediosa y probablemente la consideremos una tarea sin importancia, sin embargo la documentación será vital al momento de entender o explicar una función determinada, en una documentación se indica que hace el cuerpo de la función, tipo de datos que deben tener los argumentos y el valor de retorno, tomemos el ejemplo de nuestra función anterior, vale, tener en cuenta que hay muchas formas de documentar una función y aquí les muestro una forma, vamos a copiar la lista en la función anterior, lo copiamos aquí y para poder documentar como dije lo vamos a hacer después de la palabra reservada def, después lo que quiero decir en la siguiente línea y vamos a usar las comillas que representan digamos comentarios, líneas donde nos van a ayudar a describir lo que queremos hacer con esta función, vale, entonces para nosotros dentro de Python dentro de Jupyter Notebook nosotros podemos escribir ciertos comentarios haciendo uso de la almohadilla o también de las tres comillas, vale, entonces hacemos lo siguiente, perfecto, ahora en primer lugar yo puedo colocar el objetivo final de la función, bien, entonces para este ejemplo en específico voy a colocar función que convierte los valores de un diccionario en una lista, entonces básicamente lo que estoy colocando en primer lugar es el objetivo de la función, el objetivo final, vale, ahora seguido puedo colocar los parámetros, parámetros, voy a hacer como un subbrayado y debajo de ello voy a colocarlos, entonces voy a poner jugadores, voy a hacer dos puntos y voy a poner el tipo de valor que tiene que ser, en este caso y para esta función en específico el parámetro jugadores debe ser un diccionario, vale, entonces lo pongo aquí abajo diccionario diccionario de entrada, muy bien, luego lo que puedo lo que puedo hacer es colocar o especificar que retorna, para eso coloco retorno, voy a hacer una una especie de subbrayado y fuera de ello voy a colocar lo que retorna, el tipo de valor que retorna mi función es una lista, vale, es una lista, bien, entonces por eso coloco list, entonces luego puedo colocar una descripción, es una lista con los valores del diccionario de entrada, vale, es una lista con los valores del diccionario de entrada, luego puedo colocar si quiero un ejemplo, puedo colocar example, example, luego colocar una especie de subbrayado y hacer un ejemplo, por ejemplo, a ver, vamos a hacer el mismo ejercicio que hicimos con el diccionario Barcelona, puede ser, vale, entonces vamos a hacer lo siguiente, vamos a traer el nombre de la función convierte lista, vamos a abrir paréntesis y dentro de él dentro del parámetro, en lugar de poner la variable voy a colocar el diccionario directamente, entonces voy a colocar este diccionario, voy a cerrar paréntesis y voy a colocar la salida de ejemplo, básicamente como ya hemos ejecutado esta función con este diccionario y hemos obtenido esta lista pues simplemente voy a copiar y pegar lo que ya hemos obtenido como ejemplo en mi documentación, vale, entonces tengo a Gabi, Piqué y Alves como ejemplo, vale, y de esta forma puedo documentar la función, entonces decíamos que documentar una función resulta muy importante para nosotros mismos darle mantenimiento a nuestras funciones, a nuestros programas, a futuro o también para que otras personas lo puedan entender, bien, muchas veces como como habíamos comentado, cada función tiene un objetivo distinto, un objetivo final distinto, tienen un cuerpo que manejan ciertos parámetros, ciertos argumentos de una forma distinta, bien entonces en ciertas ocasiones o casi siempre va a ser necesario que nosotros documentemos nuestra función, especificando básicamente cuál es objetivo, cuáles son los parámetros, los tipos de datos que necesita bien la función, lo que retorna y si quiero un ejemplo, bien, continuamos, ahora vamos a ver cómo podemos, a ver vamos a borrar estas celdas y vamos aquí a añadir un poco de celdas, ahora vamos a ver cómo podemos importar módulos, existen módulos o librerías en Python que ya están definidos para resolver problemas en específicos, tenemos el ejemplo de pandas, mat o random, cada uno de ellos lleva consigo una serie de funciones o procedimientos llamados métodos, un módulo es una biblioteca de código, es un archivo que contiene un conjunto de métodos o funciones, bien, Python lleva consigo módulos muy útiles, evitando la programación extra, para importar módulos tenemos varias formas, cada una debe respetar cierta sintaxis, vale, por ejemplo, a ver, la primera forma en la que podemos importar un módulo es la siguiente, vamos a utilizar la palabra reservada import y seguidamente vamos a colocar el nombre, el nombre del módulo, muy bien, esta es una primera forma, una segunda forma es colocando la palabra reservada from, luego el nombre del módulo, seguidamente la palabra reservada import y luego el elemento o función que queremos traer a partir del nombre del módulo, a ver, voy a llamarle elemento, que es básicamente, a ver, voy a poner entre paréntesis, básicamente esa referencia un método o función y tenemos la tercera forma que es de la siguiente forma, llamando a import colocamos el nombre del módulo que es muy parecido a la primera, pero en esta ocasión le hacemos un alias, el alias lo hacemos de la siguiente forma, hacemos uso de la palabra reservada as, alias, a veces tenemos un nombre de módulo un poco largo o complicado, entonces podríamos llamarle de otra forma un poco más sencilla, la primera forma hace accesible a todos los métodos que tiene un módulo, o sea, si importamos un módulo de esta primera forma podemos hacer uso de todos los métodos, de todas las funciones que contiene este módulo, de la segunda forma importa algunos métodos de un módulo, bien a lo mejor no queremos usar todos los métodos, todos los métodos, sino solamente hace falta importar un método o dos de un módulo y de la tercera forma, bien como dije, bien hacemos un alias del módulo importado, veamos algunos ejemplos, a ver vamos a borrar estas celdas que nos en falta, bien y vamos a crear aquí celtas, por ejemplo haremos uso de un módulo que trabaja con números aleatorios llamado random, vamos a hacerlo de la siguiente forma, vamos a importar random, ejecutamos y primero llamamos al módulo importado, luego seguido de un punto colocamos el método que queremos usar y haremos uso de la función random que devuelve un número aleatorio entre dos números, bien de la siguiente forma llamamos al módulo importado, como dijimos hacemos un punto, vamos a hacer uso de la función random y vamos a abrir el paréntesis y vamos a colocar dos números, a ver vamos a ver qué obtenemos, ejecutamos y obtenemos el número 9, bien el número 9 es un valor un número aleatorio entre estos dos límites entre el 2 y el 10, cosa que se ejecutamos nuevamente, podemos obtener otro número porque es un valor aleatorio, este es un método, esta es una función del módulo random, continuamos con la segunda forma, por ejemplo vamos a seguir con el módulo random pero esta vez vamos a hacer uso del método choice, esta nos permite seleccionar de manera aleatoria una serie de elementos de un objeto, lo vamos a hacer de la siguiente forma, vamos a hacer from el nombre del módulo random y voy a hacer un import de choices ejecutamos para llamar este método y lo que voy a hacer es crear para este ejemplo una lista en donde guarde nombres de jugadores, por ejemplo Messi, como Maradona y Ronaldo, no cristiano sino Ronaldo Nazario, vamos a ejecutar para guardar esta lista dentro de la variable y ahora voy a hacer uso del método choices que he importado del módulo random, para ello llamo a choices, y choices necesita que le pase el nombre de la variable que guarda la lista, por ejemplo colocamos lista y luego necesita que le coloque el parámetro K con el número de palabras que quiero que extraiga de la lista de forma de forma aleatoria, en este caso voy a querer que extraiga sólo dos elementos, vamos a ejecutar y obtenemos Ronaldo y Ronaldo, si ejecutamos nuevamente podemos obtener de manera aleatoria dos elementos de la lista, incluso se puede repetir como hemos visto en la primera ejecución, ejecutamos nuevamente tenemos Ronaldo, Maradona en esta tercera, Messi y Maradona en esta cuarta, notar que la sintaxis cambia ligeramente, en este caso podemos llamar al método directamente sin hacer referencia a su módulo, continuamos con la tercera forma, esta vez tomaremos el ejemplo de la primera, pero haremos una pequeña modificación, por ejemplo quiero importar random y le quiero llamar como alias para eso hago uso de las y R simplemente, entonces lo que vamos a hacer es ejecutar el mismo método de random, pero en esta ocasión random tiene un alias de R, entonces si yo simplemente coloco r punto Rending 210, voy a obtener el mismo resultado que he obtenido en esta celda, a ver vamos a ejecutar, a ver entero no atributo Rending, vale es que aquí no hemos ejecutado, no hemos ejecutado esta celda, es decir no hemos no hemos llamado R a random como alias, entonces vamos a ejecutar y volvemos a ejecutar esta celda y obtenemos efectivamente el mismo resultado, es decir random es llamado R, puedo hacer lo mismo otra vez R Rending por ejemplo del 5 al 100 ejecuto y obtenemos el número 93, muy bien