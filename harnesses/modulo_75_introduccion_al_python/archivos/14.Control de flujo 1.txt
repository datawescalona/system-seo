Muy bien, ahora vamos a ver qué es un control de flujo en Python. Veremos los diferentes tipos de flujo condicional como por ejemplo el if, el elif y el elif. Seguidamente veremos qué son las instrucciones iterativas básicamente el while y el for in. Ahora vamos a entrar al tema de los controles de flujo en Python, un punto muy importante al momento de construir nuestros programas. En primer lugar vamos a preguntarnos qué es una instrucción de flujo condicional. Las instrucciones condicionales permiten elegir una acción según el resultado al evaluar una condición. Vamos a decir que una condición es una expresión que puede tomar dos valores, este puede ser true o false, es decir de tipo buliano. Digamos que en la instrucción condicional siempre hay una expresión buliana que gobierna cuál es la acción a realizar. Para llevar a cabo los ejemplos que veremos a continuación vamos a respetar la siguiente estructura. Decíamos que tenemos que evaluar una condición y que si esta es verdadera le vamos a decir a Python que ejecute ciertas acciones, caso contrario si la condición es falsa le diremos a Python que ejecute otras acciones. La primera instrucción condicional que veremos será if else y su comportamiento es exactamente lo que describimos al principio. Veamos un ejemplo, digamos que quiero hacer un programa que me ayude a decidirse un alumno a prueba el curso o no. En primer lugar vamos a declarar una variable, se va a llamar nota y en él vamos a guardar la nota final de un examen, por ejemplo la nota 10 en él. Para este ejemplo ejecutamos para guardar el valor dentro de la variable y en esta celda vamos a construir la instrucción iniciando con la palabra reservada if. Seguido a esta palabra if vamos a colocar la condición que vamos a evaluar. Decíamos que si el alumno aprueba el curso vamos a realizar cierta acción. Entonces qué es lo que podemos hacer con esta variable quedada, lo que podemos hacer es evaluar si la nota es mayor a 11 por ejemplo. Entonces lo que he escrito a continuación del if es una condición bien que Python va a evaluar. Si esta operación, si esta operador resulta ser true le vamos a decir a Python que ejecute una acción. Como hacemos esto después de la condición vamos a poner dos puntos, luego hacemos un enter o un salto de línea, por ejemplo que imprima la palabra aprobado. Caso contrario si la condición resulta ser falsa vamos a usar la palabra reservada el se. Seguido de dos puntos luego hacemos un enter o un salto de línea y luego vamos a colocar las instrucciones que queremos que ejecute mi programa en caso mi condición resulte ser falsa. En este caso voy a hacer que imprima la palabra desaprobado. Ejecutamos para ver qué obtenemos como resultado y obtenemos la palabra desaprobado con lo que quiere decir que la evaluación de esta condición fue falsa. ¿Por qué? Porque Python ejecutó las acciones que están después de la palabra el se. Ahora notar que nota guardaba el número 10 y entonces Python al evaluar esta condición es decir al evaluar si 10 era mayor a 11, vamos a ejecutar para ver Python que obtuvo, obtuvo un valor de falso. Por eso es que esta instrucción ejecutó la acción que va después del el se. Una cosa importante, notar que las acciones tienen un espacio a la izquierda, esto se llama identación. Estoy hablando de estos espacios. Tener cuidado con este espacio debido a que Python no reconocerá las instrucciones que estén fuera de la identación como una acción. ¿Y qué pasa si queremos evaluar dos condiciones? Pues haremos uso de los operadores lógicos que ya vimos anteriormente. Veamos ejemplos de ellos. Digamos que quiero evaluar la nota del examen final del alumno, lo que ya habíamos evaluado en este ejemplo, pero además quiero evaluar que si entregó el trabajo final en formato pdf. Para este ejemplo vamos a crear una variable adicional. Voy a crear la variable tfm y en él voy a guardar el valor booleano true ejecuto. Y también voy a crear la variable nota barbaja2 y en él voy a guardar en este caso la nota 11 ejecuto para guardar el valor dentro de la variable y ahora vamos a construir nuestra instrucción if else. Decíamos que tenemos que empezar siempre con la palabra if. Seguido vienen las condiciones que vamos a evaluar y lo que vamos a hacer es si nota es mayor a 11 y decíamos que tenemos que incluir, es decir, se tiene que cumplir dos condiciones para que el alumno pruebe. Entonces para este caso vamos a usar el operador ant. Ant tfm es igual a true, es decir, que si el alumno tiene como nota del examen final mayor a 11 y además entregó su trabajo final en formato pdf, haremos dos puntos, colocaré el print aprobado como acción que quiero que ejecute python si es que estas dos condiciones resultan ser verdaderas. Caso contrario, uso la palabra else, seguido de dos puntos, un enter o salto de línea ejecuta esta acción, que es imprimir la palabra desaprobado. Recordar que el operador ant entrega el valor verdadero cuando las dos condiciones sean verdaderas. Bien, vamos a ver si ejecutamos, que obtenemos y en este caso obtenemos la palabra desaprobado. ¿Por qué? Vale, a ver, a ver, acá puse la variable nota, pero debería ser la variable nota barraja 2. Ejecutamos nuevamente y bueno, de todas formas obtenemos la palabra desaprobado, lo que quiere decir que la evaluación de estas dos condiciones resultó ser falso. Vamos a ver por qué, nota barraja 2 guarda el número 11. Entonces python al evaluar esta primera condición de 11 es mayor a 11, resultó ser falso y decíamos que para este ejemplo y para este operador lógico las dos condiciones deben de arrojar un resultado verdadero, es decir, un valor voleno tú. Como ya obtuvimos en una de ellas el resultado false pues el camino al ejecutar, en este caso específico, fue la de imprimir la palabra desaprobado efectivamente como hemos obtenido. Ahora digamos que la condición para que aprobado el curso es que cumpla con el menos una de estas dos condiciones. En este caso vamos a usar el operador lógico or, veamos a ver, if nota es mayor a 11 or tfm es igual a true. Bien hacemos dos puntos, luego las acciones que quiero que ejecute python, aprobado, luego la palabra else hacemos dos puntos, la acción que si es que las condiciones son falsas, quiero que imprima desaprobado. Bien muy similar al código que escribimos en esta celda pero en este caso y para este ejemplo estamos diciendo que para que el alumno apruebe el curso debería de pasar una al menos una de estas dos condiciones que estamos colocando acá. Para ello estamos usando el operador lógico or, vamos a ver que obtenemos al ejecutar. En este caso y para este ejemplo en específico estamos obteniendo el valor de aprobado, porque lo que quiere decir es que python al evaluar estas condiciones al menos una de estas dos ha sido verdadera. Vamos a ver nota 2, guarda el valor de 11, python al preguntar si 11 era mayor a 11 resultó ser falso. Bien 11 es mayor a 11 ejecutamos y efectivamente obtenemos falso. Pero como tenemos el operador lógico or también debería de ejecutar y esperar a ver si es que esta operación resulta un true. Entonces tefeme guardaba o guarda el valor booleano true y al evaluar esta condición python debió de obtener un valor booleano true. Veamos tefeme es igual a true, ejecuto y obtengo efectivamente el valor booleano true. Como al menos de estas dos condiciones fueron verdaderas, python continuó, continuó por este camino, es decir por las acciones que están después, después de mi palabra if y no las acciones que están después de la palabra else. Muy bien, continuemos con la instrucción if que es una variación de la instrucción if else, en este caso no haremos uso de la palabra else. Recordar que se puede construir una instrucción de flujo condicional con un if sin hacer uso del else, pero no un else sin un if, lo que significa que puedo construir un flujo condicional evaluando solo un caso, en lugar de 2. Por ejemplo, digamos que quiero evaluar la condición de si comprar una entrada al estadio de mi equipo favorito, solo si clasifica a la final de la copa. Para ello en esta celda voy a crear una variable por ejemplo clasificar y en él voy a guardar un valor booleano en este caso true, clasificar. Esta variable vamos a decir que va a guardar una respuesta si es que mi equipo pasó a la final de la copa o no, en este caso quiero que sea true, es decir quiero que sea verdadero, paso a la final de la copa. Bien eso es lo que va a guardar la variable clasificar, vamos a ejecutar para guardar el valor dentro de la variable y ahora en esta celda vamos a crear la instrucción if, vamos a empezar nuevamente con la palabra if, luego vamos a escribir la condición que queremos evaluar, en este caso clasificar es igual a true, en caso sea verdadero vamos a hacer 2 puntos, hacemos un salto de línea, vamos a imprimir ir al estadio, notar que no voy a hacer uso del else entonces vamos a ver qué obtenemos, efectivamente obtenemos la palabra ir al estadio, ¿Por qué? Porque Byton al momento de evaluar esta condición ha obtenido una respuesta true, ¿Por qué? Porque clasificar guardaba el valor booleano true y al decir o al preguntarse si clasificar es igual a true, la respuesta efectivamente es un valor de verdadero, por lo que hemos obtenido la impresión de esta frase ir al estadio, pero ¿qué pasa ahora? Si mi variable clasificar guarda un valor booleano false, bien vamos a ver qué pasa, vamos a ejecutar para guardar el nuevo valor a la variable clasificar, vamos a copiar y pegar este mismo escenario de if y vamos a ver qué pasa, ejecutamos y notar que en la instrucción if, cuando la variable clasificar es falso, nota ningún resultado, debido a que comite todas las instrucciones dentro del if, ¿Pero qué pasa si mi programa tiene otro código fuera del if? Pues continúa con las instrucciones del programa omitiendo el contenido del if, bien veamos un ejemplo, por ejemplo voy a seguir con este mismo código, copiamos y pegamos en esta celda y lo que vamos a hacer es colocar otra línea de código fuera, fuera de las acciones cuando esta condición sea verdadera, por ejemplo voy a colocar un prin, otro código fuera del if, es decir cuando la condición sea verdadera, a ver vamos a ver qué obtenemos al ejecutar y efectivamente si se dan cuenta el código ha continuado bien ejecutando todas las acciones omitiendo, omitiendo las acciones que están dentro del if, ¿por qué? Porque la condición resulta con un valor booleano de falso, por lo que hemos obtenido esta frase, otro código fuera del if, condición verdadera, a ver vamos a continuar vamos a eliminar las celdas, por otro lado podemos anidar condicionales, es decir podemos añadir un if dentro de otro if y combinar tipos de instrucciones dentro de una instrucción digamos principal, por ejemplo supongamos que queremos evaluar el tipo de una variable en una primera instancia, tipo de una variable, bien y luego quiero evaluar si esta variable contiene el texto o número que buscamos, contiene texto o número que buscamos, a ver en esta misma cella voy a crear diferentes variables, la variable a que guarde el string hola, la variable b que guarde el entero tres, la variable c que guarde el string estoy bien, ejecutamos para guardar los valores dentro de las variables y en esta cella vamos a empezar a crear las condicionales, empecemos con el primero hacemos un if, después del if vamos a colocar la condición que queremos evaluar, en este caso decíamos que queremos averiguar si el tipo de una variable es el tipo que estamos buscando, por ejemplo usamos la función type, vamos a abrir paréntesis y dentro de él vamos a colocar por ejemplo la variable c y le vamos a preguntar a python si el tipo de dato de la variable c es un string, si esta condición se cumple voy a hacer que python ejecute lo siguiente y aquí en esta línea puedo colocar diferentes acciones, incluso puedo colocar un segundo if, vamos a por ello colocamos un segundo if y decidamos que también queríamos evaluar si una variable contenía un texto o un número que estoy buscando, por ejemplo si c que es la variable que estoy trabajando, si c es igual a hola, si esto es verdadero le voy a decir que ejecute lo siguiente, que imprima es un saludo, en caso esta condición sea falsa haré uso de la palabra el se, hacemos dos puntos y que ejecute no es un saludo, estas instrucciones lo puedo dejar tal cual está, o pueda ser uso de un else, en este caso haré un uso re uso de un else porque quiero imprimir en caso esta condición sea falsa la frase no es un string, bien vamos a ejecutar para ver qué obtenemos y obtenemos la frase no es un saludo, es decir hemos obtenido las acciones que están después del else anidado, veamos paso a paso y por qué hemos obtenido como resultado este print, a ver en la primera línea estamos preguntándole a python si el tipo de dato que contiene c es un string, bien veamos a ver si copiamos y pegamos en esta celda vamos a ver qué obtenemos ejecutamos y obtenemos el valor de true, por lo que quiere decir que python va a recorrer por el camino que está después del if, entonces en la segunda línea python tiene otro if y esta condición va a ser evaluado, a ver vamos a copiar y pegar ejecutamos y obtenemos el valor de falso, lo que quiere decir que python omitirá las acciones que están dentro del if anidado, bien y continuará su camino por el else, bien y este else dice que imprima no es un saludo y efectivamente es lo que hemos obtenido en esta celda, hagamos otro ejemplo para que esto quede claro, voy a hacer uso del mismo código, copio y pego, pero en este caso haré uso de la variable a, en lugar de c lo cambio por a, esta segunda línea también, coloco a, bien y vamos a ejecutar para ver qué obtenemos, obtenemos la frase es un saludo, veamos por qué, a ver en esta primera línea python está evaluando la primera condición si el tipo de dato de a es un string, vamos a copiar y pegar este trozo de código y obtenemos el valor de true, por lo que python va a continuar por el camino que está después del if, el primer if, en esta segunda línea está evaluando si a es igual a ola, vamos a copiar y pegar este trozo de código, obtenemos el valor de true, por lo que python va a efectuar lo que está dentro del if anidado y va a omitir lo que está dentro del else anidado, por lo que va a imprimir es un saludo y efectivamente es lo que hemos obtenido al ejecutar esta celda, hagamos un último ejemplo para que esto quede claro, vamos a continuar con el mismo código, copiamos y pegamos y en este caso voy a hacer uso de la variable b que contiene el número 3, bien entonces voy a colocar el b, voy a cambiar la condición, quiero preguntar si es un entero, esta segunda condición anidada, si b es igual a por ejemplo a 3 o a 5, quiero que imprima no es 3, de lo contrario quiero que imprima es otro número, cambiado y si el tipo de dato de b no es un entero, quiero que imprima cambiar por un string, por ejemplo, bien entonces veamos que obtenemos el ejecutar este código, obtenemos es otro número, cambiar, si se dan cuenta lo que ejecutó python son las acciones que están después del else del if anidado, veamos también paso a paso, en primer instancia python está evaluando la condición si el tipo de dato de b es igual entero, veamos, copiamos y pegamos este trozo de código ejecutamos y obtenemos el valor de true, por lo que python va a continuar con las acciones que están después del primer if, en la segunda línea python va a evaluar esta condición que si b es igual a 5, vamos a ver que obtenemos, obtenemos el valor de falso, por lo que python va a omitir la acción que está después del if anidado y va a ejecutar lo que está dentro del else, por qué? porque la condición de b es igual a 5, da como resultado el valor de falso, por lo que hemos obtenido el string es otro número cambiar, bien vamos a continuar, vamos a eliminar estas celdas que no hacen falta y vamos a continuar con la instrucción condicional el if, bien esta instrucción permite elegir una acción entre más de dos opciones, nuestro código más o menos se comportará de la siguiente manera, bien por ejemplo vamos a tener siempre un if, vamos a tener una condición después de ella, por ejemplo le vamos a llamar condición 1, si esta condición 1 es verdadera haré que python ejecute las siguientes instrucciones, instrucciones, instrucciones 1, si esta condición es falsa le dirá python que evalúe una segunda condición, si esta condición es verdadera haré que ejecute las siguientes instrucciones y si las dos condiciones anteriores son falsas le diré que evalúe una tercera condición 3 y si esta es verdadera que ejecute una serie de instrucciones 3 y en caso todas sean falsas, en última instancia diré que ejecute después de lo que tenga mi else, por ejemplo una instrucción 4, la estructura podría contener solo el if y el if sin hacer uso del else porque si no cumple con todas las condiciones de los el if simplemente no hará nada, es decir puedo obviar este else dentro de una estructura el if, si miramos el ejercicio anterior y suponemos que tenemos más de dos condiciones dentro de cada if, bien simplemente haremos que nuestro código sea muy complejo, en este caso lo recomendable es construir la estructura el if porque con el tendríamos un código más claro más ordenado, entonces veamos cómo hacerlo, supongamos que tenemos que poner etiquetas por así decirlo a una nota de un examen del curso, por ejemplo vamos a crear una variable en esta celda que se anota y en el vamos a guardar el número 15, por ejemplo en esta misma celda vamos a crear una lista dentro de la variable etiquetas, abrimos corchetes y vamos a colocar por ejemplo malo otro string que sea regular, otro string que sea bueno y por último un string que sea excelente, estas serán mis etiquetas, vamos a ejecutar por abordar los valores dentro de las variables y vamos a empezar a crear mi estructura el if, a ver podemos hacer un if colocar la primera condición y voy a decir si nota es menor a 5, bien si esto se cumple ejecuta lo siguiente, haz un print por ejemplo voy a usar la lista que es etiqueta y voy a decir que imprima el primer valor que contiene mi lista y lo voy a llamar por índice, esto ya lo hemos estudiado, si es menor a 5 haré que imprima el string malo, el string malo hace referencia al índice 0, por lo que al llamar o al colocar dentro del corchete el número pondré el 0, ahora si esto es falso voy a tener que evaluar otra condición por ejemplo si la nota es mayor o igual a 5 y que la nota sea menor a 11, si esto se cumple le voy a decir que imprima el valor que contiene el string regular de la lista etiquetas, es decir en este caso el índice 1 hace referencia al string regular, si esto es falso quiero que evalúe lo siguiente, por ejemplo hago un if si nota es mayor o igual a 11 y nota es menor a 19 haré que ejecute lo siguiente que imprima el valor que hace referencia a bueno, es decir coloco el nombre de la lista, abro corchete y coloco el índice, en este caso el índice es 2 haciendo referencia al string bueno, a ver si todas estas condiciones no se cumplen pues haré que imprima el string excelente, entonces hago uso de un else, hago else 2 puntos y que imprima etiquetas, si etiquetas, abro corchete y coloco el índice número 3, vamos a ejecutar y veremos que a ver, acá hay un problema de sintaxis, acá no le he puesto ningún valor, bien lo segundo era nota mayor o igual a 5, ejecutamos y tenemos otro tipo, vale esto tiene que ir junto, hay que tener cuidado con la sintaxis, ahora si, uy etiqueta, bien, bien, bien, bien, la lista que está guardada dentro de la variable es etiquetas, bien, aquí hemos puesto etiqueta y es etiquetas, ahora si, bien, bueno, como resultado hemos obtenido la palabra bueno, bueno, porque? porque la nota contiene el número 15, bien, entonces vamos a un poco analizar cada paso de esta estructura, bien, entonces la primera condición es si la nota es menor a 5, nota guarda el valor de 15, 15 es menor a 5, bien, entonces por ejemplo vamos a copiar este pedazo de código, vamos a pegar, ejecutamos y obtenemos el valor de falso, claramente, si es falso, Python va a evaluar la siguiente condición que está dentro del segundo o del primer leaf, entonces vamos a evaluar esta segunda condición, copiar y pegar, ejecutamos y obtenemos falso, entonces Python va a continuar con el siguiente leaf, con la siguiente condición, en este caso es esta, entonces copiamos, ejecutamos y obtenemos efectivamente en esta ocasión el valor de true porque? porque 15 sí cumple con esta condición, por lo que debería de imprimir el valor que hace referencia al índice número 2 de la lista, en este caso es bueno, que es efectivamente lo que hemos obtenido al ejecutar esta instrucción, notar que el leaf permite añadir una condición adicional a un leaf, bien, nosotros podemos colocar todos los leaf que queramos, no hay un límite, recordar que también podemos copiar el uso del else porque como hemos dicho en un inicio, si no colocamos else y si todas las condiciones anteriores son falsas, simplemente el programa no hará nada, recordar eso. Bueno, ahora es que voy a escribir para escribirnos a nosotros y os atendamos y luego tenéis aquí tres cosas.