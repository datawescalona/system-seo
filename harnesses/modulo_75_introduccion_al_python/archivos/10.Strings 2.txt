Ahora continuamos con otro punto, borremos estas celdas, a ver continuamos con las comparaciones en strings, los strings se comparan según el orden alfabético, las mayúsculas son anteriores o menores por así decirlo a las minúsculas, veamos algunos ejemplos, a ver aquí agregamos más celdas, ahora veamos un ejemplo con el orden alfabético, vale, donde por ejemplo B tendría que ser mayor a la letra A, veamos un ejemplo, a ver defino directamente la letra A, la letra A es un string por eso estoy usando las comillas, inicio y cierro con comillas para que sea un string, entonces que hago, voy a compararlo, voy a preguntarle a Python si A es mayor a la ejecutar, veamos que sucede, obtenemos el valor buliano false, ¿por qué? porque B tendría que ser o es mayor a según las comparaciones que tiene Python, decíamos que los strings son comparados según el orden alfabético, ahora quiero comparar por ejemplo la letra A, quiero preguntarle a Python si la letra A es menor a Z, vamos a ver que tendríamos como resultado, deberíamos entender el valor buliano true, porque realmente A es menor a Z, ejecutamos y efectivamente obtenemos el valor buliano true, ahora veamos otro ejemplo con mayúsculas y minúsculas, vale por ejemplo definimos la letra j de mayúscula, le voy a preguntar a Python si la mayúscula es mayor a la letra minúscula j, deberíamos obtener el valor buliano falso, porque las mayúsculas son menores que las minúsculas, ejecutamos y efectivamente obtenemos el valor buliano de falso, veamos un último ejemplo, quiero definir en mi variable A la letra M en mayúscula y en esta misma celda quiero definir en mi variable B la letra M en minúscula y en esta misma celda quiero hacer una comparación y voy a preguntar a Python si A es menor a B, veamos que obtenemos el ejecutar, ejecutamos y efectivamente obtenemos el valor de true ¿Por qué? Porque la variable A contiene la letra M en mayúsculas y la letra M en minúsculas está contenida en mi variable B, por eso hay que preguntar si M en mayúsculas es menor a M escrito en minúsculas, el resultado es true ¿Por qué? Porque lo vuelvo a repetir, las mayúsculas son anteriores o digamos son menores a las minúsculas, ahora continuemos con el tema de longitud, voy a agregar más celdas con la letra B, ¿Qué quiere decir cuando hablamos de longitud en un string? Decíamos que un string viene dado por la constitución de diferentes caracteres y que cada elemento de un string es representado por un carácter, veamos un ejemplo, definimos la variable A y en él guardamos un string cualquiera por ejemplo ola, si ejecutamos el valor de ola que es un string se guarda en la variable A y ahora quiero conocer, quiero saber cuál es la longitud, es decir cuál es la cantidad de caracteres que posee mi string si nosotros contamos y tratamos de resolver por nosotros mismos esta cuestión diríamos que el string ola contiene cuatro caracteres, lo que quiere decir que la longitud tendría que ser 4, ahora hay una función en python que podríamos usar para obtener esa respuesta de una manera inmediata, esa función es len, entonces como lo usamos simplemente escribimos len, abrimos paréntesis y dentro del paréntesis colocamos el nombre de la variable, en este caso es a, ejecutamos y efectivamente como habíamos dicho en nuestra hipótesis que la longitud de ola debería ser 4, efectivamente python dice que es 4 y qué pasa si aplicamos la función len a un string que tiene un espacio, es decir un espacio en blanco, decíamos que el espacio en blanco también es un carácter especial, veámoslo de manera gráfica, definamos una variable b y escribimos un string por ejemplo un espacio, esta es nuestra string, ahora ejecuto y ahora quiero conocer la longitud de mi string, decíamos que simplemente escribíamos la palabra len, abrimos paréntesis y dentro del colocamos el nombre de la variable que queremos conocer su longitud, ponemos b, ejecutamos y obtenemos el valor de 10, notar que contó 10 caracteres, recuerden que el espacio en blanco es considerado un carácter, bueno veámoslo por nosotros mismos y contemos cada carácter que posee este string, vamos 1, 2, todavía no voy a encontrar el espacio en blanco, 3, 4, 5, 6, 7, 8, 9, dije que todavía no voy a contar el espacio en blanco pero ahora lo contamos y efectivamente sería un 10, por otro lado podemos reemplazar algún string que quizás hayamos escrito mal, para ello podemos hacer uso del método replace, veamos algunos ejemplos agregaré algunas cendas, definamos una variable por ejemplo la palabra mal escrita, un poco largo el nombre pero bueno, entonces dentro de esta variable quiero añadirle una palabra cualquiera mal escrita por ejemplo universidad y aquí observamos que la palabra universidad no se escribe con velarga sino con v, vale, entonces ejecuto para guardar la palabra en la variable palabra mal escrita y podemos hacer uso del método replace como lo hacemos digamos que quiero añadir el resultado en otra variable, palabra bien escrita por ejemplo y lo que vamos a hacer es llamar a la variable que contiene el string el cual quiero reemplazar algún elemento, llamo a la variable en este caso es palabra mal escrita, añado un punto, seguidamente coloco el método replace abro paréntesis, y el método replace me pide dos argumentos, el primero es el elemento que va a ser reemplazado, el segundo elemento que me pide es un string el cual va a reemplazar el elemento que queremos que se vaya, entonces escribimos primero el elemento que queremos reemplazar en este caso es la velarga seguidamente hacemos una coma y ahora colocamos algún elemento que quisiéramos que tome su lugar, en este caso es la v, y en esta misma celda vamos a imprimir el valor de la variable palabra bien escrita ejecutamos para ver qué pasa y efectivamente obtenemos ahora el string bien escrito, también podemos reemplazar el espacio en blanco, recordemos que el espacio en blanco es considerado también un carácter, por ejemplo creamos una variable llamada a y en él vamos a escribir a propósito una palabra que esté separada vale por un espacio por ejemplo ola y aquí ponemos un espacio ahora usamos el método replace lo quiero guardar en la misma variable esta vez es decir en la variable a y qué es lo que tengo que hacer pues llamo al string que en este caso el string está contenido en la variable a hago un punto coloco replace abro paréntesis y le tengo que pasar primero el carácter que quiero que se reemplace y en segundo lugar le tengo que pasar el string que quiero que tome su lugar en este caso quiero reemplazar el espacio en blanco entonces seguidamente añado una coma y luego añado un nada si te das cuenta estoy abriendo un string pero dentro de él no hay ningún espacio en blanco es decir está vacío entonces en esta misma celda vamos a imprimir el nuevo valor de la variable a ejecutamos y vemos qué pasa bien obtenemos correctamente la palabra ola sin ningún espacio porque porque reemplazamos el espacio en blanco por un vacío ahora veamos cómo cambiar el texto a mayúsculas o minúsculas vale tomemos un ejemplo un caso donde utilizaremos el método lau para obtener todo el string y minúsculas vale voy a crear más celdas creamos la variable a por ejemplo y en él voy a guardar el string la casa notar que la está en mayúsculas vale y quiero que el texto esté minúsculas para ello a ver ejecutamos para guardar el string dentro de la variable a ahora creo la variable y en él voy a guardar el resultado de mi método la como utilizo el método lo primero llamo a la variable que contiene el string que quiero transformar en este caso llamo a la variable a hacemos a seguidamente colocamos un punto y luego llamamos al método la abrimos para éncesis y no le pasamos ningún argumento dentro de él no hace falta vale y en esta misma celda quiero imprimir el valor que guarda mi variable de vale ejecutamos y efectivamente obtenemos todo el texto que contiene la variable a en minúsculas la operación contraria al agua donde obtenemos la conversión a mayúsculas el asilente supongamos que quiero guardar en esta variable c el resultado del método que ahora vamos a ver y para ello utilizaremos esta misma variable a que contiene la casa entonces voy a llamar a la variable en este caso esa seguido de un punto luego coloco el método a pero de esta forma vale ahora aparente 6 y no voy a colocar tampoco ningún argumento no hace falta en esta misma celda voy a imprimir lo que ahora contiene mi variable c hago prínce ejecuto y efectivamente obtengo todo el texto en mayúsculas y qué pasa si queremos que un string tenga solo la primera letra de cada palabra que contenga en mayúsculas veamos cómo hacerlo voy a agregar más celdas y ahora creamos una variable por ejemplo de y en él voy a escribir por ejemplo hola buenas tardes ahora voy a crear la variable donde quiero guardar el resultado del método que ahora vamos a ver ahora quiero llamar primero como habíamos visto anteriormente a la variable que contiene el string que quiero transformar en este caso es de vale hacemos un punto vale y luego colocamos title abrimos paréntesis tampoco le pasamos ningún argumento no hace falta y en esta celda también quiero imprimir el valor que contiene la variable que es donde vamos a guardar el resultado del uso de esta función entonces hago prínce ejecutamos y como podemos observar cada palabra de mi texto vale empieza con una mayúscula y en el caso que queramos solo solo la primera letra de todo el texto que tengamos en un string este en mayúsculas utilizamos otro método por ejemplo creo la variable f donde quiero guardar el resultado del método que utilizaré ahora y quiero utilizar la misma la misma variable de que contiene hola buenas tardes vale entonces quiero transformar este string de hago un punto y luego coloco capital y se habrá paréntesis tampoco le paso ningún argumento no hace falta y voy a utilizar el o la función prínce prínce para visualizar el resultado que guarda la variable f ejecutamos y efectivamente vale sólo la primera letra de todo el texto lo podemos obtener en mayúscula por último veamos el operador y borremos esta celda aquí voy a agregar más celdas el operador y nos ayuda a probar si una cadena es una subcadena de otra vale nos da como resultado un buliano por ejemplo creamos una variable que se llama y en el voy a guardar un string voy a escribir manzana ejecutamos para guardar el valor es sin dentro de mi variable y le quiero preguntar a python lo siguiente quiero validar si es que la letra m o el elemento m está incluido dentro de mi string manzana está guardado a su vez en mi variable a y como le pregunto hago lo siguiente abro comillas coloco la letra m y utilizo el operador y seguido tengo que colocar el string vale en este caso está guardado en mi variable a que pasa si ejecutamos efectivamente obtenemos un valor buliano como resultado y en este caso es tru lo que quiere decir que m si está incluido dentro de mi string manzana que a su vez está guardado en la variable a veamos otro ejemplo que pasa si quiero preguntar si por ejemplo b letra b está incluido dentro de mi string veamos que nos da como resultado python ejecutamos y efectivamente b no está incluido dentro del string manzana vale por eso que python dice que es falso vale hagamos otro ejemplo creamos esta variable y en él voy a escribir por ejemplo python y programación vale ejecutamos para guardar este string en mi variable b y ahora quiero preguntarle a python si la palabra programación está incluido dentro de mi string python y programación vamos a validar esto entonces abrimos comillas dentro del escribimos la palabra programación seguidamente colocamos el operador y y finalmente el string que quiero que reconozca vamos a ver qué pasa cuando ejecutamos ejecutamos y efectivamente obtenemos el valor buliano true porque la palabra programación está incluido dentro del string que está guardado en mi variable b vale ahora que pasa si colocamos otro string por ejemplo agua quiero preguntar la python si agua está incluido dentro de mi string python y programación ahora lo siguiente agua operador y le paso el string que quiero que valide en este caso es b ejecutamos y efectivamente agua no está incluido dentro de mi string que está guardado en la variable b