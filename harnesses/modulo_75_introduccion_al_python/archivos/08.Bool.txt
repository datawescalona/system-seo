El siguiente tipo de dato que veremos es BUL, que representa valores de lógica binaria, es decir, verdadero o falso, y lo usaremos cuando la respuesta se resuelve con un sí o con un no. Una variable buliana es una variable que solo puede tomar dos posibles valores, true o false. Veamos un ejemplo, creamos la variable a y le asignamos el valor de true. Lo ejecutamos para guardar el valor y ahora quiero preguntarle a Python qué tipo de dato guarda mi variable a. Hacemos uso de la función type. Abrimos paréntesis y colocamos el nombre de la variable. Ejecutamos y efectivamente obtenemos un tipo de dato bul. Ahora quiero crear esta variable b y le asigno el valor de false. De igual forma lo ejecutamos y también quiero preguntarle a Python qué tipo de dato guarda mi variable b. Hago type b y ejecutamos y efectivamente también obtenemos un tipo de dato bul. Ahora creo esta variable c y le quiero asignar nuevamente true. Notar que en esta ocasión estoy escribiendo true de una manera distinta. Veamos qué pasa al momento de ejecutar. Lo ejecutamos y obtenemos un error. Tengamos mucho cuidado al escribir un true y un false. La primera letra siempre va en mayúscula. Si no lo escribimos de manera correcta el intérprete de Python no lo reconocerá y tendremos un error de no definición. Sigamos. Los valores bulianos son además el resultado de expresiones que utilizan operadores relacionales. Los operadores relacionales evaluan comparaciones entre valores. Por ejemplo tenemos el operador igual. Hagamos algunos ejemplos. Nos situamos en esta celda y vamos a crear la variable a y le asignamos el número entero 4. En esta misma celda creamos la variable b y le asignamos el entero 2. Muy bien ejecutamos para guardar los valores y ahora quiero preguntar si mi variable a es igual a b. Veamos qué respuesta nos da Python. Ejecutamos y obtenemos un valor de tipo bul. En esta ocasión el valor es falso. ¿Por qué? Porque le estoy preguntando a Python si a es igual a b. Mi variable a guarda el número entero 4. Mi variable b guarda el número entero 2. Y cuando les pregunto Python si 4 es igual a 2, la respuesta efectivamente es falso. Ahora qué pasa si quiero igualar o quiero hacer una operación relacionar decir si 2 es igual a 2. Epotéticamente deberíamos obtener un valor de true. Veamos qué pasa lo ejecutamos y efectivamente obtenemos el valor de true. ¿Por qué? Porque 2 si es igual a 2. Ahora hagamos otro ejemplo para que esto quede claro. Por ejemplo creo una variable a. Bueno hay que llamarlo c y le igualamos a una operación matemática. Por ejemplo que sea 1 más 2. Ejecutamos. Ahora creamos otra variable que se llama de por ejemplo y le quiero igualar por ejemplo el número entero 3. Ejecutamos y ahora quiero preguntarle a Python nuevamente si mi variable c es igual a de lo ejecutamos y efectivamente obtenemos el valor de true. ¿Por qué? Porque mi variable c guarda el resultado de esta operación el cual es 1 más 2 igual a 3. Por lo que mi variable c guarda el valor de 3 ¿vale? y mi variable de guarda también el valor de 3. Por eso que al preguntar a Python si c es igual a de es decir si 3 es igual a 3 obtenemos efectivamente el valor de true. Muy bien veamos otro ejemplo. Ahora voy a hacer uso de la función type. Agot type. Abro el paréntesis y en esta ocasión usaré una variable ¿vale? Tomaré la variable de que contiene el valor de 3. Usaré el operador relacionado igual ¿vale? y le preguntaré a Python si esto es un entero. Ejecutamos y efectivamente obtenemos el valor de true ¿Por qué? Porque el tipo de datos de la variable de que contiene el entero 3 es igual a un entero. ¿Quieres validarlo? Lo validamos ¿vale? Hacemos type de ejecutamos y efectivamente el valor resultante es de tipo de dato entero. Bien por otro lado tenemos el operador no igual. Que valúas y los valores son distintos ¿vale? Por ejemplo hagamos esta operación 5 es diferente a 3 ¿vale? Veamos que nos arroja Python cuando lo ejecutamos, lo ejecutamos y obtenemos efectivamente un valor booleano true ¿Por qué? Porque 5 es diferente a 3 ¿vale? Por eso que nos dice verdadero. Muy bien por ejemplo hagamos otra operación 5 es diferente a 5 esa es la pregunta ejecutamos y efectivamente obtenemos un valor de false porque 5 es igual a 5. Hagamos otro ejemplo, definamos una variable que se llama a y le vamos a igualar una operación matemática. Por ejemplo colocamos 5 más 5. Ejecutamos para guardar el valor de mi operación matemática en la variable a ¿vale? Hago uso de la función print a para poder visualizar el valor que guarda mi variable a. Ejecutamos y obtenemos el valor de 10 ¿vale? Que es la suma de 5 más 5. Bien creamos otra variable le llamo b y le vamos a igualar otra operación matemática por ejemplo 3 más 7. Bien ejecutamos y de igual forma quiero visualizar el valor que guarda mi variable b por ello hago uso de la función print ejecutamos y obtenemos el valor de 10 ¿Por qué? Porque 3 más 7 es igual a 10. Ahora quiero preguntar la Python si mi variable a es distinto a b. Ejecutamos y obtenemos el valor booleano de false ¿Por qué? Porque 10 es igual a 10. Mi variable a guarda el valor de 10. Mi variable b guarda el valor de 10. Bien ahora tenemos el operador menor que valúa si el valor del lado izquierdo es menor que el valor del lado derecho ¿vale? Borremos estas celdas y hagamos un ejemplo. Colocamos el valor de 5 preguntamos si es menor a 4. Ejecutamos y obtenemos un valor booleano false ¿Por qué? Porque 5 no es menor a 4. Hacemos un ejemplo, definamos una variable, le llamamos a y la asignamos una operación matemática por ejemplo 5 más 4 ¿vale? Ejecutamos quiero visualizar el valor que guarda mi variable a usando la función print a o print a. Ejecutamos y obtenemos el valor de 9 porque 5 más 4 es igual a 9. Quiero crear otra variable que se llama b igualo por ejemplo 5 más 6 ¿vale? Ejecutamos para guardar el valor y quiero imprimir b para visualizar el valor que guarda. Ejecutamos y obtenemos el valor de 11. Ahora quiero preguntar la Python si a es menor a b. Ejecutamos y obtenemos el valor booleano de true ¿Por qué? Porque mi variable a guardaba el valor de 9 y mi variable b guarda el valor de 11 y cuando le preguntamos a Python si 9 es menor a 11 la respuesta efectivamente es true y en su contraparte también tenemos el operador mayor. ¿Qué valor hace el valor del lado izquierdo es mayor que el valor del lado derecho? Bien hagamos un ejemplo la misma operación utilizando los mismos números pero en esta ocasión el operador mayor. Preguntamos a Python si 5 es mayor a 4. Ejecutamos y en esta ocasión la respuesta es true porque verdaderamente 5 es mayor a 4. Hagamos un ejemplo similar como comicimos con el operador menor por ejemplo definamos una variable a hacemos 5 por 5 y en la misma celda vamos a crear una variable b y vamos a guardar 5 por 6. Ejecutamos para guardar los valores en las variables a y b y ahora quiero preguntarle a Python si a es mayor a b. Ejecutamos y la respuesta es false ¿Por qué? Porque mi variable a guarda el valor de esta operación matemática que en teoría sería 25 porque 5 por 5 es igual a 25 y mi variable b en teoría tendría que guardar 5 por 6 y es 30. Por eso que cuando la preguntamos a Python si 25 es mayor a 30 la respuesta es false efectivamente. Bien ahora continuamos con el operador menor o igual este operador evalúa si el valor del lado izquierdo es menor o igual que el valor del lado derecho. Hagamos el mismo ejemplo esta vez usando el operador menor o igual preguntamos a Python si 5 es menor o igual a 4 veamos la respuesta obtenemos el valor buliano de false porque efectivamente 5 no es menor o igual a 4. Bien hagamos un ejemplo con variables definamos a es igual a por ejemplo una operación 5 por 7 ¿vale? y en la misma celda también creamos la variable b y le igualamos también a otra operación por ejemplo 5 por 8 ejecutamos para guardar los valores en nuestras variables y ahora quiero preguntar a Python si a es menor o igual a b ejecutamos y la respuesta efectivamente es true ¿Por qué? Porque la variable a guarda el valor de 5 por 7 y 5 por 7 es igual a 35 y variable b guarda el valor de 40 porque 5 por 8 es igual a 40 y por eso cuando le preguntamos a Python si 35 es menor o igual a 40 la respuesta efectivamente es true y por último veamos el operador mayor o igual ¿Qué valúa si el valor del lado izquierdo es mayor o igual que el valor del lado derecho ¿vale? pues hagamos la misma operación pero esta vez usando el operador mayor o igual mayor o igual a 4 ¿vale? y veamos qué pasa cuando lo ejecutamos en esta ocasión obtenemos un valor buliano de true ¿Por qué? porque efectivamente 5 es mayor o igual a 4 ¿Bien? ahora hagamos un ejemplo utilizando variables como en este como en esta celda por ejemplo hacemos a le igualamos a una operación matemática 5 por 10 en la misma celda creamos una variable 4 por 5 ¿vale? y en la misma celda vamos a preguntarle a Python si a es mayor o igual a b ¿vale? ejecutamos y obtenemos como respuesta el valor buliano de true ¿Por qué? porque mi variable a guarda el valor de 50 5 por 10 es igual a 50 y mi variable b guarda el valor de 20 ¿vale? 4 por 5 es igual a 20 por eso cuando la preguntamos a Python si 50 es mayor o igual a 20 la respuesta efectivamente es true