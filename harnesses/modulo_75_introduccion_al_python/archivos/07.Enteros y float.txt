Bueno, ya tenemos todo preparado para iniciar a ejecutar código en nuestro entorno de trabajo y junto a él iré explicando a detalle cada uno de los temas de este curso. Antes que nada te sugiero practicar una y otra vez los ejercicios que hagamos juntos. Recuerda que el arte del análisis de datos requiere de poner las manos a la masa. En primer lugar debemos ser capaces de responder a la pregunta qué es una variable. Las variables almacenan valores y tal como dice su nombre puede cambiar de valor en el tiempo. Digamos que es la forma más simple del almacenamiento y que son reutilizables para diferentes cálculos. Esto lo veremos en un momento. En Python podemos hacer uso de tipos de datos enteros y float. Las operaciones típicas de suma, resta, multiplicación, división o potencia están disponibles. El tipo de dato float hace referencia a un número con decimales. Puede ser este positivo o negativo, es decir estamos hablando de números reales. Podemos usar Python para cálculos muy simples, como si estuviésemos usando una calculadora. Por ejemplo si nos situamos en esta celda y hacemos una simple suma, hacemos 5 más 4, ejecutamos, obtenemos el número 9. Si queremos hacer una resta, por ejemplo 10 menos 8 y ejecutamos obtenemos el valor de 2. Y si queremos hacer una simple multiplicación, por ejemplo 5 por 5, ejecutamos y obtenemos el valor de 25. Ahora definamos una variable. Las variables pueden tener un nombre cualquiera. Para este ejemplo vamos a llamarle result. Y lo vamos a igualar a una operación matemática. Por ejemplo hacemos 2 elevado a la potencia de 3 más 2 por 2 y más 4. Lo que estamos haciendo en esta línea es igualar el valor resultante de esta operación matemática a la variable result. Lo ejecutamos y para poder visualizar el valor que guarda la variable result, hacemos uso de la función print. Hacemos print, abrimos paréntesis y colocamos dentro de él el nombre de la variable. Ejecutamos y obtenemos el valor de 16. El valor de 16 es el resultado de la operación matemática que igualamos a la variable result. Aquí una cosa importante, la prioridad de operación de Python viene respetando en primer lugar la potencia seguido de la multiplicación y finalmente la suma. Pero ¿qué pasa si queremos cambiar las prioridades en la ejecución? Pues hacemos uso de paréntesis. Por ejemplo si quiero que primero se ejecute la suma, tendría que colocar el paréntesis fuera de la operación. Veamos cómo hacerlo. Definamos una variable. Decíamos que una variable puede tener un nombre cualquiera, incluso puedo usar el mismo nombre de una variable que ya definí anteriormente. Lo llamamos result. Y lo igualo a una operación matemática. En este caso usaré la misma operación para validar las diferencias entre usar paréntesis y no. Era 2 elevado a la potencia de 3, más 2 por 2 y más 4. Decíamos que para el ejemplo queremos que se ejecute primero la suma. Hacemos uso del paréntesis y repito nuevamente lo que vamos a hacer en esta línea para que quede claro. Lo que vamos a hacer es guardar el valor resultante de esta operación matemática en mi variable result. Lo ejecutamos para guardarlo y para visualizar el valor que guarda ahora mi variable result hacemos uso de la función print. Abrimos paréntesis y colocamos el nombre de la variable. Ejecutamos y ahora obtenemos el valor de 20. Para validarlo dicho ahora resolvamos nosotros mismos cada operación matemática. Vamos por la primera. Decíamos que Python, la primera operación que ejecutaría sería la potencia, luego la multiplicación y por último la suma. Veamos 2 elevado a la potencia de 3 es igual a 8 más 2 por 2 es igual a 4 y más 4. 8 más 4 más 4 es igual a 16 efectivamente como obtuvimos en esta celda. Ahora resolvamos esta segunda operación. Para esta segunda operación decíamos que Python primero resolvería la operación que está dentro del paréntesis. Esto sería 2 elevado a la potencia de 3 que todavía no se ejecutaría, más 2 por y aquí ponemos el resultado que está dentro del paréntesis. 2 más 4 es igual a 6. Muy bien, esto es igual a 2 elevado a la potencia de 3 es igual a 8 más 2 por 6 igual a 12 y 8 más 12 es igual a 20. Efectivamente como obtuvimos en esta celda. Como has podido notar la variable result ha cambiado de valor. Primero obtuvimos el valor de 16 y luego el valor de 20. Esto deja claro que las variables pueden cambiar según las necesidades de mi código. Pueden variar el valor que contienen de acuerdo a los requerimientos de mi programa. Ahora veamos cómo podemos reutilizar variables con el objetivo de hacer uso del valor que contienen. Por ejemplo, defino una variable, la llamo x igual a y la signo un entero 10 por ejemplo. Ejecuto. Ahora quiero definir otra variable llamada y y la signo el valor 8 y ejecuto. Ahora quiero crear una variable, la llamaré r, que contendrá una operación matemática. Esta vez no usaré números matemáticos, sino haré uso de las variables que he definido antiduramente. Por ejemplo quiero una resta, quiero x menos y. ¿Qué es lo que vamos a hacer en esta línea? Vamos a guardar el valor resultante de esta operación matemática en la variable r. Vamos a ejecutar para ver qué obtenemos. Ejecutamos y ahora quiero visualizar el valor que contiene mi variable r. Usamos print, abrimos paréntesis y colocamos el nombre de la variable. Ejecutamos y obtenemos el valor de 2. ¿Qué pasó? ¿Por qué obtenimos el valor de 2? Pues mi variable r contenía el resultado de esta operación matemática. Mi variable x es igual a 10 y mi variable y es igual a 8. 10 menos 8 es igual a 2 efectivamente. ¿Y qué pasa? Si ahora quiero hacer uso del valor de mi variable r, por ejemplo, creo una variable r2 igual a r y vamos a sumarle en esta ocasión un número por ejemplo 10, ¿vale? Directamente ejecutamos. Quiero averiguar cuál es el valor que contiene mi variable r2. Hago uso de la función print, abro paréntesis y coloco el nombre de la variable. Ejecutamos y efectivamente tenemos 12. ¿Por qué? Porque mi variable r tenía el valor de 2 y al sumarle 10 obtenemos 12. Otra operación que podemos aplicar es la división estándar, que es representado por este símbolo. Barra inclinada, ¿vale? Es la división que todos conocemos. El resultado de esta operación da lugar a un número decimal, es decir, a un número flowad. Y en Python también existe una división entera que es representada por este símbolo, que es la doble barra inclinada. Y el resultado de esta operación da como resultado un valor de tipo de dato entero. Veamos unos ejemplos para quedar claros con este concepto. Definamos una variable, la quiero llamar x y lo igualo a una operación matemática. Por ejemplo, abro paréntesis, hago 4 por 9 y luego quiero usar la división estándar. Usamos este símbolo entre 6. Ejecutamos para guardar el valor resultante de esta operación matemática en la variable x. Ahora quiero averiguar el valor que guarda mi variable x. Usamos la función print, print x y ejecutamos. Obtenemos el valor de 6, pero aquí tenemos algo curioso, algo importante. El valor de 6 lleva un punto, vale. Este punto representa la coma decimal, lo que quiere decir que estamos frente a un valor con tipo de datos flowad. En Python podemos averiguar qué tipo de dato representa algún valor. Por ejemplo, puede hacer una variable o directamente un número. Lo hacemos de esta forma, utilizamos la función type, abrimos paréntesis y en este caso colocaré la variable x que contiene el valor 6.0, hacemos type x y ejecutamos. Efectivamente, obtenemos el tipo de dato flowad. Ahora quiero hacer la misma operación y lo quiero asignar a una variable, pero esta vez quiero usar la división entera. A ver, por ejemplo, definamos una variable z por ejemplo y lo igualo a la misma operación. Esto era 4 por 9, pero esta vez usaré la división entera entre 6. Lo ejecutamos, hacemos print z para averiguar el valor que guarda, lo ejecutamos y esta vez obtenemos el 6 de la misma forma, pero con una diferencia esta vez obtenemos el 6 sin el punto 0, es decir, sin la parte decimal. Ahora quiero averiguar qué tipo de dato contiene mi variable z. Hacemos type, abrimos paréntesis y colocamos el nombre de la variable. Lo ejecutamos y efectivamente obtenemos un entero, por lo que podemos decir que al usar la operación de división estándar obtenemos siempre un flowad y al usar la operación de división entera obtenemos siempre un entero. Hagamos un segundo ejemplo, por ejemplo, quiero llamarle a mi variable e, le quiero igualar una operación matemática, por ejemplo esta vez 4 por 5, uso la división estándar entre 6, lo ejecuto, quiero imprimir el valor de e, lo ejecuto y obtengo 3.33 y más. Aquí vemos el punto, vale, lo que significa o lo que representa mejor dicho a la coma decimal. Hacemos type e para averiguar el tipo de dato que hipotéticamente debería ser un flowad, vale, o un decimal. Lo ejecutamos y obtenemos efectivamente el tipo de dato flowad. Ahora quiero crear la misma operación, el mismo ejercicio que hicimos antes, vale, pero esta vez usando la división entera. Creo una variable, por ejemplo b, igual a 4 por 5, quiero usar la división entera entre 6, lo ejecutamos, hacemos print b, lo ejecutamos, obtenemos en esta ocasión el 3. Ahora quiero averiguar el tipo de dato de b, hacemos type b, lo ejecutamos y efectivamente obtenemos un entero, vale, con este ejemplo queda claro lo que habíamos dicho, que la división entera nos da un valor entero y la división estándar nos da un valor flowad. Como hemos visto las variables en Python se pueden introducir sin declarar el tipo de datos, pero es posible averiguar el tipo de datos que tiene una variable gracias a la función type. ¿Y qué pasa si queremos ahora convertir un valor flowad a un entero? Por ejemplo, defino una variable a y lo igualo directamente a un flowad, 2.6 por ejemplo, lo ejecuto y quiero averiguar el tipo de dato o mejor dicho validar, porque le estoy asignando un flowad, un 2.6, vale, validamos usando type y lo ejecutamos, efectivamente obtenemos un flowad. Ahora para convertir un flowad a un entero tenemos muchas formas, una de ellas es usando una función llamada entero, veamos cómo hacerlo, asignamos una variable por ejemplo b, le asignamos usando la función entero, abrimos paréntesis y dentro del paréntesis puedo colocar directamente un valor numérico o una variable, en este caso colocaré la variable que contiene el flowad 2.6, coloco a, lo ejecuto y quiero ver qué valor guarda ahora la variable b, hago print b, lo ejecuto y vemos que obtenemos el valor de 2, quiero averiguar el tipo de datos de mi variable b, hago type b, lo ejecuto y efectivamente es un entero, usamos la función entero y le pasamos como argumento el valor de a, que a su vez contiene un flowad 2.6, notar que esta función redondea al entero inferior y qué pasa si queremos redondear al entero superior, podemos hacer uso de otra función que nos otorga python, en este caso podemos utilizar la función round, por ejemplo creo una variable a y le voy asignar el mismo flowad 2.6, lo ejecuto, averiguo nuevamente con la función type, qué tipo de dato guarda mi variable a, hago type a, lo ejecuto, efectivamente obtengo un tipo de dato flowad, vale 2.6, muy bien, ahora cómo utilizo la función round para redondear al entero superior, vale de esta forma, por ejemplo creo una variable c, lo igual lo utilizo la función round, abro paréntesis y de igual forma le puedo pasar directamente un valor numérico o una variable que almacena un valor numérico, en este caso le pasaré la variable que guarda el 2.6, le paso a ejecuto y quiero visualizar el valor que guarda mi variable c, hago un print c ejecuto y obtenemos el valor de 3, efectivamente lo redondió al entero superior, pero además de esto lo convierte a un entero como hemos dicho antideormente y como validamos el tipo de dato que esconde mi valor c utilizando la función type, hacemos type c y lo ejecutamos y efectivamente obtenemos el tipo de dato entero, ahora miremos algo curioso para entender aún más las variables, haremos los mismos ejercicios que hicimos hace un momento pero haciendo algo distinto, a ver si lo pueden ver, creamos la variable b y la asignamos el flowad nuevamente el 2.6, ejecutamos, quiero conocer el tipo de datos que esconde mi variable type b, lo ejecutamos y obtenemos el tipo de dato flowad, ahora quiero redondiarlo haciendo uso de la función round, abro paréntesis y le paso mi variable b, hago round b, lo ejecuto y efectivamente obtenemos el valor redondeado de mi flowad 2.6 al entero superior, en este caso efectivamente obtenemos el número 3, ahora quiero otra vez validar el tipo de dato que tiene mi variable b, hacemos uso de type nuevamente, abrimos paréntesis, colocamos el nombre de la variable y lo ejecutamos, notar que obtenemos nuevamente el tipo de dato flowad a pesar de haberlo redondeado y obtener un valor de 3 y por qué sucede esto, pues al aplicar la función round no lo guardamos en ninguna parte, es decir no asignamos el valor resultante al hacer uso de esta función en ninguna variable, por eso que cuando le pregunto a python por el tipo de dato de la variable b, me sigue diciendo que es flowad, porque aún sigue guardando el flowad 2.6, que es lo que tengo que hacer, tengo que guardarlo, lo podría guardar en otra variable, pero si es que quiero continuar una secuencia de operaciones en un programa, lo podría guardar en la misma variable, es decir en la misma variable b, hacemos lo siguiente, hacemos b igual a round b, lo ejecutamos y ahora preguntamos nuevamente por el tipo de dato que guarda la variable b, hacemos type b, lo ejecutamos y ahora sí me guarda el tipo de dato entero, ¿por qué? porque esta vez si lo asignamos a una variable en este caso, la misma variable b, ahora quiero imprimir el valor que guarda mi variable b y deberíamos de obtener el número 3, lo ejecutamos y efectivamente obtenemos el tipo de dato entero 3, también podemos aplicar otras funciones matemáticas haciendo uso de otras librerías, por ejemplo el módulo mat, para poder hacer uso de sus métodos usamos la palabra reservada import y colocamos el nombre del módulo que queremos llamar, en este caso es mat, lo ejecutamos y aquí una observación, no te preocupes si aún no manejas este tema, el llamado de módulos lo veremos a detalle más adelante en el curso, esto solo tomalo como un ejemplo, continuando con el hilo de redondido de números, usaremos el método flor que redondea al entero inferior, sin importar la proximidad de la parte decimal del dato numérico, hagamos algunos ejemplos para que esto se entienda, creamos la variable a y la asignamos el float 3.7, en la misma celda creamos la variable b y la asignamos el float 3.2, creamos la variable c y la asignamos el float 3.5, muy bien ejecutamos para que se guarden los valores en mis variables y ahora lo que vamos a hacer es utilizar la función flor del módulo mat, de la siguiente forma, directamente utilizamos un print para visualizar el valor que va a arrojar el uso de la función, entonces hacemos un print, abrimos un paréntesis y directamente colocamos el módulo mat punto, la función es flor, abrimos paréntesis y dentro del paréntesis colocamos la variable que queremos redondear, colocamos a y ejecutamos, obtenemos el número 3, hacemos lo mismo con b, hacemos print, el módulo mat, la función flor, abrimos paréntesis y colocamos la variable b, ejecutamos y obtenemos el valor de 3, hacemos lo mismo pero ahora con la variable c, hacemos print, abrimos paréntesis, el módulo mat, la función flor y abrimos paréntesis y colocamos la variable c, ejecutamos y obtenemos también el número entero 3, como habíamos dicho hace un momento la función o el método flor redondea al entero inferior, cualquier tipo de dato float, sin importar la proximidad de la parte decimal del dato numérico, en este caso 3.7, en este caso 3.2 y en este caso 3.5, no importa el valor de la parte decimal siempre va a redondear al entero inferior, en este caso es 3 y en su contraparte usamos el método sale que redondea al entero superior, también sin importar la proximidad de la parte decimal del dato numérico, hagamos un mismo ejemplo, vamos a usar los mismos valores, le asignamos el 3.7, en la misma celda creamos la variable b, 3.2, creamos la variable c y ponemos 3.5, ejecutamos y en este caso vamos a usar el método sale, hacemos lo mismo, hacemos print, llamamos al módulo mat, colocamos el método sale, abrimos paréntesis y le pasamos como parámetro la variable a, ejecutamos y obtenemos el número 4, hacemos nuevamente print, llamamos al módulo mat, colocamos el método sale, abrimos paréntesis y esta vez colocamos la variable b, ejecutamos y obtenemos el número 4, hacemos lo mismo con la variable c, hacemos print, llamamos al módulo mat, ponemos el método sale, abrimos paréntesis y le pasamos la variable c, ejecutamos y nuevamente obtenemos el número 4, como hemos dicho hace un momento el método sale redondea al entero superior cualquier valor tipo flowad, sin importar la proximidad de la parte decimal de la turmédico, como veíamos 3.7, 3.2, 3.5 no importa el valor de la parte decimal, siempre va a redondear al entero superior, por último veamos qué tipo de dato obtenemos como resultado, a multiplicar un entero por un decimal, el resultado va a ser un flowad siempre, automáticamente, veamos un ejemplo, creamos una variable, le llamamos zeta y le igualamos a una operación matemática, por ejemplo un flowad 2.6, lo multiplicamos por 5, lo ejecutamos, lo que vamos a hacer en esta línea y lo repito nuevamente para que quede claro, vamos a guardar esta operación matemática, su resultante en la variable zeta, ahora quiero visualizar el valor que guarda mi variable zeta utilizando la función print, hago print zeta y lo ejecuto, obtenemos el valor de 13.0, recordemos que el punto representa la coma decimal, lo que quiere decir que hipotéticamente estamos frente a un valor tipo flowad, y como validamos esta hipótesis por así decirlo haciendo uso de la función type, hacemos type zeta, lo ejecutamos y efectivamente obtenemos un valor flowad.