Muy bien, ahora estudiaremos otro tipo de datos, en esta ocasión vamos a ver los strings. Los strings representan secuencias o cadenas de caracteres, básicamente todo lo que se puede escribir como texto puede ser un string. Hay una regla importante que tenemos que seguir en Python, se pueden poner entre comillas dobles o simples, lo importante es abrir y cerrar con el mismo hipode comillas. Veamos un ejemplo, nos situamos en esta celda y creamos la variable A. Voy a crear un string, voy a utilizar las comillas dobles y voy a colocar un string cualquiera, por ejemplo, Hola. Ejecuto para guardar el valor, quiero validar que efectivamente se haya guardado el valor Hola en mi variable A, para eso hago uso de la función print, print a ejecuto y efectivamente tengo un Hola dentro de mi variable A, vale. Quiero validar el tipo de datos que contiene mi variable A utilizando la función type, hago type A, ejecuto y efectivamente es un string, vale, hasta aquí no ningún error. Ahora hagamos lo mismo pero esta vez quiero crear un string utilizando comillas simples, vale, para eso creo la variable B, abro comillas simples, escribo el mismo string, Hola. En la misma celda vamos a imprimir el valor que guarda mi variable B y también vamos a ver qué tipo de dato guarda mi variable, hago type B, ejecuto y obtenemos efectivamente que el valor que guarda mi variable B es un string, bien, hasta aquí no hay ningún problema, vale. Ahora veamos qué pasa, si escribo nuevamente el texto Hola, pero en esta ocasión quiero abrir el string con comillas simples y lo cierro con comillas dobles, qué pasa, lo ejecutamos y obtenemos un error, esto por lo que decíamos al inicio, no podemos iniciar un string y cerrarlo con diferentes tipos de comillas, tengo que abrir con el mismo tipo de comillas. Ahora qué pasa si abro con la comilla doble y escribo nuevamente Hola y cierro en esta ocasión con la comilla simple, pues al momento de ejecutar también tenemos un error, no podemos escribir un string con diferentes tipos de comillas, vale, que quede claro porque si no vamos a obtener errores. Ahora es normal que en estos momentos te estés preguntando si es lo mismo escribir un string con comillas dobles o con comillas simples, pues la respuesta es que da lo mismo, vale. Un string no se va a diferenciar de otro string por escribir o por definirlo con diferentes tipos de comillas, vale. Para eso vamos a hacer uso y para que también Python lo responda, vamos a hacer uso del operador igual, ok, lo que vamos a igualar es estas dos variables que ya habíamos creado con anterioridad, por ejemplo la variable A que guarda el string Hola con comillas dobles y la variable B que guarda el mismo string o digamos el mismo texto, pero con comillas simples, vale. Entonces hagamos una igualdad para ver qué Python es lo que responde, por ejemplo, A es igual a B, ejecutamos y efectivamente tenemos un valor booleano 2, lo que quiere decir que da igual, si lo escribes con variables dobles y lo escribes con variables simples, vale. Lo importante como dije en un inicio y para que quede claro lo repito, lo importante es abrir y cerrar con el mismo tipo de comillas, siempre, vale. Bien, ahora te voy a enseñar las operaciones más frecuentes que hacemos con los strings, una de ellas es la concatenación, vale. Es posible unir dos strings mediante el símbolo más, aquí cabe resaltar que el símbolo más no hace referencia a un símbolo arimético, sino a la unión de dos strings. Miremos un ejemplo, definamos una variable, le quiero llamar saludo y le voy a igualar al string Hola, quiero concatenar un nombre, ok, un nombre cualquiera, por ejemplo Juan, vale, ejecuto para guardar el valor dentro de la variable saludo y ahora para visualizar el valor que contiene mi variable saludo, voy a usar print, ejecutamos y obtenemos efectivamente la concatenación de mi string Hola con mi string Juan, vale, pero aquí lo vemos un poco raro, notar que Hola Luis está junto, no, esto porque no le dimos un espacio entre estas dos palabras, tenemos que entender que el espacio también es considerado como un carácter, vale, podemos hacer un espacio de esta forma, voy a aquí agregar más celdas, muy bien, entonces creo la variable saludo, ok, voy a abrir un string Hola, pero en esta ocasión antes de cerrar mi palabra Hola con la comilla, voy a darle un espacio, luego utilizo el símbolo más y coloco el nombre de la persona, en esta misma celda voy a hacer uso de la función print y veamos qué sucede, ejecutamos y obtenemos Hola Juan y en esta ocasión sí tengo un espacio, otra forma de contatenar el espacio entre ambas dos palabras es haciéndolo de la siguiente forma, por ejemplo otra vez creo la variable saludo y en esta ocasión voy a guardar en ella otra vez el saludo Hola, no le voy a poner un espacio al final sino lo que voy a hacer es abrir comillas y colocar el carácter espacio entre ella, vale, luego usamos otra vez el símbolo más, voy a poner el nombre Juan, ok, y en la misma celda voy a imprimir el valor que contiene mi variable saludo, ejecutamos y también obtenemos el saludo de Hola Juan con un espacio, vale, bien, otra operación frecuente es la repetición que a diferencia de la concatenación la repetición toma un stream y un número, vale, veamos a qué se refiere esto, borremos estas celdas, se puede concatenar un stream consigo mismo una cierta cantidad de veces mediante el operador de multiplicación, también cabe resaltar que el símbolo de multiplicación no se refiere a un símbolo arimético sino a la repetición de dos streams, miramos un ejemplo, define una variable A y le igualo a un stream cualquiera, por ejemplo curso, quiero validar el tipo de dato que guarda mi variable A para asegurarme que sea un stream, ejecutamos y efectivamente es un stream, vale, ahora quiero repetir el valor que contiene mi variable A que es curso, ok, digamos unas tres veces, digamos quiero concatenarlo por sí mismo tres veces, con una variable y en él hago la operación de repetición, decíamos que quiero repetir el valor de mi variable A por tres veces, en la misma celda voy a imprimir el valor que contiene mi variable B y también voy a preguntarle a Python cuál es el hipode de dato de mi variable B, ejecutamos y efectivamente obtenemos la repetición del mismo stream curso tres veces y el tipo de datos que arroja esta operación también es un stream, antes de continuar borremos estas celdas para maximizar el espacio, vale, ahora hablemos sobre un tema muy importante sobre la manipulación de streams, podemos acceder a los elementos de un stream mediante sus índices, vale, los índices de un stream empiezan desde cero, miremos un ejemplo, creamos una variable que se llama A y en él quiero guardar un stream por ejemplo, ahora ¿qué son los índices en un streams? los índices hacen referencia a los elementos y ¿qué son los elementos? los elementos son cada carácter que lo compone, vale, por ejemplo la letra h hace referencia al índice cero, la letra o hace referencia al índice uno, la letra l hace referencia al índice dos y la letra a hace referencia al índice tres, vale, y ahora voy a ejecutar para guardar el valor, ahora si quiero imprimir la letra a solo quiero obtener la letra a, vale, hacemos lo siguiente, llamo a la variable a, abro corchetes y dentro del corchetes debo de colocar el índice y hace referencia a la letra que quiero imprimir, en este caso decíamos que la letra a hacía referencia al índice dos, perdón al tres, cero, uno, dos y tres, vale, entonces colocamos tres, ejecutamos y efectivamente obtenemos la letra a, ahora quiero imprimir la letra l, entonces llamo a la variable a, abro corchetes y colocó ahora el número dos, ejecuto y efectivamente obtengo la letra l, que pasa si ahora quiero obtener la letra o, hago lo mismo, llamo a la variable, abro corchetes y colocó en esta ocasión el número uno, ejecuto y efectivamente obtengo la letra o, ahora quiero imprimir la primera letra que es h, hago lo mismo, llamo a la variable, abro corchetes y en esta ocasión colocó cero, porque la letra h hace referencia al índice cero, ejecutamos y efectivamente también obtenemos la letra h, algo importante, los espacios en blanco también son considerados como un carácter como ya lo hemos dicho anteriormente, vale, entonces ¿qué pasa si hacemos lo siguiente? por ejemplo, creo una variable b y le quiero igualar la palabra casa, pero hago un espacio al final del string, entonces ejecuto y en esta ocasión, en esta ocasión quiero imprimir el espacio, ¿cómo imprimo el espacio? pues hago lo siguiente, hago el conteo, el índice cero hace referencia a c, el índice uno hace referencia a s a 2 a 3 y el espacio a 4, ¿vale? entonces ¿qué pasa si coloco b, abro el corchetes y coloco el número 4? ¿qué pasa si ejecutamos? efectivamente obtenemos el carácter espacio, ¿vale? el carácter espacio y un espacio dentro de comillas también es un string, ¿vale? lo validamos usando la función type, abro entre comillas, puedo colocar directamente la variable o la función que en este caso hacemos b, abrimos corchetes y colocamos el 4, ejecutamos y efectivamente obtenemos un string, también podemos seleccionar elementos, ¿vale? desde el final usando números negativos, siendo menos uno la última letra, ¿vale? y luego va decreciendo hacia la izquierda, a ver hagamos un ejemplo para que esto se entienda, vale por ejemplo defino una variable en este caso c y quiero guardar en él un string cualquiera, por ejemplo bueno, usamos otra vez sola y en esta misma celda quiero imprimir la última letra, la última letra, ¿qué hacemos? y si es que queremos usar números negativos, hacemos lo siguiente, ponemos el nombre de la variable en este caso c abro corchetes y decíamos que si usamos números negativos la última letra empieza con el valor de menos uno, es decir si hablamos con números negativos, ¿vale? y queremos un poco averiguar los índices de cada elemento, entonces podemos decir que a hace referencia al índice menos uno, l al menos dos o al menos tres y h al menos cuatro, quiero imprimir la última letra es decir el elemento a, entonces si quiero utilizar números negativos tendría que colocar el menos uno, ejecutamos y efectivamente obtenemos el elemento a, ahora quiero imprimir el elemento o el carácter por ejemplo o, decíamos que si es que queremos usar números negativos, empieza con menos uno, l menos dos y o menos tres, entonces que hago, llamo a la variable que tiene mi string, abro corchetes y en esta ocasión si quiero usar números negativos pongo menos tres para poder imprimir la letra o, ejecutamos y efectivamente obtenemos la letra o, también podemos seleccionar un intervalo del string, es decir un pedazo de una palabra por ejemplo, otra vez creo otra variable la letra de por ejemplo y en él escribo un carácter cualquiera, Python por ejemplo, ¿vale? ejecuto para guardar el valor y ahora imaginar que quiero extraer las tres primeras letras de mi palabra Python, ¿cómo puedo hacer? de la siguiente forma llamo al carácter a la variable e, abro corchetes y como quiero imprimir los tres primeros, voy a poner el número cero que hace referencia al primer índice que quiero extraer en este caso hace referencia a la letra p, coloco los dos puntos, ¿vale? y después de los dos puntos voy a averiguar por mí mismo qué índice le pertenece a la letra t, entonces p es cero y es uno y t es dos pero para respetar la sintaxis de Python al momento de colocar el último índice le debo de sumar más uno, si es dos, dos más uno es igual a tres y tres debe estar aquí, ¿vale? si ejecutamos efectivamente obtenemos p y y t, ¿vale? debemos repetar la siguiente sintaxis debemos colocar el string, abrimos corchetes, colocamos el índice que hace referencia a la primera letra que queremos extraer, abrimos dos puntos y luego el índice final más uno, notar que seleccionamos la posición cero que le pertenece a la letra p hasta la posición dos que le pertenece a la letra t, ¿vale? y no toma la posición tres, ¿por qué? porque le sume más uno, esto para repetar la sintaxis de Python, ahora hablemos sobre los carácteres especiales antes de continuar voy a borrar las celdas para tener más espacio entonces existen ciertos caracteres que no podemos escribir directamente desde el teclado para ello ocupamos el símbolo de barra invertida antes de cada carácter especial que nosotros escribamos, veamos un ejemplo, a ver aquí voy a agregar con el teclado B más celdas para tener espacio suficiente, vale, definamos una variable en este caso le llamaré especial por ejemplo y escribamos por ejemplo abrimos comillas son caracteres especiales, ¿vale? cierro el string con otra comilla entonces vamos a ejecutar, veamos qué sucede, obtenemos un error de sintaxis, ¿qué pasa? que las comillas son consideradas caracteres especiales y qué decíamos que teníamos que ocupar tenemos que ocupar la barra invertida antes del carácter, veamos cómo hacerlo, definamos otra vez la variable especial y en ella vamos a colocar la misma sintaxis pero esta vez vamos a ocupar la barra invertida, ponemos la barra invertida, luego la comilla colocamos comillas luego otra barra invertida antes de la comilla que es un carácter especial, luego colocamos el resto del string caracteres especiales, ¿vale? y en esta misma celda vamos a imprimir el valor que contiene la variable especial ejecutamos y efectivamente ahora sí Python no nos arroja ningún error y tenemos las comillas son caracteres especiales sin ningún error con el carácter especial de las comillas, hay otros caracteres especiales como por ejemplo el salto de línea, ¿vale? que sirve para representar el fin de línea en un texto, veamos un ejemplo, definamos una variable, le vamos a llamar especial barra baja 2, le vamos a igualar un string que contenga, a ver hay otros caracteres que sirven para representar el fin de línea en textos y en esta misma celda quiero imprimir el valor que contiene mi variable especial barra baja 2, ejecutamos y obtenemos efectivamente el string hay otros caracteres que sirven para representar el fin de línea en textos, en este caso quiero hacerle alguna modificación, quiero colocar un punto final después de la palabra caracteres y que la siguiente línea continúe el resto del texto, ¿qué es lo que podemos hacer? modificamos directamente en la celda, ¿vale? y entonces decíamos que queríamos que haya aquí un punto final y que la siguiente línea continúe el resto del texto, lo que podemos hacer simplemente descolocar un punto y colocar el carácter especial que significa el salto de línea, hacemos lo siguiente, hacemos la barra invertida y después la n, ¿vale? notar que esto debe estar sin ningún espacio con el último carácter de la frase que nosotros queremos hacer el salto de línea, veamos qué sucede, ejecutamos nuevamente y obtenemos efectivamente el salto de línea después del punto final de este trozo del string, hay otros caracteres, punto y tenemos un salto de línea y luego continúa el resto.