Al iniciar el tema de los controles de flujo de Python, en primer lugar estudiamos a las instrucciones condicionales y decíamos que estas permiten elegir una acción según el resultado al evaluar una condición. Ahora nos adentraremos al tema de los bucles o también llamadas instrucciones iterativas. Un bucle nos permite repetir una instrucción o secuencia de acciones un número determinado de veces o hasta que esta cumpla una condición establecida. En otras palabras estamos hablando de un ciclo repetitivo. Por ejemplo, ¿qué pasaría si queremos sumar todos los números enteros hasta el número 100? Pues tendríamos que sumar como una calculadora desde 0 hasta 100, de esta forma 0 más 1 más 2 más 3 más 4 más 5 más 6 más 7 más 8 más 9 más 10 y así sucesivamente hasta llegar al entero número 100. Pero para esto Python nos ofrece una solución y justamente son las instrucciones iterativas. Estas nos plantean soluciones eficientes a este tipo de problemas. Veamos el primer tipo. El bucle while hace que las instrucciones se repitan hasta que la condición dada resulte ser falsa y vamos a respetar las siguientes sintaxis. Bien, vamos a empezar con la palabra while. Seguidamente vamos a poner la condición que queremos que Python evalúe en cada iteración. Seguidamente colocaremos dos puntos y luego vamos a colocar las instrucciones. Para que esto quede claro vamos a decir que cada vez que esta condición sea verdadera, Python va a ejecutar las instrucciones hasta el número de veces que sea necesario, hasta que el valor de la condición sea falsa. Por ejemplo, resolvamos el ejercicio que planteamos anteriormente. Decíamos que queríamos sumar todos los números enteros hasta el número 100 y para ello vamos a crear una serie de variables. En primer lugar la variable límite que va a guardar el número 100, luego la variable suma que guarda el número 0 y una variable de indexación que voy a iniciarla con 0. Bien, ejecutamos para guardar los valores dentro de las variables y a continuación crearemos el bucle while. Vamos a iniciar con la palabra while y seguidamente vamos a colocar la condición. Nosotros queremos una sumatoria hasta que i llegue a ser a menor o igual al límite. Recuerda que el límite guarda el número 100. Ahora vamos a colocar las instrucciones que queremos que Python ejecute en cada iteración y vamos a hacer que suma sea igual a suma más i. Y adicionalmente que i en cada iteración sea aumentado en un valor de 1. Bien, vamos a ejecutar y ahora vamos a imprimir el valor de suma que era la variable que iba a guardar la sumatoria final. Vamos a sumar y obtenemos 5.050. A ver, estudiamos cada línea. Primero empezamos con la palabra reservada while, luego la condición y en la primera iteración vamos a colocar primera iteración. En la primera iteración Python debe de evaluar esta condición y recordemos que inicia con 0. Entonces Python va a tener que valorar esto 0 menor o igual al límite que contiene 100 recordemos. Vamos a ejecutar y obtenemos efectivamente el valor de true. Por lo que Python va a ejecutar las acciones que están después del while. Entonces suma empieza con 0 y a suma hay que sumarle i. Y también guarda 0. Quiere decir que el valor de suma en la primera iteración es igual a 0. Continuamos con la segunda línea. Ahí hay que sumarle en cada iteración 1. Quiere decir que i en esta primera iteración es igual a 1. Continuamos con la segunda. Segunda iteración. En esta segunda iteración Python va a continuar evaluando la misma condición pero con diferentes valores. En este caso i vale 1 menor o igual a 100 que es límite. Bien, vamos a ejecutar para ver que obtenemos y efectivamente obtenemos el valor de true. Por lo que Python va a continuar ejecutando las acciones que están después del while. En este caso veamos cuánto vale suma. Suma es igual a suma más i. Suma todavía contiene el valor de 0 pero i ha cambiado y ahora vale 1. 0 más 1 es igual a 1. E i hay que sumarle 1. Quiere decir que el valor de i en la segunda iteración es igual a 2. Continuamos con la tercera iteración. Tercera iteración. En este caso Python va a seguir evaluando la misma condición pero con diferentes valores. Ahora i es igual a 2 menor o igual a 100. Ejecutamos. Obtenemos nuevamente el valor de true por lo que Python va a continuar ejecutando las instrucciones que están después del while. Bien, veamos cuánto vale suma. Suma vale igual a suma. En esta iteración vale 1. E i vale 2. 1 más 2 es igual a 3. E i vale 2 más 1. Quiere decir que es igual a 3. Y así sucesivamente hasta llegar por ejemplo a que i logre obtener un valor de 100. Por ejemplo si i llega a hacer 100, Python lo que va a hacer es evaluar esto. 100 menor o igual a 100 va a ejecutar igual a true. Y en ese caso como es true va a seguir ejecutando las instrucciones que están después del while. Pero ¿qué pasa cuando i llegue a hacer 101? menor o igual a 100. Ejecutamos y por fin obtenemos un valor de falso. Es allí cuando Python va a decir hasta aquí voy a ejecutar las acciones. Ya no voy a ejecutar las instrucciones que están después del while. Ahora tratemos de entender cada iteración, cada suma. En la primera iteración decíamos que la variable suma vale 0 y la variable i suma 1. En la segunda suma vale 1 y vale 2. En la tercera suma vale 3 e i vale 3. ¿Qué pasa si lo sumamos nosotros manualmente? 0 más 1 es igual a 1. 0, perdón, 1 más 2 es igual a 3. Bien, y así sucesivamente. Con lo que quiero decir que en la primera iteración suma empezó con 0. En la segunda iteración la sumatoria fue 1. Recordar que suma guarda el valor resultante de la sumatoria final. Aquí obtenemos 1 también. En la tercera iteración suma valió 3. También vemos aquí una sumatoria de 3. Y así va a sumar hasta llegar a que la condición sea finalmente falsa. Hagamos un while, un último ejemplo, pero con una sumatoria mucho más fácil. Por ejemplo quiero sumar los números enteros hasta el número 5. Vamos a hacerlo primero manualmente. Hasta 5. Ejecutamos y obtenemos el valor de 15. Bien, veamos cómo lo hacemos con while. Vamos a copiar y pegar. Pero previamente tenemos que crear variables, digamos las variables que van a iniciar previamente el while. Bien, en este caso mi límite es 5 porque quiero sumar hasta el número entero 5. Entonces límite va a ser, perdón, control z, mi límite va a ser 5. La suma lo dejaré con 0 y también con 0. Tratemos de ejecutar esta misma instrucción. Y ahora vamos a imprimir el valor de suma que va a guardar, recordar la sumatoria total. Vamos a imprimir suma, en este caso, a ver, es que no ejecutamos, a ver, nunca olvidar ejecutar una celda porque si no, no va a guardar los nuevos valores en las variables. No ejecutamos aquí. Ahora sí, ejecutamos nuevamente esta celda con instrucción while. Ahora vamos a imprimir la nueva suma. E imprimimos efectivamente el valor de 15. Como aquí, obtumimos manualmente en la sumatoria de 0 hasta 5. Bien, ahora vamos a estudiar otro tipo de bucle. Estamos hablando de los bucles acotados for in. Y este tipo nos permite repetir una serie de instrucciones, un número definido de veces. Y vamos a respetar la siguiente sintaxis. Vamos a empezar con la palabra fort, seguido de la variable que va a recorrer una serie de secuencias. Luego vamos a colocar la palabra in. Seguidamente colocamos la secuencia que va a recorrer nuestra variable. Seguidamente colocamos dos puntos, hacemos un salto de línea y vamos a colocar el cuerpo del bucle, que básicamente son las instrucciones que queremos que Python ejecute. En el primer ejemplo que resolvimos con la estructura while, también lo podríamos haber hecho de una manera más sencilla y corta, utilizando la estructura for in. Debido a que tenemos predifinido el número exacto de veces que queremos que se repita nuestro bucle. En el ejemplo queríamos sumar los números enteros hasta el número 100. Y habíamos dicho que podríamos sumar como una calculadora desde Python desde 0 hasta 100. De esta forma, 0 más 1 más 2 más 3 más 4 más 5 y así sucesivamente hasta llegar al número entero 100. Pero ahora tratemos de resolverlo con el bucle for in. En primer lugar vamos a crear una variable donde podamos guardar el resultado final de la sumatoria y le vamos a llamar suma de igual forma. Vamos a iniciar con 0. Ejecutamos para guardar el valor dentro de la variable y ahora vamos a estructurar nuestro bucle for in, empezando con la palabra for. Vamos a colocar la variable que va a recorrer la secuencia, colocamos la palabra in y vamos a colocar la secuencia, utilizando la función range. Ahora vamos a ver qué significa range. Abrimos paréntesis dentro del coloco el número 101, hacemos 2 puntos y a continuación colocamos el cuerpo o las instrucciones. Entonces queremos que nuestra variable suma en cada iteración sea sumado Bien, vamos a ejecutar. Ahora quiero imprimir la variable suma que contiene el resultado final de la sumatoria ejecuto y efectivamente también obtengo el valor de 5050. Notar que el código es muy similar pero más corto que la estructura wild. En este caso creamos la variable i que iterará desde 0 hasta el 101 no inclusive, es decir hasta 100. Usando for la variable i se inicializa y se incrementa implicitamente y con ello nos ahorramos crear la variable acumuladora o de indexación y sumarle 1 al final de cada instrucción. Seguramente te estás preguntando qué significa la función range, pues aquí te lo explicó. La función range genera un rango algo muy parecido a una lista y que se puede convertir en una lista sencillamente también range 101. Esto genera una secuencia de números que parte desde 0 hasta el fin menos 1, es decir hasta 100. Por ejemplo hagamos el siguiente ejercicio, vamos a hacer un for, voy a crear la variable que va a iterar en base a una secuencia, colocamos la palabra in y voy a usar la función range, habro paréntesis y coloco el número 6. Luego coloco 2 puntos, coloco la instrucción que quiero y voy a hacer un print i. Vamos a ejecutar para ver qué obtenemos. Ejecuto y obtengo los valores enteros desde 0 hasta el número 5. ¿Por qué no imprimió el número 6? Pues decíamos que esta función parte desde 0 hasta el número fin menos 1, es decir hasta el número 5. ¿Y qué pasa si queremos un rango que inicie en otro número que no sea 0? Pues hacemos lo siguiente, hacemos un for nuevamente, for i, hacemos un in, llamo a la función range, habro paréntesis y en primer lugar voy a colocar el número que quiero que inicialice, no en 0, sino en 5 esta vez, hago una coma y coloco el número final. Recordar que este número final sería 10 en realidad porque tenemos que restarle menos 1. Hacemos 2 puntos y vamos a ver qué pasa sin primo en cada iteración. Ejecutamos y efectivamente obtenemos los números enteros desde el 5, desde el 5 hace el número 10. ¿Por qué? Porque range toma como número final el número que nosotros le colocamos pero le resta menos 1. Y como habíamos dicho, este rango lo podemos convertir en una lista haciendo uso de la función list. Por ejemplo, a ver, quiero crear una lista range, bien, en esta variable voy a guardar lo siguiente, hago list, habro paréntesis y dentro del vamos a colocar una función que sea range de 5,11, es decir que inicie desde 5 hasta el número 10. Bien, vamos a ejecutar para guardar el valor dentro de la variable lista range y vamos a imprimir ahora que obtenemos en nuestra lista range. Ejecutamos y efectivamente obtenemos una lista que va desde el entero número 5 hasta el entero número 10. Y para validar que esto es una lista, uso type list range, ejecuto y efectivamente obtengo un tipo de valor lista, esto para que quede claro la función range. Ahora vamos a regresar a la función inicial o al problema inicial que tratamos en un inicio antes de llegar al tema de range. Bien, aquí voy a agregar más celdas y vamos a comprobar cada iteración o al menos las primeras, ¿vale? A ver, la primera iteración, primera iteración, pero antes de esto vamos a ejecutar otra vez las celdas, ejecutamos, también voy a imprimir la variable suma otra vez para que tengamos todo, todo bien, tenemos 5,050, bien, en la primera iteración y nuestra variable y tiene que recorrer una secuencia, bien, en este caso tenemos range 101, recordar que esta secuencia va desde 0 hasta el número 100, es decir 101 menos 1, entonces en la primera iteración y vale 0, entonces como vale 0 la suma hay que sumarle y y suma empieza con 0, quiere decir que en la primera iteración suma vale 0, en la segunda iteración, segunda iteración, el valor de y, el valor de y va a ser, va a tomar el valor de 1, va a tomar el valor de 1, quiere decir que suma es igual a 0, bien más 1, quiere decir 1, entonces en la segunda iteración suma vale igual a 1, veamos la tercera iteración y en la tercera iteración va a tomar el valor de 2, 2, quiere decir que en la tercera iteración suma es igual a 1 más 2, quiere decir que es igual a 3 y así sucesivamente hasta llegar al valor de 100, veamos, a ver vamos a imprimir range 101 para validar lo que estamos diciendo, imprimimos y si se dan cuenta obtenemos los valores de 0 hasta el número 100, pero vamos a imprimirlo, vamos a hacer un for y y range 101, 2 puntos y le voy a decir que imprima en cada iteración el y, imprimimos y obtenemos efectivamente los números enteros de desde 0 hasta el número 100, básicamente en esto se resume el bucle acotado for y