Muy bien, vamos a empezar a estudiar la segunda herramienta que nos hará falta en este curso. Estamos hablando de PIB. PIB es un gestor de paquetes que nos va a permitir instalar diferentes librerías. PIB viene instalado de forma predeterminada al momento de instalar anaconda navigator. Quiere decir que no vamos a buscar ningún instalador en la web ya que lo tenemos. Para poder hacer uso del, vamos a ir a environments, vamos a darle click en cualquiera de los entornos que tenemos activos. En este caso voy a ir a base, me voy a acercar al símbolo de play, le daré click y abro el terminal. Bien, esto ya lo hicimos en la clase anterior. Ahora está activo el entorno base. Como quiero hacer ciertas pruebas instalando librerías y borrándolos y no quiero que se ensucie mi entorno base, quiero saltar a otro entorno y como lo hacíamos, hacíamos conda, activate y luego colocamos el nombre del entorno que queremos ir. En este caso, ya modo de ejemplo, vamos a ir a prueba barra baja entorno. El primer comando que vamos a aprender será PIB install. Con este comando vamos a poder instalar la librería que nosotros queramos. Bien, vamos a hacer PIB install y vamos a colocar un nombre cualquiera, el nombre de la librería que queramos. Por ejemplo, quiero colocar la librería mat, vamos a ejecutar para ver qué obtenemos y obtenemos un error. Probablemente no estamos escribiendo de la forma correcta el nombre de la librería, por eso que PIB no nos está haciendo caso. Lo que podemos hacer ya modo de ayuda es buscar en Google la forma correcta de cómo escribir con PIB y para poder instalar la librería mat, pues hacemos lo siguiente, vamos a buscar en Google y vamos a buscar PIB install mat, buscamos y vamos a darle clic a la primera búsqueda que encontramos. Esta página web es muy recomendable, te la recomiendo de verdad guardala, vamos a darle clic. Muy bien y aquí podemos observar la forma correcta de escribir con PIB para poder instalar esta librería necesitamos colocar Python, guion, mat y no mat como yo lo he colocado, entonces lo que podemos hacer es copiar simplemente, regresar a nuestra terminal y pegar, vale, ahora vamos a ver qué obtenemos. Muy bien dice que esta librería ya está instalada, ya estaba instalada en nuestro entorno prueba, así que no pasa nada, perfecto, ahora si quiero averiguar si realmente lo tenemos instalado, lo que podemos hacer es con da list, que nos dará la lista de librerías que tenemos instalado dentro de este entorno, muy bien y efectivamente lo tenemos aquí, tenemos a Python mat con la versión 0.0.1 y qué pasa si quiero instalar otra librería con alguna versión en específico, recordar que cada versión de cada librería podría traer diferentes funcionalidades, va a depender mucho de nuestro programa, bien para ello hacemos lo mismo, PIB install, vamos a colocar el nombre del paquete, luego hacemos el igual y vamos a colocar entre comillas la versión específica que necesitamos, no va entre comillas por ejemplo va así 1.5.1 por ejemplo y ejecutamos, vale, muy bien hemos instalado Python mat dentro del entorno que está activo, ahora quiero validar si realmente hemos instalado esta librería en mi entorno de prueba, bien pues usamos con da list, ejecutamos, recordar que este comando nos muestra todas las librerías que tenemos instaladas dentro del entorno que está activo, muy bien efectivamente como puedes observar tenemos a Python mat dentro del con la versión 0.0.1, hablando de versiones podemos usar ciertos comandos para actualizar a la versión que nosotros queramos, una extensión de PIB install es el siguiente, después de PIB install podemos agregar dos guiones y colocar upgrade, después de upgrade podemos colocar el nombre de la librería que nosotros queramos, por ejemplo bueno, ahora vamos a colocar nombre librería, simplemente para el ejemplo no vamos a ejecutar precisamente algo real, simplemente quédate con esta línea, cópialo y lo que va a hacer esta línea este comando va a actualizar a la última versión de la librería pero que esté disponible dentro de la versión de Python que tiene instalado mi entorno, recordar esto y por otra parte PIB install también tiene otra extensión por así decirlo donde nosotros podemos especificar la versión que nosotros queramos, no necesariamente la última que esté disponible, por ejemplo lo que vamos a hacer es PIB install, vamos a usar Python mat nuevamente, vamos a hacer el igual, dos iguales y luego vamos a colocar por ejemplo la versión que ya tenemos 0.01, notar que en el entorno de prueba ya tenemos a Python mat con la versión 0.0.1 y yo estoy instalando Python con la versión de 0.01 a lo mejor al momento de ejecutar vamos a obtener un mensaje donde nos dirá PIB que nosotros ya tenemos satisfecho esta necesidad, entonces vamos a ejecutar y efectivamente PIB nos arroja el resultado o el mensaje que dije hace un momento, si por ejemplo una librería, no estamos hablando de esta bien por ejemplo otra librería tuviese alguna versión superior o inferior y nosotros tengamos que necesitarla, tengamos que usarla dentro de nuestro programa, lo podemos efectuar sin ningún problema y por último vamos a ver un comando muy importante, en caso nosotros queramos desinstalar alguna librería y vamos a hacer el ejemplo desinstalando a Python mat, lo que vamos a hacer es PIB un install y seguidamente vamos a colocar el nombre de la librería que queremos eliminar y en este caso y a modo de ejemplo Python ejecutamos, hay una pregunta si procedemos, si si o no tecleamos y ejecutamos y efectivamente hemos desinstalado Python mat, ahora con el fin de validar si realmente hemos desinstalado esta librería, lo que vamos a hacer es conda list ejecutamos y vamos a ver si en esta lista ya nos encuentra Python mat, bien y efectivamente no lo podemos observar