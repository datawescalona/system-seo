Ahora veamos un tipo de datos un poco más complejo. Una lista es una secuencia de elementos que pueden ser de cualquier tipo. Pueden estar constituidas por un string, por un entero, un real, hasta incluso una lista. Este tipo de datos está delimitado por corchetes y sus elementos están separados por comas. Veamos un ejemplo. Definamos una variable, la quiero llamar lista y le voy a igualar a una lista a brocorchetes. Coloco como primer elemento por ejemplo un string, hacemos sola, colocamos una coma, para agregar otro elemento puede ser un entero, ahora un decimal, por ejemplo 5.5, hacemos una coma y agregamos una lista, por ejemplo 1,2,3. Ejecutamos para guardar el valor dentro de mi variable lista y un poco para confirmar lo que estamos diciendo, vamos a averiguar el tipo de datos que guarda mi variable lista usando la función type. Abrimos paréntesis y en él colocamos el nombre de la variable. Ejecutamos y efectivamente obtenemos un tipo de dato list. Para que se entienda vamos a repetir el ejercicio paso a paso. Creamos una variable a y le vamos a igualar el string. Hola en esta misma celda vamos a hacer uso de la función print y dentro de la función print voy a agregar la función type, habrá paréntesis y en él añado el nombre de la variable que en este caso es a. En esta misma celda vamos a crear otra variable en este caso la llamaré b y la voy a asignar el número entero 5. También en esta misma celda voy a hacer uso de la función print, dentro del coloco la función type habrá paréntesis y voy a colocar el nombre de la variable b. Continuamos trabajando en esta celda, creo una variable c y le voy a asignar un número decimal 5.5 como le añadimos en esta lista. Hacemos lo mismo, vamos a imprimir el tipo que guarda mi variable c, seguimos trabajando, creamos una variable d y le vamos a igualar una lista, abrimos corchetes y vamos a colocarle la misma lista que añadimos dentro de g1 lista, todos y 3. También voy a imprimir el tipo de datos que contiene mi variable d, pues vamos a imprimir, ejecutamos y lo que hemos dicho, tenemos un string para la variable a, tenemos un entero para la variable b, tenemos un flowad para la variable c y tenemos una lista para la variable d. Ahora lo que quiero hacer es guardar todos estos elementos en una lista usando variables, por ejemplo colocamos la miss el mismo nombre de la variable que usamos en este primer ejemplo hacemos lista, abrimos corchetes, que habíamos dicho que una lista primero empieza con corchetes y también cerramos con corchetes, entonces decíamos que queríamos usar las variables, entonces hacemos lo siguiente, hacemos a, coma, los elementos deben estar separados por comas b, c y d, ahora en esta misma lista también quiero imprimir el valor que contiene mi variable lista, ejecutamos y efectivamente obtenemos una lista con los elementos que habíamos creado en esta celda, por otro lado podemos crear listas vacías, que son listas muy útiles para agregar elementos en una variable existente, esto lo veremos más adelante con un poco más de detalle, podemos crearlas de dos formas, una lista vacía por ejemplo creamos esta variable, le igualamos, abrimos corchetes y dentro de los corchetes no colocamos absolutamente nada, es decir mediante dos corchetes sin elementos, lo ejecutamos y para asegurarnos si es que realmente la variable vacía contiene una lista, le vamos a preguntar a Python qué tipo de datos guarda, hacemos type, abrimos paréntesis, colocamos el nombre de la variable y ejecutamos, efectivamente obtenemos el tipo de datos lista, una segunda forma para crear una lista vacía, creamos una variable vacía 2 por ejemplo, igualamos y hacemos uso del constructor list, abrimos paréntesis y no hace falta pasarle ningún argumento y en esta misma celda también vamos a imprimir el tipo de datos que contiene mi variable vacía 2, para confirmar lo que estamos diciendo, entonces ejecutamos y efectivamente también obtenemos un tipo de datos lista, ahora veamos cómo podemos averiguar la longitud de una lista mediante la función len, para ello construiremos una lista dentro de la variable objeto, abrimos corchetes y agregamos elementos por ejemplo el entero 1, un string hola, agregamos otro elemento por ejemplo otro string python y por último quiero agregar un flowad, ejecutamos para guardar la lista dentro de la variable objeto, ahora quiero preguntarle a Python cuál es la longitud de la lista creada, para ello usamos la función len, abrimos paréntesis y en él colocamos el nombre de la variable que guarda mi lista, en este caso se llama objeto, ejecutamos y obtenemos el valor de 4, ¿por qué 4? porque mi lista contiene 4 elementos, el primer elemento es el entero 1, el segundo elemento es el string hola, el tercer elemento es el string python y por último tenemos el flowad 2.6, veamos otro ejemplo, creamos la variable objeto barra baja 2 y le asigno una lista, por ejemplo esta vez quiero que solamente contenga enteros 2,3 y 10, vamos a añadir un entero más 15, ejecutamos y otra vez hago uso de la función len, agregó paréntesis y colocamos el nombre de la variable, ejecutamos y efectivamente obtenemos 5 ¿por qué? porque tenemos 5 elementos en la lista 1, 2, 3, 4 y 5, muy bien, ahora veamos cómo recorrer elementos dentro de una lista, los elementos de una lista están ordenados, esto significa que cada uno tiene un índice o posición única, estos elementos parten desde cero, veamos un ejemplo, creamos una variable que se llama objeto nuevamente y voy a crear una lista, por ejemplo quiero un string hola, un entero 5 nuevamente, un flowad 2.9 y en esta ocasión también le voy a agregar otra lista que contenga los enteros 4, 5 y 15, ejecutamos y quiero imprimir el segundo elemento de mi lista, como decidimos las posiciones empiezan desde el índice cero, quiere decir que el string hola hace referencia al índice cero, el 5 al índice 1, el 2.9 al índice 2 y la lista de enteros hace referencia al índice 3, entonces ¿qué pasa si quiero imprimir el segundo elemento? pues hacemos lo siguiente colocamos el nombre de la variable que contiene mi lista, abro corchetes y coloco el índice número 1, porque queremos imprimir el primer elemento, mejor dicho el índice número 1, ejecutamos y obtenemos efectivamente el índice o el elemento que tiene como índice el número 1, ahora si quiero validar el tipo de dato del elemento que hace referencia al índice 1 de una lista hacemos uso de la función type, abro paréntesis y directamente puedo colocar dentro del paréntesis el nombre de la lista, abro corchetes y por ejemplo quiero el primer elemento, quiero averiguar el tipo de dato del primer elemento de mi lista, ejecutamos y obtenemos un string ¿por qué? porque estoy imprimiendo dentro del paréntesis de type objeto con el índice cero, lo que quiere decir que es el primer elemento de la lista, quien es el primer elemento de la lista hacemos referencia al string hola, vale, veamos, llamamos a la lista objeto colocamos el índice cero e imprimimos y efectivamente es el string hola, de igual forma podemos seleccionar un subconjunto de una lista, vale, miremos cómo, voy a agregar más celdas y voy a hacer uso de la misma lista que he creado en esta en esta celda, supongamos que quiero los dos primeros elementos, vale, ya no quiero imprimir solo uno, ahora quiero los dos primeros elementos, hacemos lo siguiente colocamos el nombre de la variable que contiene la lista, abrimos corchetes y vamos a utilizar la siguiente sintaxis, Python nos dice que primero tenemos que colocar la lista, luego tenemos que abrir corchetes y colocar el índice de inicio, en primer lugar luego dos puntos y finalmente el índice final pero tengo que sumarle más uno, si queremos obtener los dos primeros elementos tenemos que colocar dentro del corchete cero, dos puntos y el índice final en este caso es uno pero tengo que sumarle más uno, uno más uno es igual a dos, ejecutamos y efectivamente obtenemos los dos primeros elementos, ahora si quiero los tres primeros elementos que tendría que hacer llamar a la variable, vale, abro corchetes, coloco el índice de inicio luego los dos puntos y como quiero los tres primeros tendría que sumarle el índice final más uno, índice es cero, uno, dos, dos más uno es tres, colocamos tres, ejecutamos y efectivamente obtenemos los tres primeros elementos, vale, hagamos un último ejemplo, ahora quiero obtener los dos últimos elementos, veamos cómo hacerlo, otra vez llamamos al objeto que contiene la lista, abrimos corchetes, vale y esta vez vamos a contar, a ver, índice uno, índice cero, uno, dos, colocamos dos que es el índice de inicio, dos puntos y esta lista le pertenece al índice tres, tres más uno es igual a cuatro, ejecutemos y veamos que efectivamente obtenemos los dos últimos elementos de mi lista, también podemos contar el número de apariciones de un valor dado en una lista haciendo uso del método count, por ejemplo, definamos una lista que se llama a por ejemplo, creamos dentro de una lista, abrimos corchetes, colocamos uno, dos, tres y tres, vale, supongamos que quiero conocer cuántas veces se repite el elemento número tres, hacemos lo siguiente, en la misma celda, hacemos a, seguidamente un punto y luego hacemos uso de la función count, abrimos paréntesis y dentro del paréntesis debo colocar el elemento que quiero contar cuántas veces aparece en mi lista, en este caso como habíamos dicho para el ejemplo colocamos el número tres, ejecutamos y obtenemos el valor de tres, ¿por qué? porque el entero tres se repite tres veces en la lista, uno, dos y tres, muy bien, ahora entremos al tema de cómo podemos modificar los elementos de una lista, como primer punto podemos decir que tenemos la capacidad de reemplazar algún elemento por su posición, veamos un ejemplo, creamos una variable que se llama a, vale, en el vamos a construir una lista, hacemos un entero, uno por ejemplo, creamos un string hola, otro string python y por último agregamos un entero cuatro, ejecutamos para guardar la lista dentro de la variable a, vale, y digamos que quiero conocer cuál es el elemento que hace referencia al indice dos, por ejemplo, ejecutamos y obtenemos el string python que efectivamente hace referencia al indice dos, si contamos de izquierda a derecha iniciando desde cero, tenemos cero, uno y dos efectivamente, digamos que quiero reemplazar el string python por otro valor, ¿vale? como lo hacemos hacemos lo siguiente, llamamos a la variable a, abrimos corchetes, coloco el indice que en este caso es dos y le igualo al otro valor que quiero reemplazar, este valor puede ser de cualquier tipo, por ejemplo un entero, quiero colocar el número 18, en esta misma celda quiero imprimir el nuevo valor que contiene mi variable a, hago print up, ejecutamos y efectivamente el string python ha sido reemplazado por el entero número 18, hagamos otro reemplazo, ahora quiero reemplazar por ejemplo el elemento número 4, que lo que hago llamo a la variable, abro corchetes y coloco el indice que hace referencia al elemento que quiero modificar, en este caso el entero número 4 tiene como posición el número 3, 1, 2 y 3, muy bien, entonces lo igualo y vamos a reemplazarlo por otros, por un string, por ejemplo, buenos días y en esta misma celda vamos a imprimir el nuevo valor que contiene la variable a, bien ejecutamos y efectivamente hemos reemplazado el número 4 por el string buenos días, por otro lado podemos añadir más elementos a una lista haciendo uso del método append, el elemento se va a añadir al final de la lista original, veamos otro ejemplo, creamos la lista b, vamos a construir por ejemplo el 2, el 3, el string python y el float 2.8, ejecutamos para agarrar la lista dentro de mi variable b y ahora supongamos que quiero añadirle a la lista un string, como hacemos pues llamamos a la variable b, hacemos un punto, hacemos uso del método append de esta forma, abrimos el paréntesis y colocamos el elemento que queremos que sea añada, bien por ejemplo quiero añadir un string por ejemplo casa, en esta misma celda vamos a imprimir el nuevo valor que contiene la variable b, ejecutamos y efectivamente el string casa se añadió al final del todo de mi lista, hagamos otro ejemplo, ahora quiero añadirle a la lista por ejemplo un entero, como hacemos llamamos a la lista b, hacemos un punto, seguidamente el método append, abrimos paréntesis y dentro de él colocamos el elemento que queremos añadir, decíamos que un entero por ejemplo, entonces colocamos 15, el entero 15 y en la misma celda voy a imprimir la variable b con el nuevo valor, ejecutamos y efectivamente hemos añadido al final del todo el número entero 15 y ¿qué pasaría si queremos añadir más de un elemento? para este caso usamos el método extend, este método usa como parámetro otra lista, por ejemplo creamos una lista que sea guardada en la variable h por ejemplo, una lista simple la letra a y el entero número 1, en la misma celda vamos a crear otra variable por ejemplo k, creamos una lista simple por ejemplo el número entero 34 y el número entero 35, ejecutamos para guardar los valores y en este caso quiero agregar los elementos de la variable k a la lista que contiene mi variable h, lo hacemos, hacemos lo siguiente, colocamos la lista original o la lista principal, seguido de un punto hacemos uso del método extend, abrimos paréntesis y dentro de él vamos a colocar la lista con los elementos que queremos que se añade a la lista principal, en este caso colocamos k y en esta misma celda lo que vamos a hacer es imprimir el nuevo valor que contiene mi variable h que era la lista principal, vamos a ejecutar y veamos que obtenemos efectivamente obtenemos una nueva lista con los elementos añadidos de la variable k a la lista principal, tenemos el string a, el entero 1 fusionado con los elementos de la lista k, también podemos agregar un elemento precisando su posición en la lista, con los dos últimos métodos que hemos practicado hemos añadido elementos pero al final del todo, pero esta vez quiero usar un método que me permita precisar el índice donde quiero yo agregar el elemento, hagamos un ejemplo creamos una lista, una lista simple por ejemplo 1, 2, 3, 4, 5 y 6, ejecutamos para guardar la lista dentro de la variable a, ahora quiero insertar por ejemplo el número 20 en el índice 1, como lo hacemos llamamos a la variable a que contiene la lista que quiero modificar, entonces llamamos al método insert y el método insert me va a pedir primero la posición y luego el elemento, decíamos que queríamos agregar en la posición 1 el elemento número 20, en la misma celda vamos a imprimir el nuevo valor que guarda mi variable a, hacemos print a y ejecutamos y efectivamente el número 20 ha sido agregado en el índice 1 de la lista que está guardada en la variable a, ahora vamos a tratar de validarlo, hagamos un llamado de la lista a, abrimos corchetes y colocamos la posición 1, veamos si es correcto, ejecutamos y efectivamente obtenemos el número 20 que hace referencia al índice 1, notar que el primer parámetro se refiere a la posición y el segundo se refiere al elemento, esto también modifica las posiciones de los otros elementos, esto nos lleva a otra cuestión, como conocemos la posición de un elemento, pues esto lo podemos conocer haciendo uso del método index, por ejemplo hagamos uso de la última lista que hicimos, que construimos, para validar los elementos hacemos un print a, ejecutamos y obtenemos esta lista, muy bien ahora quiero conocer qué índice o qué posición hace referencia por ejemplo el número 6, entonces llamo a la variable a que contiene la lista, entonces hago uso del método index, abro paréntesis y coloco el elemento que quiero conocer su índice, ejecutamos y obtenemos el índice 6, porque, a ver contemos de izquierda a derecha a partir del 0 y veamos si efectivamente el número 6 hace referencia al índice 6, 0, 1, 2, 3, 4, 5 y 6, bien veamos otro ejemplo, creamos otra lista por ejemplo en la variable k, por ejemplo el string hola, el string operación, el string python y el número entero 250, ejecutamos para guardar el valor dentro de la variable k, y ahora supongamos que quiero conocer cuál es el índice que hace referencia el string python, hacemos lo siguiente, colocamos el nombre de la variable, luego un punto, colocamos el método index, abrimos paréntesis y dentro de él colocamos el string python, ejecutamos y obtenemos el índice número 2, tratemos de validarlo nosotros mismos, contemos izquierda a derecha a partir del 0, 0, 1 y 2 efectivamente, de igual manera podemos eliminar elementos, una de las formas es usando el método remove, este método toma como parámetro un elemento conocido en la lista, tengamos en cuenta que no devuelve ningún valor, vale y que elimina la primera aparición del objeto, miremos un ejemplo, creamos una lista que se llama a, creamos una lista simple de números enteros, ejecutamos para guardar el valor dentro dentro de mi variable a, y supongamos que quiero eliminar el número entero 5, hacemos lo siguiente, llamamos la variable que contiene la lista, hacemos uso del método remove, abrimos paréntesis y colocó el número 5, vamos a imprimir el resultado en la misma celda, hacemos print a, bien y ejecutamos, efectivamente ha borrado el método remove, el primer elemento que ha encontrado, vale que sea el número 5, en la lista original tenemos cuatro apariciones del entero número 5, ahora después de haber eliminado la primera aparición del número 5, solamente obtenemos tres apariciones del entero número 5, otra forma de eliminar elementos es haciendo uso del método pop, con esto borramos el elemento en el índice dado como parámetro, y tengamos en cuenta que este método si devuelve el elemento eliminado como valor de retorno, lo que significa que es recomendable guardarlo en una variable, por ejemplo creamos una variable, construimos una lista, por ejemplo el primer elemento quiero que sea hola, el segundo elemento universidad, el tercer elemento el entero número 2, ejecutamos para guardar el valor que en este caso es una lista en la variable a, y vamos a crear una variable que se llama borrado y le vamos a igualar a la operación, ok, entonces llamamos a la lista, ha seguido de un punto, llamamos al método pop, abrimos paréntesis y supongamos que quiero borrar el string universidad, la universidad se que hace referencia el índice 1, por eso coloco el número 1 aquí, dentro de la misma celda quiero imprimir el nuevo valor que contiene mi variable a, ejecutamos, efectivamente observamos que se ha eliminado el elemento que hacía referencia al índice número 1, ahora quiero imprimir elemento que está dentro de mi variable borrado, decíamos que el método pop nos retorna el valor que ha eliminado, veamos que pasa ejecutamos y efectivamente me retorna el valor que ha sido eliminado dentro de mi lista, y que pasa si no le pasamos un índice, el método pop lleva el índice en menos 1 como predeterminado, recordemos que el índice en menos 1 hacía referencia al último elemento, por ejemplo hagamos uso de la última lista creada, bueno creada y modificada, para ello hacemos nuevamente imprimedad, para validar los elementos que tenemos finalmente en esta variable tenemos 12 elementos y que pasa si ahora borramos usando el método pop pero sin pasarle ningún argumento, pues hacemos, creamos una variable que se llama me borrado 2, vamos a igualar el nombre de la variable, hacemos un punto, colocamos el método pop y abrimos paréntesis, no vamos a pasarle absolutamente nada y en esta misma celda vamos a imprimir el nuevo valor de la variable a y también vamos a imprimir el elemento borrado 2, ejecutamos y efectivamente como decíamos hace un momento el método pop lleva consigo el índice en menos 1 como predeterminado, si no le pasamos ningún índice, por lo consiguiente ha borrado el último elemento de la lista, el último elemento de la lista es el número 2, por eso que al imprimir el valor de retorno de esta operación que se guardaba en la variable borrado barra baja 2 obtenemos el número entero 2, por otro lado podemos desear eliminar todos los elementos entre dos posiciones dadas, para ello haremos uso del método del, hagamos un ejemplo, creamos una variable y en él voy a guardar una lista simple de 0, 1, 2, 3, 4, 5 y 6, notar que estoy construyendo esta lista a propósito para hacer referencia a cada elemento de mi lista a su índice, veamos que este elemento vale que es igual al número entero 0 hace referencia al índice 0, el entero número 1 hace referencia al índice 1, el entero número 2 hace referencia al índice 2 y así sucesivamente, no tenemos que confundir dos conceptos distintos, el elemento es un concepto que no tiene nada que ver con su posición índice, no tenemos que confundir eso, lo estoy construyendo así para poder visualizar mejor el ejemplo que vamos a hacer, entonces decíamos que si queremos eliminar los elementos que están dentro de un rango de posiciones o índices, tendríamos que usar o podríamos usar el método del, entonces por ejemplo tenemos como tarea eliminar los valores que están dentro desde la posición 2 hasta la posición 5 por ejemplo, bien, entonces para hacer uso del método del tenemos que seguir las sintaxis de python y cuál es esa sintaxis pues es la siguiente tenemos que colocar la palabra reservada del, después un espacio en blanco, luego llamar a la lista, abrimos corchetes, colocar el índice inicial, dos puntos y el índice final, pero python me dice que tengo que sumarle más uno al índice final, entonces debemos colocarle o sumarle más uno, esta es la sintaxis que debemos de repetir, bien, entonces seguimos en esta misma celda y decíamos que nuestra tarea era eliminar todos los elementos que están incluidos desde la posición 2 hasta la posición 5, entonces que lo que hacemos colocamos la palabra reservada del, luego el nombre de la lista en este caso es la variable a, abrimos corchetes y colocamos el índice inicial, en este caso es 2, colocamos dos puntos y el índice final es 5, recordar que el índice final le tenemos que sumar más uno, 5 más 1 es igual a 6, entonces colocamos 6 y en esta misma celda voy a imprimir el nuevo valor de la variable a, antes de ejecutar esta lista tratemos de nosotros resolverlo los elementos que hacen referencia a la posición de la de la 2 hasta la 5 son los siguientes, este es el índice 0, índice 1, índice 2, incluye, entonces 2, 3, 4 y 5, bien, deberían de irse o eliminar estos cuatro elementos y mi variable a simplemente se quedaría con tres elementos de una lista, bien, entonces ejecutemos a ver si estamos en lo correcto y efectivamente obtenemos una lista que solo tiene tres elementos porque hemos eliminado una fracción o una parte de una lista dada, note que eliminó los elementos desde la posición 2, vale, que le pertenecía al número 2 hasta la posición número 5, que le pertenecía al número 5, no incluye el último índice como tal, porque, porque la sintaxis de payton me dice que tengo que sumarle más uno al último índice dado, ahora veamos cómo podemos concatenar listas, eliminamos esta celda y entonces nosotros podemos fusionar, concatenar listas con el signo más, bien, veamos un ejemplo, creamos una lista, por ejemplo, con los enteros 1 y 2, en esta misma celda crearé otra lista, trae la variable b, coloco aquí el número entero 3, 4 y 5 y en esta misma celda lo que vamos a hacer es concatenar la lista a y b, como haciendo uso del signo de suma, hacemos a más b, ahora vamos a ejecutar la celda para ver qué obtenemos y efectivamente obtenemos una lista con los elementos fusionados, mi nueva lista contiene 5 elementos, el 1, el 2, el 3, el 4 y el 5, de igual forma podemos repetir los valores de una lista con el operador de multiplicación, como, por ejemplo, quiero que se repitan los valores dos veces, vale, de mi variable a, por ejemplo, llamo a la variable a y quiero que se repita dos veces, entonces hago a por 2, ejecutamos y obtenemos una lista que contiene ahora 4 elementos, notar que originalmente la lista contenía 2, también podemos ver que se está repitiendo dos veces dos elementos de la lista, ahora tenemos 1, 2 y también 1,2, otro ejemplo haciendo uso de las dos formas de concatenación, por ejemplo, creamos una lista que se llame c, por ejemplo colocamos un string hola, hacemos otro elemento que se llame como estás, en esta misma celda vamos a crear otra lista, abrimos corchetes y colocamos otros strings, por ejemplo, Python, y un número entero 5, en esta misma celda lo que vamos a hacer es concatenar estas dos variables, vale, en una lista nueva, le vamos a asignar un nombre de una nueva variable y después de sumar y después de concatenar estas dos listas, lo que queremos es que se multiplique por 3, es decir, repetir los elementos 3 veces en otra nueva lista, bien, entonces vamos a ejecutar estas líneas para que se guarden los valores dentro de mi variable cid y la tarea era obtener una nueva lista, por ejemplo, le llamo e, el cual sea la concatenación de estas dos variables, como lo hacemos hacemos de esta forma c más de, la variable e va a contener el resultado de la concatenación de estas dos variables, y en esta misma celda vamos a imprimir el valor de e, ejecutamos y efectivamente obtenemos la concatenación de estas dos variables, como segunda tarea dentro de lo que habíamos dicho hace un momento, nosotros queríamos multiplicar por 3 veces esta lista y guardarla en una nueva variable, es decir, queremos multiplicar o repetir 3 veces los elementos que contiene mi lista, hacemos lo siguiente, creamos una nueva lista o una nueva variable perdón que se guarde esta multiplicación, entonces que tengo que llamar a la variable e, que contiene estos elementos y quiero que se repitan 3 veces, hago e por 3 e imprimo el valor de la variable de, ejecutamos y efectivamente obtenemos la repetición de los valores de la lista que contiene mi variable e por 3 veces, hola como estas python y el entero número 5 3 veces