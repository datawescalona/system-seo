Muy bien, un entorno de desarrollo integrado o entorno de desarrollo interactivo es lo que comúnmente le llamamos en informática un IDE. Esta es una aplicación que proporciona servicios integrales para facilitarle al desarrollador justamente la programación de algún software. Bien, este proporciona un editor de lenguaje que se está utilizando. Con el propósito de escribir código es básicamente la parte gráfica. Un ejemplo de esto es el notebook de Jupyter. Bien, que justamente es la herramienta que ahora vamos a estudiar y para esto te pido toda la atención que me puedas dar. Esta es una herramienta muy versátil, podemos guardar nuestros informes en PDF, en HTML, incluso en punto PI, que es básicamente el script. El notebook naturalmente lo vamos a guardar en el formato punto PI nb y en este formato podemos hacer lo que queramos. Nota obvies con lo que acabamos de decir, esto lo vamos a ver paso a paso, detalle a detalle. Si te das cuenta, tengo abierto el Anaconda Navigator y en él podemos ver las diferentes aplicaciones que están instaladas o que podemos instalar dentro del entorno que está activo. Ahora mismo tenemos el entorno base root, bien que está activo. Algo muy importante, cuando tú instales el Anaconda Navigator probablemente no tengas el Jupyter Notebook instalado. El Jupyter Notebook no viene nada más instalando el Anaconda Navigator, lo tenemos que instalar nosotros mismos. Para poder instalarlo primero nos vamos a tener que ubicar en un entorno de trabajo. Puede ser base, bien sin ningún problema, pero te recomiendo que crees tu propio entorno de trabajo para este curso. Bien, vamos a abrir por ejemplo en prueba entorno. Vamos allá, vamos a esperar que cargue unos segundos, avísame más tarde. Ya estamos posicionados en el entorno de prueba y como verás en las aplicaciones que están disponibles, no tengo, no visualizo el Jupyter Notebook. Entonces como lo instaló? Fácilmente vamos a Enviroments, nos posicionamos en prueba entorno, le daremos clic al play y vamos a abrir el terminal. Para poder instalar el Jupyter Notebook vamos a utilizar un comando que es el siguiente, Conda Install Jupyter. Ahora tenemos la famosa pregunta, muy bien si procedemos o no, entonces le vamos a dar el ok, tecleando la letra y ejecutamos y esperamos unos segundos más. Muy bien, ahora sí, ya lo tenemos. Hemos instalado el Jupyter Notebook dentro del entorno de prueba, vamos a minimizar la terminal y vamos a regresar a la pestaña de home. Si te das cuenta, aún no puedo visualizar el Jupyter Notebook, no veo la aplicación dentro del entorno, bien, a pesar que lo tenemos activo. Entonces lo que comúnmente hacemos es reiniciar la aplicación de Anaconda Navigator. Entonces lo cerramos, le daremos kit y lo vamos a abrir nuevamente. Vamos a esperar que abra. Muy bien, una vez tenemos reiniciado el Anaconda Navigator, nos vamos a posicionar en el entorno de trabajo donde hemos instalado el Jupyter Notebook. Aquí una cosilla puntual, bien, es necesario recordar que el Jupyter Notebook no viene nada más instalando el Anaconda Navigator, como lo hace el pip, bien, recordar eso. Vale, entonces vamos a activar entorno que es prueba, vamos a esperar unos segundos, a ver, dos segundos más, le vamos a decir no a esta ventana de actualización y dentro de las aplicaciones que tenemos disponibles dentro del entorno que está activo, que en este caso es prueba, bien, podemos observar el Jupyter Notebook. Para abrir el Jupyter tenemos dos formas, la primera es haciendo uso de la Anaconda Navigator justamente y le vamos a dar clic en launch, bien, le damos y como verás hemos abierto un Jupyter. Muy bien, vamos a cerrarlo para mostrarte la segunda forma y la segunda forma es usando el terminal, vamos a ir a prueba entorno en el botón de play, vamos a abrir un terminal, lo tenemos abierto, bien, a ver, para abrir un Jupyter Notebook tenemos que posicionarnos en el entorno de trabajo donde estamos trabajando, en este caso nuestro entorno de trabajo es el entorno de prueba, bien y como verás lo tenemos activo, en este caso no tenemos que saltar, no tenemos que cambiar de entorno, muy bien, entonces el comando que vamos a utilizar para poder abrir un Jupyter es el siguiente, Jupyter Notebook, si escribes este comando, esta línea y ejecutas vamos a abrir de la misma forma como hemos abierto haciendo uso de la Anaconda Navigator, vamos a ello ejecutamos y como podrás observar en un segundo, abrimos y tenemos nuevamente el Jupyter abierto, da igual la forma en como lo hagas, bien, es lo mismo, da el mismo resultado, bien, lo importante es que te sientas cómodo, vale, una vez abierto el Jupyter podemos visualizar los diferentes directorios, diferentes rutas que podemos navegar dentro de nuestro sistema, por ejemplo si quiero navegar dentro de documentos lo puedo hacer dando clic encima del y dentro de documentos podemos observar los diferentes archivos, diferentes folder que tenemos guardados, para regresar podemos hacer uso del árbol que tenemos aquí, para regresar atrás simplemente le podemos hacer clic al icono que hace referencia a un folder, bien, entonces hacemos clic y regresamos atrás, por ejemplo ahora quiero navegar dentro de desktop o escritorio y por ejemplo si quiero seguir navegando en un archivo que está dentro de escritorio, por ejemplo he creado el folder de prueba, bien, vamos a entrar a él y dentro de prueba quiero seguir navegando, quiero seguir ingresando a los diferentes archivos o folder que están dentro de él, entonces le daré clic a 1 y si te das cuenta dentro del folder 1 hay un documento de texto que también se llama 1, ahora como puedo regresar pues como habíamos dicho hacemos uso de este pequeño árbol, árbol de ruta por así decirlo bien, ahora quiero regresar atrás, quiero regresar al folder de prueba, simplemente hago clic aquí en prueba, hacemos clic y como verás dentro de prueba tenemos el folder 1, bien ahora quiero regresar a desktop, vamos a desktop, podemos visualizar nuevamente los archivos que están dentro de esta ruta, ahora si quiero regresar al origen simplemente le puedo dar otra vez clic al icono que hace referencia a un folder, en primer lugar y antes de empezar antes de abrir un Jupyter Notebook lo que tienes que hacer es definir una ruta de trabajo, en mi caso voy a definir 1 dentro del escritorio, vamos a darle clic a desktop y ya estamos posicionados dentro de esta ruta, lo que vamos a hacer y toma atención, vamos a hacer clic en New, lo puedes ver en la parte superior, en la parte derecha, hacemos New y le vamos a dar clic en Folder, hacemos folder y si te das cuenta hemos creado un folder vacío, es este, muy bien y si te das cuenta incluso aquí dice la fecha en que lo has creado o la última modificación que tiene un archivo, un folder, incluso su tamaño, vale, muy bien, lo que vamos a hacer es lo siguiente, para cambiar el nombre le podemos hacer clic aquí, vale, en la parte de izquierda, en la parte de arriba vamos a darle clic a Rename y lo vamos a llamar por ejemplo zona de trabajo para el curso de Python, hacemos Rename aquí y si te das cuenta el Python que no tenía un nombre en específico, he podido cambiar el nombre sin ningún problema, muy bien, ahora como te dije nosotros podemos navegar dentro de los escritorios, dentro de las rutas, dentro de los folder, bien, de todos los archivos que tenemos disponibles dentro del sistema, entonces lo que vamos a hacer es ingresar en este folder, si te das cuenta, la ruta es escritorio y después zona de trabajo para el curso de Python y lo que vamos a hacer ahora así es crear un Jupyter Notebook y es de la siguiente forma, nuevamente nos vamos a posicionar en el botón New le vamos a dar clic y al abrir si te das cuenta aquí dice Python 3, pero arriba dice Notebook, bien, le vamos a dar clic a Python 3 y vamos a abrir nuestro primer Jupyter Notebook, este es el Jupyter Notebook, te lo presento, en la parte de arriba se crea con un nombre digamos vacío, bien, que nosotros podemos modificarlo y como simplemente le damos clic aquí y me parece una ventana para cambiarle de nombre, puedo llamarle como yo quiera, por ejemplo quiero llamarle Notebook de ejemplo, de esta forma le cambio de nombre, voy a rename y si te das cuenta ya le he cambiado de nombre, vale, entonces si yo voy atrás dentro de mi carpeta de zonas de trabajo para el curso de Python podemos visualizar el nuevo Jupyter Notebook que he creado dentro de él, bien, y si te das cuenta el formato natural o original es lo que habíamos dicho en un principio, termina en punto y pi nb, bien, este es el formato del Notebook, muy bien, entonces regresemos al Jupyter Notebook y vamos a ver cada detalle del Jupyter Notebook.