Muy bien, ahora vamos a iniciar con la instalación de las herramientas de trabajo, y básicamente trabajaremos con 3 de ellas. En primer lugar tenemos a la Anaconda Navigator, que es una distribución abierta de Python, que simplifica la gestión e implementación de librerías y entornos de trabajo. Como segunda herramienta tenemos a PIP, que es un gestor de paquetes, y finalmente a Jupyter Notebook, que combina código de Python con texto e imágenes. Muy bien, llegó el momento de iniciar a instalar las herramientas que usaremos en el curso. En primer lugar, buscaremos en el navegador anaconda.com, ¿vale? Y Anaconda Navigator es un software gratuito que instala Python y una serie de herramientas adicionales para programar en él. En primer lugar, vamos a buscar en la pestaña de productos la versión personal que se encuentra en Anaconda Distribution. Le daremos clic, y en esta zona vamos a encontrar los diferentes instaladores para los diferentes sistemas operativos disponibles. Tenemos Windows, tenemos Mac y tenemos Linux. Daremos clic aquí, y encontramos los diferentes instaladores que responden a los diferentes requisitos de cada sistema operativo. En este caso, y por requisitos de mi ordenador, voy a descargar la versión del 64-bit. Tenemos un mensaje de gracias por tu descarga y vamos a esperar hasta obtener el instalador, ¿vale? Demora un poquito porque pesa un poco más de 500 megas. Perfecto, lo vamos a abrir, ¿vale? Y veamos cómo lo podemos setear. Simplemente, en la primera ventana, vamos a darle Next, vamos a aceptar las condiciones. Bien, aquí continuamos con el Next, aquí tenemos la posibilidad de colocar un folder destino de donde queremos que esté nuestro Anaconda. Bien, incluso nosotros lo podemos seleccionar desde el botón Browse, bien, o aquí directamente podemos colocar un nombre del folder donde queremos que esté. Por ejemplo, en mi caso, quiero colocarle Introduction. Haremos Next, y finalmente colocamos Install, ¿vale? Esperamos que cargue un poco. Una vez instalado Anaconda, continuamos con el botón Next, y finalmente el botón Finish. Bien, una vez tenemos el Anaconda Navigator instalado en nuestro ordenador, vamos a proceder a ingresar en él. Bien, aquí tenemos muchas posibilidades para ingresar y la manera más fácil, más sencilla es haciendo uso del acceso directo que se ha creado en nuestro escritorio. Bien, vamos a darle click, esto espera unos minutillos, bien, en abrir, demora un poco. Muy bien, perfecto. Ya tenemos abierta nuestra interfaz gráfica de Anaconda Navigator, y en él vamos a poder trabajar con diferentes herramientas haciendo uso de Python. En la pestaña principal, o en la pestaña de home, vamos a poder visualizar las aplicaciones instaladas en ella. Nota gobies si no lo tienes igual porque evidentemente he ido instalando diferentes aplicaciones a través del tiempo. Lo importante es conocer el contexto de trabajo usando Anaconda, y más importante aún, y es lo que me interesa que conozcas y que pongas mucha atención porque es crucial, es el concepto del entorno de trabajo. Anaconda nos permite crear diferentes entornos de trabajo para diferentes objetivos. Un entorno de trabajo es un conjunto de paquetes de Python que cumple un fin. Por ejemplo, si queremos un entorno para aplicar un algoritmo de machine learning en específico, lo podemos hacer sin ningún problema. Otro entorno para el curso de introducción a Python, también otro para inteligencia artificial, también sin ningún problema y sin ninguna restricción y entre otras cosas, ¿vale? Y te estarás preguntando y por qué creamos entornos para diferentes fines, pues cada proyecto a menudo tiene sus propias librerías y una propia versión de Python. Bien, esto porque se debe de cumplir con ciertos requisitos mínimos. Por ejemplo, si la aplicación A necesita la versión 5.0 de un módulo en particular y la aplicación B necesita la versión 6.0, entonces los requisitos entran en conflicto y el proyecto sería incapaz de ejecutarse. Entendamos esto como una dependencia. Por ejemplo, si solíamos usar una función dentro de una hoja de cálculo del Excel 2016 en un ordenador antiguo, a lo mejor esta función sea obsoleta con nuestro nuevo ordenador que tiene instalado a lo mejor la versión del Excel 2021. En este caso, que es lo que tendríamos que hacer pues es actualizar la función para que cumpla con los nuevos requisitos de nuestra nueva versión del Excel. Para que se entienda un poco lo que hemos dicho, vamos a ir en la pestaña de environments. Vamos a ver que en la zona inferior tenemos diferentes botones y le vamos a dar clic en el botón de create. Tenemos abierto una pequeña ventana y en él vamos a poder colocar un nombre cualquiera. Para este entorno, por ejemplo, le vamos a poner introducción programación. Por ejemplo, seguidamente vamos a poder ver la zona donde se va a instalar este entorno. Bien, ahora vamos a hablar de él. Seguidamente y por último vamos a poder ver la zona de package. Vemos que podemos instalar tanto Python o R o lo dos a la vez sin ningún tipo de problema. Como este curso es de Python, nos interesa solamente instalar Python. En esta zona, vamos a poder seleccionar la versión que nosotros queramos y le vamos a dejar en 3.7. Muy bien, entonces vamos a darle a create. Vamos a esperar un momento. Esto toma su tiempo, ¿vale? Y si te das cuenta se está instalando en una carpeta en específico. Bien, estamos hablando de environments, barra invertida, introducción, programación. Bien, recuerda esto porque ahora vamos a ver la diferencia al momento de instalar diferentes entornos. Bien, vamos a esperar unos segundos más. Muy bien, unos segundos más. Vale, perfecto. Y aquí me gustaría mostrarte a modo de ejemplo lo que te contaba sobre los entornos. Vamos a crear otro entorno y para ello vamos a situarnos en la parte inferior y vamos a darle nuevamente en el botón create. Ahora en esta ventanita, como hemos hecho en el principio de crear el primer entorno de introducción, programación, vamos a darle un nombre cualquiera, ¿vale? Prueba entorno. Y en package lo dejaremos seleccionado en Python y en esta ocasión quiero seleccionar la versión de 3.6 por ejemplo. Bien, 3.6 y la daremos create nuevamente. Notar que en esta ocasión este nuevo entorno se va a crear en otra ruta distinta. Muy similar a la de la primera, pero en esta ocasión estamos hablando de environments, barra invertida, prueba entorno. Notar que ambos entornos están creando en diferentes zonas de nuestro ordenador. Bien, vamos a esperar unos segundos más hasta que termine las dependencias del entorno. Muy bien, recordar que ambos entornos han sido creados en diferentes zonas, diferentes rutas de nuestro ordenador y ahora vamos a probar que ambos son independientes. Ambos entornos podrían estar creándose para diferentes objetivos como hemos dicho. Bien, para este ejemplo vamos a situarnos en prueba entorno y en esta zona, en este filtro, si lo abrimos, vamos a poder ver las librerías que están instaladas, las librerías que no están instaladas y las que se pueden actualizar. Ahora, lo que vamos a hacer es lo siguiente. Vamos a instalar una librería que se llama TensorFlow. Vamos a buscar en search package TensorFlow y como notarás, Byton no muestra ninguna respuesta porque justamente seguimos seleccionando las librerías instaladas. Como verás TensorFlow aún no lo instalamos. Entonces, donde lo tenemos que buscar? Lo tenemos que buscar en no instalados. Muy bien, ahora sí como podrás visualizar si tenemos respuesta de Byton y lo que voy a hacer es seleccionar uno cualquiera, por ejemplo este. Bien, seleccionamos y vamos a dar en el botón aplicar. Vamos a esperar a que se instale esta librería en este entorno de prueba. Vamos a esperar unos segundos. Bien, ahora lo que vamos a hacer es tratar de instalar una librería dentro de mi entorno de prueba y para ello vamos a buscarlo en este buscador donde pone search package. Bien, entonces vamos por ejemplo a colocar ADS. Anaconda Navigator como podrás observar no muestra ningún resultado porque aún tenemos filtrado a las librerías que están instaladas. Bien, lo que tenemos que hacer es filtrar por no instalado. Como podrás notar, ahora sí podemos visualizar todos los resultados que hacen referencia a la búsqueda ADS. En este caso y a modo de ejemplo vamos a seleccionar esta y vamos a darle aplicar. Muy bien, volvemos a darle aplicar. Ahora ya lo tenemos instalado y como podemos buscarlo pues podemos filtrar con instalados. Lo tenemos aquí. Ahora vamos a quitar esta búsqueda. Bien, dentro de todos los paquetes que tenemos instalado dentro de mi entorno, ahora encontramos la librería ADS. Ahora, ¿qué pasa si me cambio al primer entorno que he creado? ¿Qué es introducción, programación? Vamos a darle clic y veamos si podemos encontrar la librería ADS. Vamos a esperar a qué cargue. Ya estamos dentro de nuestro entorno de introducción, programación y como podrás observar dentro de las librerías que están instaladas, no podemos observar la librería ADS. Esto porque no está instalado. Ahora vamos a validarlo filtrándolo por ejemplo en no instalados. Si buscamos aquí en search package ADS, lo podemos encontrar. Esto deja claro que los entornos son independientes. Cada uno tiene su propia estructura y su propio objetivo como lo hablamos anteriormente. Ahora para poder eliminarlo simplemente le vamos a dar a eliminar de esta forma. Por ejemplo quiero borrar el entorno de prueba. Nuevamente me paso a ella dándole clic. Vamos a esperar. Una vez cargado el entorno vamos a situarnos en la parte de abajo y vamos a darle clic al botón de remove. Python nos muestra un mensaje. Bien, para ver si estamos totalmente seguros de borrar el entorno. En este caso como es un entorno de prueba lo borramos sin ningún problema. Entonces le damos remove. Y Anaconda va a proceder a borrarlo junto a las librerías que contenía. Vamos a esperar unos segundos y vamos a ver que ya no lo vamos a poder ver dentro de nuestras listas de entornos. Vale, perfecto. Muy bien, como podrás notar, ya no está dentro de nuestra lista de entornos. Bien, el entorno de prueba que creamos y ahora hemos eliminado. Y recuerdas que el entorno de prueba fue creado con Python 3.6 y el entorno de introducción barra baja programación fue creado con la versión de Python 3.7. Aquí también entendamos que al cambiar entre versiones de Python puede significar cambios de sintaxis en el código. Eso es importante que al crear nuestro entorno de trabajo lo instales con la versión 3.7. Esto para evitar errores al ejecutar lo que trabajemos en el curso. Otra cosa que me gustaría que sepas y que conozcas que nosotros ya tenemos un entorno que viene predifinido nada más instalando Anaconda Navigator. Este entorno es el entorno base o también llamado root. En este entorno base, si nosotros filtramos por instalados en la parte de abajo, podemos observar más de 300 paquetes que están habilitados listos para usarse. Puedes usar este entorno si lo prefieres, pero te recomiendo usar diferentes entornos que tú mismo vayas instalando las librerías que van a ser necesarias para tu programa. Por otro lado hablemos del entorno base o el entorno root que viene de forma predeterminada al momento de instalar Anaconda Navigator. Si nos situamos en él y vamos al filtro de instalados y borramos el filtro de ADS, vamos a poder visualizar un montón de librerías que también vienen predefinidas dentro de él y como podrás visualizar en la parte inferior, pone que tenemos más de 300 paquetes ya listos para ser usados. Si bien es cierto, puedes usar este entorno sin ningún problema, pero te recomiendo que crees tu propio entorno de trabajo. Puedes colocar la versión que tú quieras, puedes instalar las librerías que solo te hagan falta y además vamos a decir y esto es muy importante que al cambiar entre versiones de Python, puedes significar cambios de sintaxis en el código. Es importante que al crear tu entorno de trabajo para este curso, lo instales con la versión 3.7. Esto para evitar errores, al ejecutar lo que trabajemos durante todo el programa. Antes de tratar la segunda herramienta que vamos a trabajar en el curso, quiero hacer un pequeño resumen de lo que hemos visto en Anaconda Navigator. Anaconda nos permite crear diferentes environments, diferentes entornos de trabajo y cada entorno tendría sus propias librerías y una propia versión de Python. Puedes tener un entorno para un proyecto que necesite una versión específica de tal paquete o una versión inclusive que sea antigua, bien por ejemplo el 3.5. Cuando vayas a home seguramente te habrás preguntado para qué sirve este filtro de applications on, pues puedes seleccionar en este filtro el entorno con el cual quieras trabajar. Previamente antes de abrir alguna aplicación que tenemos disponible, por ejemplo si quiero seleccionar introducción a programación, lo puedo hacer de esa forma, esperar que cargue el Anaconda Navigator, bien una vez cargado puedo abrir las aplicaciones que están disponibles dentro de este entorno de trabajo. Ahora, unas cosillas rápidas, bien aquí en este botón de sync in quiere decir que te puedes loguear, bien y tener algunos beneficios de tener una cuenda en Anaconda, puedes tener backup para guardar tus modelos, ciertas versiones de tus programas. Por otro lado si es que vas en Byromance, aquí puedes ver dos botones, puedes actualizar versiones de paquetes en un entorno haciendo clic en Update Index y si quieres previamente puedes ingresar a un entorno específico simplemente dándole click. Y este botón de channels también sirve para actualizar paquetes pero le puedes pasar inclusive una URL donde puede estar direccionado el paquete con la versión que quieres actualizar. Muy bien esas cosillas rápidas y bien hablemos sobre la segunda herramienta que vas a referido a trabajar en base a la línea de comandos, todo lo que hemos hecho es decir crear un entorno de trabajo, actualizar un paquete, instalar alguna librería, bien en un entorno específico lo podemos hacer también sobre una línea de comandos, por ejemplo si yo me posiciono en este entorno base, vamos a esperar que cargue, ya estoy posicionado aquí, tengo cargado inclusive de todas las librerías que están disponibles dentro de mi entorno base y puedo hacer lo siguiente, puedo abrir un terminal dándole click a este botón play y en él me dice que puedo abrir el terminal pues hacemos open terminal y nos abre una línea de comandos, que es una línea de comandos, una línea de comandos o también llamada consola o terminal por ejemplo es el cmed de windows y nos permite escribir o ejecutar comando sobre el sistema operativo, la interfaz de línea de comandos nos permite dar instrucciones a las computadoras a través de texto plano, dichas instrucciones se ejecutan en una capa de bajo nivel que permite que la velocidad de ejecución sea notoriamente superior a la interfaz gráfica de usuario, la interfaz gráfica de usuario es justamente este programa que estamos viendo en pantalla, esto es una interfaz gráfica, la línea de comandos la podemos aprovechar por su alta velocidad y qué puedo hacer con una línea de comandos pues puedo navegar entre directorios, puedo crear, puedo mover, puedo copiar, puedo remover archivos y carpetas, puedo ejecutar archivos o programas, ver y detener procesos, crear tareas programadas para automatizar algún tipo de proceso, hacer despliegue de sitios web y ejecutar comandos especializados, incluso configurarlos para ciertos programas, entonces una vez entendido el contexto o lo que es una línea de comandos vamos a explicar ciertos comandos que van a ser importantes que sepas y a lo mejor este ya eres un usuario avanzado en línea de comandos y a lo mejor simplemente necesites la formación de ciertos comandos que son básicos, es muy fácil en realidad y veamos el primero, nosotros como había dicho puedo crear un entorno a partir de la línea de comandos, de la siguiente forma puedo hacer conda, create y colocar el nombre del entorno nuevo que quiero que quiero crear por ejemplo hago prueba, barra baja 2, vamos a darle enter, create, bueno me salta una pregunta si procedo o no, voy a dar la yes y vamos a esperar unos segundos para que la interfaz gráfica lo pueda visualizar, vamos a dar unos segundos y en un momento vamos a ver en nuestra lista de entornos disponibles un entorno que justamente se va a llamar prueba barra baja 2, ya lo estamos viendo, se están creando sus dependencias con algunas librerías ya predefinidas y lo tenemos, nosotros al crear un entorno anaconda lo descarga con la última versión disponible de python y esto es automático, que pasa si a lo mejor nuestro entorno de trabajo está orientado a trabajar sobre un programa que necesite una versión anterior de python, por ejemplo mi programa necesita la versión 3.5 de python, bien porque ha estado trabajando o he estado trabajando mejor dicho en algunos paquetes que responden a esta versión de python y si quiero crear un entorno con justamente esta versión 3.5 de python como lo hago, puedo hacerlo de la siguiente forma, puedo poner conda, create, guion n, el nombre del entorno, vamos a hacer prueba barra baja 3, por ejemplo y aquí vamos a colocar python igual a 3.5, vamos a ejecutar dándole enter, vamos a esperar un momento, ahora me pregunta si quiero proceder, aquí me visualiza ciertos paquetes que se van a instalar, que se van a construir dentro del entorno de trabajo, entonces le doy yes, le doy enter para que proceda y aquí se están instalando los paquetes que va a tener mi entorno de trabajo, recordar que los entornos de trabajo que vamos creando vienen con ciertas librerías ya predifinidas, bien incluso el entorno base venía ya con más de 300 paquetes instalados, en esta ocasión ya tenemos el entorno creado, vamos a esperar unos segundos para ver si en la interfaz gráfica se encuentra este entorno en nuestra lista de entornos disponibles, recordar que se llama prueba barra baja 3, vamos a esperar unos segundos, muy bien, ahora como puedes ver en la lista de entornos disponibles ya tenemos disponible el entorno prueba barra baja 3, lo tenemos listo para trabajar en él, muy bien, este entorno de prueba barra baja 3 lo he instalado con la versión de python 3.5 a través de la línea de comandos, ahora si te das cuenta en la parte inicial de esta línea dice base y está entre paréntesis, esto quiere decir que estoy ahora mismo posicionado en el entorno de trabajo llamado base o route, ahora como puedo cambiar desde la línea de comandos a otro entorno de trabajo pues tengo que hacer lo siguiente hago conda activa y el nombre del entorno, en este caso quiero ir a prueba barra baja 3, si te das cuenta ya no tengo base dentro del paréntesis al inicio de esta línea ahora tengo la prueba barra baja 3, quiere decir que ahora ya estoy posicionado en mi entorno de prueba 3, ahora quiero preguntar la versión de python que tiene el entorno de prueba barra baja 3, puedo hacer lo siguiente puedo hacer conda python guion versión, me dice que es un comando que no existe pues hago lo siguiente hago python barra baja v en mayúscula y podemos visualizar que me dice que python o que mejor dicho este entorno de trabajo de prueba barra baja 3 tiene una versión de python de 3.5.6 justamente la versión 3.5 que habíamos creado ahora porque este punto 6 pues son las subversiones que se van creando que se van instalando y este al momento de de tu darle 3.5 puede que te instale la última versión referida al 3.5 se puede instalar por ejemplo el 3.6 y puedes tener 3.6.8 3.6.9 pero al final eso no tiene mucha importancia no le das caso a ello y ahora para validar la versión de python con el cual hemos instalado este entorno de trabajo vamos a hacer lo siguiente vamos a preguntar si la versión de python concretamente es la versión de 3.5 como lo hemos instalado hace un momento bien para eso hacemos python guion guion y la v en mayúscula para eso le daremos enter que pasó aquí a ver python-h bien lo que podemos hacer vamos de nuevo python barra baja versión bien obtenemos efectivamente el python 3.5 bien para el entorno de prueba barra baja 3 lo que intenté al principio no es con doble guion sino con solo uno bien también podemos preguntarle la versión de python de esta forma hacemos python un solo guion y la v en mayúsculas hacemos enter y obtenemos el mismo resultado que el entorno de trabajo prueba barra baja 3 contiene la versión de python 3.5 bien ahora también podemos al momento de crear el entorno y al momento de decirle la versión de python con el cual queremos trabajar podríamos también instalar simultáneamente alguna librería por ejemplo quiero crear otro entorno por ejemplo konda create el nombre del entorno guion n pruebas barra baja 6 por ejemplo y quiero también instalar por ejemplo esta esta librería que es es si pie bien entonces vamos a dar lenter se está creando mi entorno pruebas barra baja 6 le iremos yes te creamos la letra y hacemos enter para aceptar esperamos un momento bien una vez terminado de instalar nuestro entorno y también habiendo instalado precisamente una librería en este caso y para este ejemplo es y si pie lo que vamos a proceder es averiguar qué versión de python se creó junto a este entorno recordar que he creado el entorno pruebas barra baja 6 no estoy en él todavía aún continuo con el entorno prueba barra baja 3 bien y para poder salir de él e ingresar a este entorno como hacíamos hacíamos lo siguiente konda activa y pruebas para baja 6 vamos a ejecutar dándole enter bien como verás ahora estamos posicionados en el entorno pruebas barra baja 6 entonces vamos a preguntar qué versión de python tiene este entorno vamos a darle y tenemos el python 3.9 porque porque no especificamos la versión de python al momento de crear mi entorno por lo que anaconda lo instaló con la última versión que tiene de python ahora si queremos crear otro entorno por ejemplo create guion n vamos a colocar un nombre cualquiera por ejemplo nombre entorno ya no voy a crearlo porque ya estamos creando bastantes nombre entorno y lo que voy a colocar por ejemplo va a ser pandas este es el nombre de la librería bien recordar que podemos instalar una librería y también simultáneamente vamos a poder colocar una versión de python puedo hacer lo mismo que hicimos hace dos ejemplos python 3.5 o 3.6 lo que yo quiera bien con esta línea vamos a poder crear un entorno de trabajo con la versión de python que nosotros queramos y adicionalmente vamos a tener la posibilidad de instalarlo a su vez bien con una librería con la librería que nosotros queremos muy bien no lo voy a ejecutar lo voy a dejar allí para no estar llenándonos de varios entornos de trabajo muy bien por otra parte una cosa que podemos hacer y ya lo he hecho a lo largo de estos minutos este puedo desactivar entornos de trabajo puedo salir de entornos de trabajo por ejemplo esto lo voy a borrar esta línea para no volver a crear un entorno de trabajo por ejemplo lo que voy a hacer es lo siguiente quiero salir de este entorno de trabajo como lo hacemos hacemos lo siguiente hacemos conda y colocamos de activate de esta forma que pasó cuando nosotros desactivamos colocando este comando con de activate estando ya posicionados en un entorno de trabajo simplemente salimos de él y regresamos al entorno que estuvimos anteriormente bien si te das cuenta antes o previamente al estar o al posicionarnos a pruebas barra baja 6 estuvimos en prueba barra baja 3 bien por eso nos direccionó al anterior bien por ejemplo si quiero es desactivar prueba barra baja 3 veamos a donde nos lleva voy a hacer conda de activate otra vez ejecuto y ahora me lleva al entorno base al entorno ruto y así podríamos ir saliendo o desactivando los entornos de trabajo y nos va a redireccionar al entorno que anteriormente estuvimos otro comando interesante con el cual podemos averiguar cuáles son las librerías bien o los paquetes que viene instalado en un entorno por ejemplo nos posicionamos en pruebas barra baja 6 bien y lo que vamos a hacer es lo siguiente vamos a tequear con la lista con la lista vamos a ejecutar le damos enter y vamos a obtener una lista de las librerías que tenemos instalados dentro de mi entorno pruebas barra baja 6 junto a sus versiones bien y como te das cuenta en la lista tenemos a ese y pie justamente porque nosotros al momento de crear este este entorno de trabajo adicionalmente le añadimos para que instale a su vez la librería y si pie dentro del bien si bien es cierto sabemos que cada entorno que nosotros creamos viene predefinido con ciertas librerías bien ahora ya tengo la lista de paquetes de librerías junto a sus versiones bien y qué pasa si quiero eliminar una librería que pasa si ya no la voy a usar lo que puedo hacer para poder eliminar una librería es por ejemplo hacemos conda remove y quiero eliminar para este ejemplo y si pie bien hacemos lo siguiente colocamos después de remove y si pie el nombre de la librería explicitamente que quiero eliminar y le vamos a dar a ejecutar veamos qué sucede se está preparando para eliminar la librería bien ahora nos dice si proceder o no entonces le teclamos la letra y damos enter y sigue procediendo con el borrado de la librería y si pie esperamos unos segundos más muy bien se acaba de completar el proceso de borrado de la librería y si pie y ahora para poder averiguar si realmente se ha eliminado lo que pueda hacer es listar las librerías que tengo dentro de mi entorno bien y como hicimos hace un momento hago conda list bien ejecuto y veamos si y si pie todavía está dentro de las listas no está bien efectivamente la librería y si pie ya nos encuentra dentro de las listas que están instaladas dentro de mi entorno pruebas barra baja 6 muy bien otro comando muy importante muy interesante que nos va a permitir desinstalar o quitar algún entorno que ya no queramos y que probablemente ya no queramos trabajar ni con las librerías que contiene porque recordamos que si borramos nosotros un entorno además de borrar el entorno vamos a borrar las librerías que lleva dentro entonces hay que tener mucho cuidado al momento de eliminar un entorno hay que estar totalmente seguros bien entonces lo que podemos hacer es lo siguiente colocamos conda en bayron en en b remove guión en y vamos a colocar seguidamente el nombre del entorno que queremos borrar tener en cuenta que para eliminar un entorno lo primero que debemos hacer será asegurarnos de que no estamos en el entorno que queremos borrar por ejemplo en estos momentos estamos en pruebas barra baja 6 lo que quiere decir que no vamos a poder eliminar este entorno porque está activo entonces lo que vamos a tratar de borrar es el entorno pruebas barra baja 2 vamos a colocar aquí prueba barra baja 2 vamos a ejecutar y vamos a esperar a que se elimine el entorno y justamente ya se eliminó ya además eliminó todos los paquetes que estaban dentro de mi entorno prueba barra baja 2 bien ahora quiero borrar prueba barra baja 3 hago lo mismo guión en prueba barra baja 3 ejecutamos y de igual forma también se ha eliminado el entorno prueba barra baja 3 junto a todas las librerías que estaba instalado dentro de ella bien entonces ahora vamos a tratar de eliminar para comprobar lo que he dicho hace un momento de que no podemos borrar un entorno si está activo como sabemos que un entorno está activo porque al principio de la línea bien vamos a ver su nombre dentro del paréntesis entonces pruebas barra baja 6 en estos momentos está activo y no vamos a poder eliminarlo hagamos una prueba con la nb remove guión n y vamos a colocar el nombre de pruebas barra baja 6 vamos a ejecutar y efectivamente con da nos envía un error que no podemos eliminar como dije un entorno si está activo bien lo que podemos hacer es desactivarlo o simplemente movernos a otro entorno por ejemplo tengo que moverme a con da activa te base por ejemplo bien vamos a ejecutar para movernos y ahora sí vamos a tratar de borrar el entorno guión n de pruebas barra baja 6 vamos a ejecutar le damos enter vamos a esperar unos segundos y efectivamente ahora sí se ha eliminado el entorno de pruebas barra baja 6 junto a todos los paquetes que llevaba instalado por último vamos a conocer ciertos comandos que nos permitirán conocer o averiguar mejor dicho la versión que tenemos instalada de anaconda navigator para ello hacemos con da guión guión versión bien ejecutamos y obtenemos la versión de 4.12 y qué pasa si quiero actualizar la versión que tengo ahora mismo instalada pues hacemos con da update y seguidamente colocamos anaconda guión navigator bien en este caso yo no lo voy a hacer no lo ejecutar porque no quiero actualizar la versión que tengo ahora mismo instalada bien lo quiero dejar allí pero si guarda esta línea bien por último y para cerrar con el tema de anaconda se podría decir que aprendimos todo lo necesario para poder empezar a trabajar con los entornos de trabajo y todo ello lo hemos hecho usando tanto la interfaz gráfica como la línea de comandos recuerda que el resultado es el mismo pero también tener en cuenta que usando la línea de comandos obtenemos los resultados con una mayor velocidad nota gobies ya uno te sientes cómodo usando la línea de comandos puedes iniciar sin ningún problema usando la interfaz gráfica todos y absolutamente todos lo que estamos trabajando ahora mismo en análisis de datos hemos empezado con una interfaz gráfica y no es ningún problema